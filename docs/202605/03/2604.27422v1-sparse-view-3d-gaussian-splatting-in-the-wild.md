---
title: Sparse-View 3D Gaussian Splatting in the Wild
title_zh: 野外环境下的稀疏视角 3D 高斯泼溅
authors: "Wongi Park, Jordan A. James, Myeongseok Nam, Minjae Lee, Soomok Lee, Sang-Hyun Lee, William J. Beksi"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27422v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 使用3D高斯泼溅的稀疏视图合成框架
tldr: 本研究提出了一种针对包含干扰物的非受限真实场景的稀疏视图3D高斯泼溅框架。通过引入扩散模型引导的视图细化、瞬态掩码处理以及稀疏感知的高斯复制策略，该方法有效解决了稀疏输入下的渲染伪影和表示不完整问题。实验表明，该方法在PSNR和SSIM等指标上显著优于现有技术，为低成本、非受限环境下的高质量3D重建提供了新方案。
selection_source: fresh_fetch
motivation: 现有的稀疏视图合成方法难以处理包含瞬态干扰物的真实世界非受限图像集合，导致渲染质量受限。
method: 采用扩散模型进行参考引导的视图细化，并结合瞬态掩码、伪视图生成及稀疏感知的高斯复制策略来增强3D表示。
result: "在公开数据集上，该方法在PSNR、SSIM和LPIPS指标上分别提升了17.2%、10.8%和4.0%，实现了高保真渲染。"
conclusion: 该框架成功实现了在非受限稀疏视图下的高质量3D场景重建，显著降低了真实场景建模对数据采集的依赖。
---

## 摘要
我们提出了一种针对包含干扰物的非受限真实场景的 3D 新型稀疏视角合成框架。与现有方法不同，现有方法主要从不含瞬态元素的受限稀疏图像集中进行新视角合成，或者利用非受限的密集图像集合来增强真实场景中的 3D 表示，而我们的方法不仅能有效处理稀疏的非受限图像集合，还能展示高质量的 3D 渲染结果。为此，我们引入了基于扩散模型的参考引导视角细化，利用瞬态掩码和参考图像来增强 3D 表示并减轻渲染视角中的伪影。此外，我们通过伪视角生成以及稀疏感知的高斯复制策略来处理高斯场中的稀疏区域，从而增强这些区域的高斯分布。在公开数据集上的广泛实验表明，我们的方法始终优于现有方法（例如，PSNR 提升 17.2%，SSIM 提升 10.8%，LPIPS 提升 4.0%），并提供了高保真的 3D 渲染结果。这一进展为在无需劳动密集型数据采集的情况下实现非受限真实场景建模铺平了道路。我们的项目页面见 https://robotic-vision-lab.github.io/SaveWildGS/。

## Abstract
We propose a 3D novel sparse-view synthesis framework for unconstrained real-world scenarios that contain distractors. Unlike existing methods that primarily perform novel-view synthesis from a sparse set of constrained images without transient elements or leverage unconstrained dense image collections to enhance 3D representation in real-world scenarios, our method not only effectively tackles sparse unconstrained image collections, but also shows high-quality 3D rendering results. To do this, we introduce reference-guided view refinement with a diffusion model using a transient mask and a reference image to enhance the 3D representation and mitigate artifacts in rendered views. Furthermore, we address sparse regions in the Gaussian field via pseudo-view generation along with a sparsity-aware Gaussian replication strategy to amplify Gaussians in the sparse regions. Extensive experiments on publicly available datasets demonstrate that our methodology consistently outperforms existing methods (e.g., PSNR - 17.2%, SSIM - 10.8%, LPIPS - 4.0%) and provides high-fidelity 3D rendering results. This advancement paves the way for realizing unconstrained real-world scenarios without labor-intensive data acquisition. Our project page is available at $\href{https://robotic-vision-lab.github.io/SaveWildGS/}{here}$

---

## 论文详细总结（自动生成）

这篇论文提出了 **SaveWildGS**，这是一个专门针对“野外”非受限环境下、仅有稀疏输入图像的 3D 高斯泼溅（3DGS）框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何在仅有极少数（如 3、6、9 张）且包含大量干扰物（如行人、车辆等瞬态物体）的真实世界图像的情况下，重建高质量、一致性强的 3D 场景。
*   **研究背景**：
    *   传统的 3DGS 在密集视角下表现优异，但在稀疏视角下会因几何信息不足产生严重的伪影和深度歧义。
    *   现有的“野外”重建方法（如 WildGaussians）通常依赖大量图像来识别干扰物，在稀疏输入时失效。
    *   现有的稀疏重建方法（如 FSGS）主要针对静态受限场景，无法处理真实世界中的动态干扰。

### 2. 方法论
SaveWildGS 的核心思想是结合**视觉大模型（Foundation Models）的先验知识**与**改进的高斯密度控制策略**。
*   **参考引导的视角细化（RGVR）**：
    *   利用 **Grounded SAM** 生成瞬态掩码（Transient Mask），识别图像中的干扰物。
    *   重新设计了一个**单步扩散模型（One-step DM）**，通过交叉注意力机制（Cross-Attention），利用其他视角的参考图像来修复当前渲染视角中的损坏区域。
*   **参考引导的伪标签合成**：
    *   在未观测的角度生成伪视角图像，并利用扩散模型进行细化，作为监督信号来蒸馏 3D 表示，增强多视角一致性。
*   **稀疏感知的高斯复制（SAGR）**：
    *   针对稀疏初始化导致的“空洞”问题，不再单纯依赖位置梯度进行加密，而是引入**累积不透明度图（Accumulated Opacity Map）**作为权重，识别稀疏区域并复制高斯基元，从而构建密集的高斯场。
*   **正则化与优化**：
    *   引入 **LoRA（低秩自适应）** 对 VAE 解码器进行微调，并结合 **SDS（分数蒸馏采样）损失**，防止模型坍塌并维持几何一致性。

### 3. 实验设计
*   **数据集**：
    *   **NeRF On-the-go**：包含大量移动干扰物的真实场景。
    *   **Photo Tourism**：非受限的旅游景点地标数据集。
    *   **LLFF**：标准的受限静态场景（用于验证通用性）。
*   **基准测试（Benchmark）**：在 3 视角、6 视角和 9 视角设置下进行评估。
*   **对比方法**：包括基础方法（3DGS, Mip-Splatting）、稀疏视角方法（FSGS, RegNeRF, Difix3D+）以及野外场景方法（WildGaussians, GS-W, RobustSplat, DroneSplat）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A40 GPU**。
*   **训练时长**：
    *   NeRF On-the-go 和 LLFF 数据集：20,000 次迭代，耗时约 **2.3 GPU 小时**。
    *   Photo Tourism 数据集：50,000 次迭代。
*   **效率**：渲染速度保持在 **110 FPS** 左右，实现了高质量与高效率的平衡。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个主流数据集的多个场景上进行了详尽测试，涵盖了从 3 视角到 9 视角的极端稀疏情况。
*   **消融实验**：针对 RGVR 模块、SAGR 策略、LoRA 秩、SDS 损失以及训练视角数量等进行了多组消融实验。
*   **评价**：实验设计非常充分且客观。通过定量指标（PSNR, SSIM, LPIPS）和定性视觉对比，有力地证明了各组件的有效性。

### 6. 主要结论与发现
*   **性能突破**：在稀疏野外场景下，SaveWildGS 显著优于现有 SOTA 方法，PSNR 平均提升 **17.2%**，SSIM 提升 **10.8%**。
*   **干扰物处理**：通过视觉大模型引导的细化，模型能够成功“抹除”稀疏视角中的瞬态物体，重建出干净的静态背景。
*   **几何一致性**：SAGR 策略有效解决了稀疏输入下的高斯场空洞问题，使渲染结果在不同视角间更加平滑一致。

### 7. 优点（亮点）
*   **首创性**：据作者所述，这是首个同时解决“稀疏视角”和“非受限干扰物”两个挑战的 3DGS 框架。
*   **即插即用**：提出的视角细化模块具有通用性，可以作为插件提升其他 3D 重建方法的质量。
*   **效率高**：在引入扩散模型先验的同时，通过单步扩散和合理的优化策略，避免了训练时间的剧增。

### 8. 不足与局限
*   **静态干扰物识别难**：如果某个干扰物在所有视角中都是静止的（如违章停放的车辆），模型可能无法将其与背景分离。
*   **视角重叠依赖**：如果视角之间几乎没有重叠，参考引导的修复效果会打折扣。
*   **微小噪声**：扩散模型生成的细化图像有时会带有轻微的生成式噪声，可能影响极高精度的重建。

（完）
