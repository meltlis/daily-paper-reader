---
title: "Softmax-GS: Generalized Gaussians Learning When to Blend or Bound"
title_zh: Softmax-GS：学习何时融合或界定的广义高斯
authors: "Chen Ziwen, Peng Wang, Hao Tan, Zexiang Xu, Li Fuxin"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27437v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 基于高斯的几何与表面重建
tldr: 3D Gaussian Splatting (3D GS) 虽然高效，但因假设高斯分布不重叠而导致视角不一致和边缘模糊。本文提出 Softmax-GS，通过在重叠区域引入基于 Softmax 的竞争机制，利用可学习参数在平滑颜色混合与清晰边界之间取得平衡。该方法确保了顺序不变性和透射率一致性，有效解决了伪影问题，在提升重建质量的同时增强了参数效率，达到了 SOTA 性能。
selection_source: fresh_fetch
motivation: 针对 3D GS 中高斯分布重叠导致的视角不一致以及难以重建物体锐利边缘的问题。
method: 提出一种基于 Softmax 的竞争机制，通过可学习参数控制高斯分布在重叠区域是进行颜色混合还是形成清晰边界。
result: 在真实世界基准测试中实现了最先进的性能，显著提升了重建质量和参数效率。
conclusion: Softmax-GS 通过统一的框架解决了 3D GS 的核心缺陷，为高质量、一致性的神经渲染提供了有效方案。
---

## 摘要
3D高斯泼溅（3D GS）因其极高的训练和渲染效率而被广泛应用于新视角合成。然而，其效率依赖于高斯函数在3D空间中不重叠的关键假设，这导致了明显的伪影和视角不一致性。此外，高斯函数固有的弥散边界阻碍了对物体锐利边缘的准确重建。我们提出了 Softmax-GS，这是一种统一的解决方案，通过在两个高斯函数的重叠区域强制执行基于 softmax 的竞争，同时解决了视角不一致和弥散边界问题。通过控制竞争强度的可学习参数，它实现了从平滑颜色融合到清晰、明确边界的连续谱。我们的公式显式地保持了任意两个重叠高斯函数的顺序不变性，并确保输出透射率不受重叠程度的影响，从而防止了渲染输出中出现不理想的不连续性。在简单几何体上的消融实验证明了 Softmax-GS 各个组件的有效性，在真实世界基准测试上的评估表明，它达到了最先进的性能，同时提高了重建质量和参数效率。

## Abstract
3D Gaussian Splatting (3D GS) is widely adopted for novel view synthesis due to its high training and rendering efficiency. However, its efficiency relies on the key assumption that Gaussians do not overlap in the 3D space, which leads to noticeable artifacts and view inconsistencies. In addition, the inherently diffuse boundaries of Gaussians hinder accurate reconstruction of sharp object edges. We propose Softmax-GS, a unified solution that addresses both the view-inconsistency and the diffuse-boundary problem by enforcing a softmax-based competition in overlapping regions between two Gaussians. With learnable parameters controlling the strength of the competition, it enables a continuous spectrum from smooth color blending to crisp, well-defined boundaries. Our formulation explicitly preserves order invariance for any two overlapping Gaussians and ensures that the output transmittance remains unchanged irrespective of the extent of overlapping, preventing undesirable discontinuities in the rendered output. Ablation experiments on simple geometries demonstrate the effectiveness of each component of Softmax-GS, and evaluations on real-world benchmarks show that it achieves state-of-the-art performance, improving both reconstruction quality and parameter efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Softmax-GS: Generalized Gaussians Learning When to Blend or Bound》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 3D 高斯泼溅（3D GS）存在两个主要缺陷：
    1.  **视角不一致性（Popping Effect）**：3D GS 假设高斯球在空间中互不重叠，渲染时仅依赖深度排序。当两个高斯球相交且相机微动时，它们的相对顺序会突变，导致画面闪烁。
    2.  **边界模糊**：高斯函数具有长尾效应，难以精确表示物体的锐利边缘，通常需要堆叠大量微小高斯球来逼近。
*   **研究动机**：现有的改进方法（如重新排序或改变几何基元）要么无法处理同平面重叠，要么渲染速度大幅下降。本文旨在提出一种统一的、数学上严谨的方案，同时解决重叠导致的视角不一致和边界模糊问题。

### 2. 方法论
*   **核心思想**：引入一种**基于 Softmax 的竞争机制**。在两个高斯球重叠的区域，通过可学习的参数决定它们是“平滑融合”（Blend）还是“胜者通吃”（Bound，形成锐利边界）。
*   **关键技术细节**：
    *   **Softmax 竞争**：重新定义体渲染方程中的消光函数，引入权重 $w_k$，该权重由高斯指数项的 Softmax 计算得出。参数 $\beta$ 控制竞争强度：$\beta$ 趋于 0 时平滑混合，$\beta$ 较大时产生锐利边界。
    *   **高效近似算法**：为了保持渲染效率，作者没有采用全数值积分，而是通过在“完全重叠”和“完全分离”两种极端情况之间进行指数插值（由参数 $\gamma$ 控制），近似计算 Softmax 后的吸收率。
    *   **顺序不变性与透射率维护**：通过数学推导修正吸收率，确保即使两个高斯球深度相同，渲染结果也不受处理顺序影响，且总透射率在重叠前后保持一致，避免产生伪影。
    *   **广义指数函数（GEF）**：集成 GEF（参数 $\alpha$）来控制单个高斯球自身的边缘锐度。
*   **算法流程**：在 CUDA 内核中，按深度顺序处理射线上的高斯球，实时计算当前高斯与“过去累积实体”之间的 Softmax 竞争，并动态修正透射率。

### 3. 实验设计
*   **数据集与场景**：
    *   **简单几何模式**：Circle4, Rect4, Oval4（用于验证基础逻辑）。
    *   **真实世界基准**：Mip-NeRF360（7个场景）、Tanks&Temples（2个场景）、Deep Blending（2个场景）。
*   **对比方法**：包括原始 3D GS、StopThePop（排序优化）、GES（边缘优化）、EVER（精确体积渲染）、3DCS、GS-MCMC 等 SOTA 方法。
*   **评估指标**：PSNR、SSIM、LPIPS，以及针对视角一致性的 MSE 和 FLIP 指标（结合 RAFT 光流法）。

### 4. 资源与算力
*   **训练时长**：在复杂场景（如 Mip-NeRF360 的 bicycle）中，训练时间约为 36-45 分钟，比原始 3D GS 略慢（约 1.2 倍）。
*   **渲染速度**：保持了高效性，渲染速度约为原始 3D GS 的 80%（44-166 FPS），远快于 EVER 等精确积分方法。
*   **算力说明**：文中未明确提及具体的 GPU 型号和数量，但提到使用了自定义 CUDA 内核进行优化。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成几何体到复杂室内外场景的 11 个主流真实场景。
*   **消融实验**：详细对比了“仅边缘锐化”、“仅 Softmax 竞争”以及两者结合的效果，证明了双重机制的必要性。
*   **充分性评价**：实验设计较为全面，不仅对比了渲染质量，还专门针对“视角一致性”进行了量化评估，并展示了在极低参数量（5% 高斯数量）下的鲁棒性，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：Softmax-GS 在多个基准测试中达到了 SOTA 性能，PSNR 平均提升约 0.3dB。
*   **参数效率极高**：Softmax-GS Light 版本仅需原始 3D GS 一半的高斯数量，即可达到同等甚至更好的渲染质量。
*   **视角一致性显著提升**：通过 Softmax 机制消除了重叠区域的“弹出”伪影，实现了更平滑的视角过渡。

### 7. 优点（亮点）
*   **数学严谨性**：通过修正公式解决了重叠区域的顺序不变性和透射率一致性问题，这是此前很多启发式方法忽略的。
*   **统一框架**：一个模型同时解决了边界模糊和视角不一致两个核心痛点。
*   **效率平衡**：在显著提升质量的同时，维持了 3D GS 赖以生存的实时渲染能力。

### 8. 不足与局限
*   **覆盖范围限制**：为了保证反向传播的线性复杂度，算法仅应用于每条射线前 128 个高斯球，在极深场景中可能覆盖不全（室外场景覆盖率约 70%）。
*   **多重重叠复杂性**：目前的顺序不变性机制主要针对两个高斯球的重叠，当三个及以上不同颜色的高斯球同时重叠时，难以完全保证排列不变性。
*   **半透明处理**：对于远处的半透明高斯球，当前的深度和指数累积估计可能会产生偏差，影响竞争机制的准确性。

（完）
