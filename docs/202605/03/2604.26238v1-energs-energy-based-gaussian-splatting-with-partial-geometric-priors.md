---
title: "EnerGS: Energy-Based Gaussian Splatting with Partial Geometric Priors"
title_zh: EnerGS：基于能量且带有部分几何先验的高斯泼溅
authors: "Rui Song, Tianhui Cai, Markus Gross, Yun Zhang, Walter Zimmer, Zhiyu Huang, Olaf Wysocki, Jiaqi Ma"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26238v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 使用几何先验进行场景重建的3D高斯泼溅
tldr: 针对大规模户外场景中几何先验（如LiDAR）空间分布不均且不完整的问题，本文提出了EnerGS。该方法将部分可观测的几何信息建模为连续能量场，为3D高斯原语的优化提供软性几何引导，而非硬性约束。实验证明，EnerGS在稀疏多视图和单目设置下均能显著提升光度重建质量和几何稳定性，并有效缓解过拟合。
selection_source: fresh_fetch
motivation: 传统的几何先验在处理大规模户外场景时往往不完整且分布不均，直接作为硬约束会限制优化并损害重建效果。
method: 将部分几何证据建模为连续能量场，通过软性几何引导而非硬性限制来优化高斯原语。
result: 在稀疏多视图和单目设置下，该方法显著提升了光度质量、几何稳定性，并减少了训练中的过拟合现象。
conclusion: EnerGS通过能量场建模有效地利用了不完整的几何先验，为大规模场景的高质量3D重建提供了稳健的解决方案。
---

## 摘要
3D高斯泼溅（3DGS）已被广泛应用于场景重建，其训练过程本质上是一个高度耦合且非凸的优化问题。近期的研究通常结合几何先验（如 LiDAR 测量值），将其用于初始化或作为训练约束，旨在提高光度重建质量。然而，在大规模户外场景中，此类几何监督在空间上往往是不完整且不均匀的，这限制了其作为可靠先验的有效性，甚至可能对最终重建产生不利影响。为了应对这一挑战，我们将部分可观测的几何建模为由几何证据诱导的连续能量场，并提出了 EnerGS。EnerGS 并非将几何作为硬约束强制执行，而是为高斯基元的优化提供软几何引导，允许几何信息引导优化过程，而不会直接限制解空间。在大规模户外场景上的广泛实验表明，在稀疏多视图和单目设置下，EnerGS 都能持续提高光度质量和几何稳定性，同时有效缓解 3DGS 训练过程中的过拟合问题。

## Abstract
3D Gaussian Splatting (3DGS) has been widely adopted for scene reconstruction, where training inherently constitutes a highly coupled and non-convex optimization problem. Recent works commonly incorporate geometric priors, such as LiDAR measurements, either for initialization or as training constraints, with the goal of improving photometric reconstruction quality. However, in large-scale outdoor scenarios, such geometric supervision is often spatially incomplete and uneven, which limits its effectiveness as a reliable prior and can even be detrimental to the final reconstruction. To address this challenge, we model partially observable geometry as a continuous energy field induced by geometric evidence and propose EnerGS. Rather than enforcing geometry as a hard constraint, EnerGS provides a soft geometric guidance for the optimization of Gaussian primitives, allowing geometric information to steer the optimization process without directly restricting the solution space. Extensive experiments on large-scale outdoor scenes demonstrate that, under both sparse multi-view and monocular settings, EnerGS consistently improves photometric quality and geometric stability, while effectively mitigating overfitting during 3DGS training.

---

## 论文详细总结（自动生成）

这是一份关于论文《EnerGS: Energy-Based Gaussian Splatting with Partial Geometric Priors》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：3D高斯泼溅（3DGS）在处理大规模户外场景（如自动驾驶）时，由于视角稀疏且几何先验（如LiDAR点云）往往空间分布不均、不完整（如LiDAR垂直视场角有限），导致优化过程容易陷入几何错误的局部最优，产生“漂浮物”（floaters）或过拟合训练视角。
*   **研究背景**：现有的结合几何先验的方法通常将深度信息视为硬约束（如L2损失），这在几何观测缺失的区域（如高楼顶部、远景）会抑制合理的结构生成，或者在光度梯度与几何梯度冲突时导致训练不稳定。

### 2. 方法论
EnerGS 将几何优化重新表述为在**连续几何能量场**中的推断问题，核心思想包括：
*   **空间三分区建模**：将空间划分为占据区（Occupied）、自由空间（Free）和未知区（Unknown），并分别定义能量势能：
    *   **占据区（Welsch M-估计器）**：采用鲁棒的吸引力场，允许近距离吸引，同时通过非凸特性削弱远距离离群点的影响。
    *   **自由空间（玻尔兹曼屏障）**：利用 Softplus 函数构建单调排斥力，将误入空旷区域的高斯原语推离。
    *   **未知区（弱先验）**：设置高方差、低幅度的吸引场，作为弱约束，允许光度优化主导该区域的重建。
*   **解耦优化策略**：
    *   **位置更新**：高斯原语的中心位置（$\mu$）仅由几何能量场的梯度驱动。
    *   **外观更新**：协方差、不透明度和颜色由光度损失（L1 + D-SSIM）驱动。
    *   这种解耦解决了光度梯度在遮挡或稀疏视角下的不适定性，确保了几何演化的单调下降。
*   **离散剪枝**：作为辅助手段，定期删除无法逃离自由空间的残余高斯原语。

### 3. 实验设计
*   **数据集**：KITTI 和 Waymo Open Dataset（侧重于具有复杂遮挡和无限背景的静态场景）。
*   **对比基准（Baselines）**：包括原始 3DGS、2DGS、Taming-3DGS、GeoGaussian、Mip-Splatting、Scaffold-GS 以及针对自动驾驶设计的 SplatAD。
*   **评估指标**：
    *   **光度质量**：PSNR, SSIM。
    *   **几何质量**：Leak（自由空间违规率）、OccCov（占据覆盖率）、Margin（空间分离度）、Thick（表面厚度）。
    *   **效率**：高斯原语数量（#G）。

### 4. 资源与算力
*   **硬件平台**：使用单张 **NVIDIA L40S GPU** 进行测试。
*   **训练时长**：所有方法均训练 30,000 次迭代。
*   **运行效率**：
    *   **渲染速度**：在 KITTI 上达到 102.2 FPS，在 Waymo 上达到 72.9 FPS。
    *   **训练速度**：约 18-40 steps/s。
    *   **预处理**：几何先验初始化（欧几里得距离变换）在 KITTI 上仅需约 1.6s，Waymo 约 5.5s。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流自动驾驶数据集上进行了详尽的定量对比。
*   **消融实验**：针对占据场、自由空间场、未知区场以及解耦优化策略分别进行了消融，验证了各组件的必要性。
*   **鲁棒性分析**：进行了超参数敏感性测试（如 $\sigma$ 和 $w$ 的比例）以及随机初始化实验，证明了方法在不同噪声水平下的稳定性。
*   **扩展性实验**：展示了与动态场景模型（如 StreetGS）的兼容性。
*   **评价**：实验设计全面，涵盖了从理论证明到实际应用的闭环，对比方法具有代表性，实验结果客观公正。

### 6. 主要结论与发现
*   **几何稳定性**：EnerGS 显著减少了自由空间中的伪影（Leak 降低），并生成了更薄、更精确的表面。
*   **光度提升**：通过在 LiDAR 盲区允许光度驱动，EnerGS 能够重建出 LiDAR 无法观测到的结构（如电线杆、建筑顶部），PSNR 优于现有正则化方法。
*   **缓解过拟合**：训练与测试集的 PSNR 差距更小，表明模型学习的是一致的几何结构而非简单的视角记忆。

### 7. 优点
*   **理论完备**：提供了关于收敛性、稳定性和 Lipschitz 连续性的数学证明。
*   **自适应性强**：通过能量场设计，自动平衡了确定性传感器证据与未观察区域的模糊性。
*   **即插即用**：计算开销极低，可轻松集成到现有的 3DGS 及其变体（如动态场景重建）中。

### 8. 不足与局限
*   **依赖初始先验**：虽然能处理不完整的先验，但仍需要一定程度的几何输入（如稀疏点云），在完全无先验的场景下无法发挥作用。
*   **静态假设**：论文主体针对静态场景，虽然展示了与动态框架的兼容性，但能量场本身尚未直接建模时序演化。
*   **体素限制**：几何场基于体素网格实现，对于极大规模场景，体素分辨率与内存占用之间可能存在权衡。

（完）
