---
title: Generalizable Sparse-View 3D Reconstruction from Unconstrained Images
title_zh: 基于无约束图像的可泛化稀疏视图三维重建
authors: "Vinayak Gupta, Chih-Hao Lin, Shenlong Wang, Anand Bhattad, Jia-Bin Huang"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.28193v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 预测3D高斯并针对目标光照条件调节外观
tldr: 针对现实世界中光照多变和遮挡严重的稀疏无位姿图像重建难题，本文提出 GenWildSplat 框架。该方法无需场景特定优化，通过学习几何先验直接预测深度、相机参数和 3D 高斯分布。结合外观适配器和语义分割处理光照与瞬态物体，在 PhotoTourism 等基准上实现了实时推理和领先的泛化渲染质量。
selection_source: fresh_fetch
motivation: 现有的稀疏视图重建方法依赖耗时的场景特定优化，且在处理光照变化和瞬态遮挡时泛化能力不足。
method: 提出一种前馈式框架，利用几何先验预测 3D 高斯参数，并通过外观适配器和语义分割处理复杂环境因素。
result: 在 PhotoTourism 和 MegaScenes 基准测试中，该方法在无需测试时优化的前提下，达到了最先进的渲染质量和实时推理速度。
conclusion: GenWildSplat 证明了通过前馈网络实现大规模、非约束性户外场景的高质量稀疏视图重建是可行且高效的。
---

## 摘要
在光照变化和瞬时遮挡的真实世界条件下，从稀疏且无位姿的图像中重建三维场景仍然具有挑战性。现有方法依赖于使用外观嵌入或动态掩码的场景特定优化，这需要大量的单场景训练，且在稀疏视图下表现不佳。此外，在有限场景上的评估引发了关于泛化性的疑问。我们提出了 GenWildSplat，这是一个用于稀疏视图室外重建的前馈框架，无需进行单场景优化。给定无位姿的网络图像，GenWildSplat 利用学习到的几何先验，在规范空间中预测深度、相机参数和 3D Gaussian。外观适配器根据目标光照条件调节外观，而语义分割则处理瞬时物体。通过在合成数据和真实数据上的课程学习，GenWildSplat 能够泛化到各种光照和遮挡模式。在 PhotoTourism 和 MegaScenes 基准测试上的评估表明，该方法达到了最先进的前馈渲染质量，且无需测试时优化即可实现实时推理。

## Abstract
Reconstructing 3D scenes from sparse, unposed images remains challenging under real-world conditions with varying illumination and transient occlusions. Existing methods rely on scene-specific optimization using appearance embeddings or dynamic masks, which requires extensive per-scene training and fails under sparse views. Moreover, evaluations on limited scenes raise questions about generalization. We present GenWildSplat, a feed-forward framework for sparse-view outdoor reconstruction that requires no per-scene optimization. Given unposed internet images, GenWildSplat predicts depth, camera parameters, and 3D Gaussians in a canonical space using learned geometric priors. An appearance adapter modulates appearance for target lighting conditions, while semantic segmentation handles transient objects. Through curriculum learning on synthetic and real data, GenWildSplat generalizes across diverse illumination and occlusion patterns. Evaluations on PhotoTourism and MegaScenes benchmark demonstrate state-of-the-art feed-forward rendering quality, achieving real-time inference without test-time optimization

---

## 论文详细总结（自动生成）

以下是对论文《Generalizable Sparse-View 3D Reconstruction from Unconstrained Images》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何在仅有极少数（2-6张）、无相机位姿、且存在剧烈光照变化和瞬态遮挡（如行人、车辆）的“野外”互联网图像中，实现高质量、可泛化的三维场景重建。
*   **研究背景**：
    *   传统的 NeRF 或 3D Gaussian Splatting (3DGS) 依赖于单场景优化和密集视图，处理稀疏图像时容易过拟合。
    *   现有的“野外”重建方法通常需要数小时的测试时优化，无法满足实时应用需求。
    *   互联网照片中的光照不一致和动态物体会导致重建出现严重的伪影（如“浮点”或几何畸变）。

### 2. 方法论：核心思想与技术细节
GenWildSplat 采用**前馈式（Feed-forward）架构**，其核心思想是将几何预测、外观解耦和遮挡过滤整合进一个统一的学习框架中：
*   **几何预测头**：基于 VGGT Transformer 骨干网络提取多视图特征，通过专门的预测头输出每像素深度、相机内外参以及 3D 高斯属性（位置、形状、不透明度）。
*   **外观适配器（Appearance Adapter）**：
    *   使用轻量级 CNN 编码器提取每张图像的“光照代码”（Light Code）。
    *   通过一个 MLP 调制规范空间（Canonical Space）下的高斯颜色，使模型能够根据目标光照条件实时调整渲染外观，实现几何与光照的解耦。
*   **遮挡建模**：引入预训练的语义分割网络（YOLOv8）识别瞬态物体（人、车等），生成二值掩码。在训练过程中，通过掩码损失函数引导模型忽略动态区域，确保 3D 高斯仅表示静态场景。
*   **课程学习策略（Curriculum Learning）**：
    *   **阶段 I**：在具有光照变化的单场景合成数据上训练，学习外观解耦。
    *   **阶段 II**：引入多场景数据，提升几何泛化能力。
    *   **阶段 III**：加入合成遮挡物，训练模型处理瞬态干扰。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用 DL3DV 户外场景，通过 DiffusionRenderer 进行离线重光照增强，并随机合成 COCO 物体作为遮挡。
    *   **评估集**：PhotoTourism（经典基准）和 **MegaScenes**（更具挑战性，包含极稀疏视图和剧烈环境变化）。
*   **对比方法**：
    *   **优化类方法**：WildGaussians, NexusSplats, GS-W（均针对野外场景优化）。
    *   **前馈类基准**：AnySplat 及其结合了风格迁移或扩散渲染器的变体。
*   **评估指标**：PSNR, SSIM, LPIPS。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX A6000** GPU。
*   **训练时长**：总计 40,000 次迭代，耗时约 **2 天**。
*   **推理效率**：单次前馈推理仅需 **3 秒**，无需任何测试时优化（Test-time Optimization），实现了实时三维重建。

### 5. 实验数量与充分性
*   **实验规模**：在 20 个极具挑战性的 MegaScenes 场景和 PhotoTourism 多个场景上进行了定量评估。
*   **消融实验**：针对外观适配器、遮挡处理机制和课程学习策略分别进行了消融，证明了每个模块对防止颜色崩溃和去除伪影的必要性。
*   **客观性**：实验对比了不同输入视图数量（2-6 视图）下的表现，并展示了跨场景的光照迁移效果。通过使用统一的相机位姿估计（VGGT）来公平对比优化类方法，增强了实验的客观性。

### 6. 主要结论与发现
*   **前馈胜过优化**：在稀疏视图下，GenWildSplat 的前馈预测比需要数小时优化的 SOTA 方法表现更好，证明了大规模预训练几何先验的优越性。
*   **成功解耦**：模型能够成功将静态几何与瞬态光照/物体分离，支持将一张图片的光照风格迁移到另一个重建场景中。
*   **鲁棒性**：通过外部语义先验而非自监督不确定性来处理遮挡，能更有效地保护稀疏视图下的静态结构（如树木）。

### 7. 优点（亮点）
*   **极速推理**：将原本需要数小时的过程缩短至 3 秒。
*   **无需位姿输入**：直接处理无约束的图像集合。
*   **强泛化性**：在完全未见过的真实世界场景中表现稳健。
*   **外观可控**：支持改变渲染光照，增强了在 AR/VR 中的应用潜力。

### 8. 不足与局限
*   **几何缺失**：在极稀疏视图下，未观测到的区域仍会出现几何空洞。
*   **视角限制**：当测试视角与训练视角偏差过大时，会出现“双重几何”或伪影。
*   **室内局限**：目前主要针对户外场景，室内场景的复杂遮挡和深度不连续性仍是挑战。
*   **物理一致性**：尚未建模投射阴影（Cast Shadows），光照调节在物理真实感上仍有提升空间。

（完）
