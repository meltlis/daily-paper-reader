---
title: Generalizable Human Gaussian Splatting via Multi-view Semantic Consistency
title_zh: 基于多视图语义一致性的可泛化人体高斯泼溅
authors: "Jingi Kim, Wonjun Kim"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25466v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 可泛化的人体高斯泼溅
tldr: 针对稀疏视角下人体高斯泼溅（Gaussian Splatting）泛化性不足的问题，本文提出了一种基于多视角语义一致性的新方法。通过将各视角的潜嵌入向量投影到共享3D空间，并利用跨视角注意力机制根据身体部位进行特征重校准，解决了复杂姿态和遮挡导致的特征不一致及空间歧义问题，显著提升了人体渲染质量和3D高斯定位的准确性。
selection_source: fresh_fetch
motivation: 现有方法在处理复杂人体动作和稀疏视角时，由于视角重叠有限，难以保持多视角特征表示的一致性。
method: 将各视角的潜嵌入通过预测深度图投影至共享3D空间，并利用跨视角注意力机制对属于同一身体部位的特征进行重校准。
result: 在基准数据集上的实验证明，该方法有效解决了纹理丰富区域和遮挡部位的空间歧义，提升了渲染性能。
conclusion: 通过引入多视角语义一致性约束，该研究为实现高质量、可泛化的稀疏视角人体高斯泼溅提供了有效方案。
---

## 摘要
近年来，针对照片级真实感人体渲染，基于稀疏视图输入的可泛化人体高斯泼溅（Gaussian Splatting）得到了广泛研究。大多数现有方法依赖于显式几何约束或预定义的结构表示来精确定位 3D 高斯。尽管这些方法在该领域取得了显著进展，但由于人体复杂的关节运动以及不同视图之间有限的重叠，它们仍然面临多视图输入之间特征表示不一致的问题。为了解决这一问题，我们提出了一种新颖的方法来精确地定位 3D 高斯，并最终提高人体渲染的质量。其核心思想是通过预测的深度图将从每个视角编码的潜嵌入（latent embeddings）反投影到共享的 3D 空间中，并基于跨视图注意力机制对属于同一身体部位的嵌入进行重新校准。这有助于模型解决在高纹理区域以及遮挡身体部位出现的空间歧义，从而实现 3D 高斯的精确定位。在基准数据集上的实验结果表明，所提方法有效地提升了基于稀疏视图输入的可泛化人体高斯泼溅的性能。

## Abstract
Recently, generalizable human Gaussian splatting from sparse-view inputs has been actively studied for the photorealistic human rendering. Most existing methods rely on explicit geometric constraints or predefined structural representations to accurately position 3D Gaussians. Although these approaches have shown the remarkable progress in this field, they still suffer from inconsistent feature representations across multi-view inputs due to complex articulations of the human body and limited overlaps between different views. To address this problem, we propose a novel method to accurately localize 3D Gaussians and ultimately improve the quality of human rendering. The key idea is to unproject latent embeddings encoded from each viewpoint into a shared 3D space through predicted depth maps and recalibrate them belonging to the same body part based on cross-view attention. This helps the model resolve the spatial ambiguity occurring in highly textured regions as well as occluded body parts, thus leading to the accurate localization of 3D Gaussians. Experimental results on benchmark datasets show that the proposed method efficiently improves the performance of generalizable human Gaussian splatting from sparse-view inputs.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **GHGS-MVSC**（基于多视图语义一致性的可泛化人体高斯泼溅）的方法，旨在解决从稀疏视角输入生成高质量、可泛化人体 3D 重建的难题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在稀疏视角（Sparse-view）下，如何实现对未见人体目标（Unseen subjects）的高质量、实时渲染。
*   **研究背景**：
    *   传统的 3D 高斯泼溅（3DGS）需要针对每个场景进行耗时的逐对象优化。
    *   现有的可泛化方法（如基于 SMPL 模型或显式几何约束的方法）在处理复杂人体动作、宽松衣物或高纹理区域时，容易出现 3D 高斯定位不准、特征表示在多视角间不一致的问题。
    *   稀疏视角导致的重叠区域有限，使得建立准确的跨视图对应关系变得极其困难。

### 2. 方法论：核心思想与技术细节
该方法的核心在于**在 3D 空间中利用语义一致性对潜嵌入（Latent Embeddings）进行重校准**。

*   **潜嵌入的 3D 反投影（Unprojection）**：
    *   使用预训练的 VGGT 编码器提取多视图的潜嵌入和语义特征（DINO 特征）。
    *   通过深度解码器预测每视图深度图，将 2D 图像平面的潜嵌入反投影到共享的 3D 空间中，形成带特征的 3D 点云。
*   **基于语义一致性的跨视图注意力机制**：
    *   **空间邻域搜索**：在 3D 空间中通过 K 近邻（KNN）寻找空间位置接近的嵌入点。
    *   **语义校准**：计算邻域点之间的语义相似度（基于 DINO 特征的余弦相似度）。
    *   **特征重校准**：利用跨视图注意力机制，将属于同一身体部位（语义一致）的特征进行聚合。这解决了因深度预测误差导致的微小空间错位，并增强了部位相关的特征表达。
*   **高斯属性回归**：
    *   将重校准后的 3D 特征投影回 2D 栅格，通过解码器预测每个像素对应的 3D 高斯参数（位置偏移、旋转、缩放、不透明度）。颜色信息直接从输入图像中采样以保持光度一致性。
*   **损失函数**：结合了光度渲染损失（L1 + SSIM）和几何正则化损失（预测点云与 SMPL 顶点之间的 Chamfer 距离）。

### 3. 实验设计
*   **数据集**：
    *   **ZJU-Mocap**：受控环境下的多视角人体捕捉数据。
    *   **HuMMan**：包含复杂动作和多样化人体的真实世界数据集。
    *   **THuman2.0**：高质量 3D 人体扫描数据集。
*   **基准测试（Benchmark）**：对比了 PSNR、SSIM 和 LPIPS 三项核心指标。
*   **对比方法**：
    *   **NeRF 类方法**：SHERF, NHP, GP-NeRF, TransHuman。
    *   **3DGS 类方法**：GPS-Gaussian, EVA-Gaussian, GHG, RoGSplat。

### 4. 资源与算力
*   **硬件**：单台 PC，配备 **NVIDIA GeForce RTX 3090 GPU** 和 Intel i9-10980XE CPU。
*   **训练细节**：
    *   使用 AdamW 优化器。
    *   Batch size 为 1。
    *   训练迭代次数为 **200k 次**。
    *   输入图像尺寸统一调整为 518 × 518。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了详尽的定量和定性评估。
*   **消融实验**：专门验证了“3D 反投影”和“语义一致性校准”两个核心组件的有效性。
*   **视角敏感性分析**：测试了在 3 视图、4 视图和 5 视图输入下的性能变化。
*   **评价**：实验设计较为充分，涵盖了从实验室环境到真实场景的迁移能力验证，对比对象包含了最新的 SOTA 方法，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **定位精度提升**：通过在 3D 空间校准特征，模型能更准确地定位 3D 高斯，尤其是在头发、宽松衣物等非刚性区域。
*   **泛化能力强**：在未见过的测试主体上，该方法在保持结构完整性的同时，捕捉到了更丰富的纹理细节。
*   **效率优势**：相比 NeRF 类方法，该方法在渲染速度上具有显著优势，且无需逐场景优化，适合实时应用。

### 7. 优点（亮点）
*   **创新性地结合了语义与几何**：不只是依赖坐标对齐，而是利用自监督视觉特征（DINO）来引导跨视图的特征融合，解决了稀疏视角下的空间歧义。
*   **端到端架构**：将深度估计、特征校准和高斯参数预测整合在一个前馈网络中，逻辑清晰且高效。
*   **鲁棒性**：对输入视角数量的减少表现出较强的鲁棒性。

### 8. 不足与局限
*   **深度依赖性**：尽管有语义校准，但初始深度预测的质量仍会影响 3D 空间的对齐效果；如果深度误差过大，KNN 搜索可能会失效。
*   **计算开销**：在 3D 空间进行 KNN 搜索和跨视图注意力计算会增加推理时的内存和计算负担。
*   **特定领域限制**：该方法目前专注于人体渲染，对于非人体对象（缺乏类似 SMPL 的几何先验或特定语义分布）的适用性尚未验证。
*   **光照一致性**：颜色直接采样自输入图像，在光照变化剧烈的环境下可能会出现视角间的不自然过渡。

（完）
