---
title: "SurfelSplat: Learning Efficient and Generalizable Gaussian Surfel Representations for Sparse-View Surface Reconstruction"
title_zh: SurfelSplat：用于稀疏视图表面重建的高效且可泛化高斯面元表示学习
authors: "Chensheng Dai, Shengjun Zhang, Min Chen, Yueqi Duan"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08370v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 生成用于稀疏视图表面重建的高斯面元表示
tldr: 针对3D高斯泼溅在表面重建中依赖密集视图且逐场景优化耗时的问题，本文提出SurfelSplat。这是一个前馈框架，能从稀疏视图中生成可泛化的像素对齐高斯面元。通过引入基于奈奎斯特采样定理的跨视图特征聚合模块和低通滤波器，解决了几何属性恢复不准确的问题。该方法在DTU基准上实现了与SOTA相当的精度，且推理速度提升100倍，无需逐场景训练。
selection_source: fresh_fetch
motivation: 现有的3DGS表面重建方法通常需要密集输入且逐场景优化极其耗时，限制了其在快速、稀疏视图场景下的应用。
method: 提出一种基于奈奎斯特采样定理的前馈框架，利用空间采样率引导的低通滤波器和跨视图特征融合网络来回归精确的高斯面元几何属性。
result: 在DTU数据集上取得了与最先进方法相当的重建质量，且预测速度提升了100倍，单场景处理时间缩短至1秒以内。
conclusion: SurfelSplat通过结合采样理论与前馈网络，实现了高效且具备泛化能力的稀疏视图三维表面重建。
---

## 摘要
3D 高斯泼溅（3DGS）在三维场景重建中展现了令人印象深刻的性能。除了新视图合成，它在多视图表面重建方面也显示出巨大潜力。现有方法采用基于优化的重建流水线，能够实现精确且完整的表面提取。然而，这些方法通常需要密集的输入视图，并且每个场景的优化耗时较长。为了解决这些局限性，我们提出了 SurfelSplat，这是一个前馈框架，能够从稀疏视图图像中生成高效且可泛化的像素对齐高斯面元表示。我们观察到，传统的前馈结构难以恢复准确的高斯面元几何属性，因为像素对齐原语的空间频率超过了奈奎斯特（Nyquist）采样率。因此，我们提出了一种基于奈奎斯特采样定理的跨视图特征聚合模块。具体而言，我们首先利用空间采样率引导的低通滤波器来调整高斯面元的几何形式。然后，我们将滤波后的面元投影到所有输入视图中，以获取跨视图特征相关性。通过专门设计的特征融合网络处理这些相关性，我们最终可以回归出具有精确几何形状的高斯面元。在 DTU 重建基准上的大量实验表明，我们的模型达到了与最先进方法相当的结果，并能在 1 秒内预测高斯面元，在无需昂贵的逐场景训练的情况下实现了 100 倍的加速。

## Abstract
3D Gaussian Splatting (3DGS) has demonstrated impressive performance in 3D scene reconstruction. Beyond novel view synthesis, it shows great potential for multi-view surface reconstruction. Existing methods employ optimization-based reconstruction pipelines that achieve precise and complete surface extractions. However, these approaches typically require dense input views and high time consumption for per-scene optimization. To address these limitations, we propose SurfelSplat, a feed-forward framework that generates efficient and generalizable pixel-aligned Gaussian surfel representations from sparse-view images. We observe that conventional feed-forward structures struggle to recover accurate geometric attributes of Gaussian surfels because the spatial frequency of pixel-aligned primitives exceeds Nyquist sampling rates. Therefore, we propose a cross-view feature aggregation module based on the Nyquist sampling theorem. Specifically, we first adapt the geometric forms of Gaussian surfels with spatial sampling rate-guided low-pass filters. We then project the filtered surfels across all input views to obtain cross-view feature correlations. By processing these correlations through a specially designed feature fusion network, we can finally regress Gaussian surfels with precise geometry. Extensive experiments on DTU reconstruction benchmarks demonstrate that our model achieves comparable results with state-of-the-art methods, and predict Gaussian surfels within 1 second, offering a 100x speedup without costly per-scene training.

---

## 论文详细总结（自动生成）

以下是对论文《SurfelSplat: Learning Efficient and Generalizable Gaussian Surfel Representations for Sparse-View Surface Reconstruction》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 3D 高斯泼溅（3DGS）表面重建方法（如 2DGS）虽然精度高，但依赖密集的输入视图，且需要针对每个场景进行数十分钟甚至数小时的优化。而现有的前馈网络（Feed-forward）虽然速度快，但在处理稀疏视图时，生成的几何属性（如法向量）往往不准确，高斯面元倾向于平行于图像平面而非贴合实际表面。
*   **研究背景**：作者发现前馈网络失效的根本原因在于**空间采样频率冲突**。像素对齐的高斯原语其空间频率超过了奈奎斯特（Nyquist）采样率，导致网络无法从有限的像素区域学习到正确的几何协方差。

### 2. 方法论
*   **核心思想**：提出 SurfelSplat 框架，利用奈奎斯特采样定理指导高斯面元的特征学习，通过前馈推理在 1 秒内生成精确的 2D 高斯面元表示。
*   **关键技术细节**：
    1.  **奈奎斯特采样分析**：推导了多相机系统中的空间采样率 $\hat{\nu}$ 和 2D 高斯面元的空间频率 $\nu$。
    2.  **面元自适应（Surfel Adaptation）**：在频率域引入自适应低通滤波器，动态调整高斯面元的几何形式，确保其频率满足奈奎斯特准则（$\hat{\nu} \ge 2\nu$），从而使网络能获得足够的监督信息。
    3.  **跨视图特征聚合**：将自适应后的面元投影回所有输入视图，识别出包含相关几何信息的图像区域，利用 Transformer 架构（交叉注意力机制）融合这些区域的特征。
    4.  **参数回归**：基于细化后的特征，直接回归高斯面元的中心、缩放、旋转、不透明度和颜色。

### 3. 实验设计
*   **数据集**：主要在 **DTU** 数据集（128个场景）上进行定量评估，并在 **BlendedMVS** 数据集上进行定性验证。
*   **Benchmark**：使用倒角距离（Chamfer Distance, CD）作为几何精度的评价指标。
*   **对比方法**：
    *   **神经隐式方法**：NeuS, VolRecon, UFORecon, NeuSurf。
    *   **神经显式方法（3DGS类）**：2DGS, GausSurf, FatesGS。
*   **实验设置**：极稀疏视图场景（仅 2 张输入图像），分辨率为 256×320。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX A6000** GPU。
*   **训练时长**：
    1.  在 RealEstate10K 数据集上预训练 300,000 次迭代。
    2.  在 DTU 数据集上微调 2,000 次迭代。
*   **推理速度**：单个场景推理仅需约 **1 秒**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 DTU 测试集的 15 个场景，进行了定量 CD 指标对比、定性视觉对比、推理效率对比。
*   **消融实验**：针对“面元自适应模块”和“特征聚合模块”分别做了消融，验证了各组件对几何精度和法线误差的影响。
*   **理论验证**：专门绘制了面元自适应前后的频率分布图，实验性地验证了奈奎斯特准则的有效性。
*   **充分性评价**：实验设计较为充分，不仅对比了精度，还重点突出了前馈网络在效率上的巨大优势（100倍加速），对比基准选择合理且公平。

### 6. 主要结论与发现
*   **精度领先**：在稀疏视图下，SurfelSplat 的平均倒角距离优于现有的隐式和显式重建方法。
*   **效率极高**：相比于需要逐场景优化的方法（如 NeuSurf 需 14 小时），SurfelSplat 仅需 1 秒，实现了质的飞跃。
*   **理论发现**：通过频率域的约束，可以有效解决前馈网络中高斯面元法线退化（平行于成像平面）的问题，使面元能够准确贴合复杂的表面细节。

### 7. 优点
*   **理论驱动**：将经典的信号处理理论（奈奎斯特采样定理）引入 3DGS 重建，解释并解决了前馈网络的几何不准确问题。
*   **泛化能力强**：无需对新场景进行耗时的重新训练，具备良好的通用性。
*   **速度优势**：极快的推理速度使其具备实时或近实时应用的潜力。

### 8. 不足与局限
*   **分辨率敏感**：由于采用像素对齐方式，高分辨率输入（如 1024×1024）会导致高斯面元数量激增（超过100万个），从而显著降低渲染和推理速度。
*   **遮挡处理**：对于场景中完全未见的部分，重建效果受限，可能需要引入生成式模型（如扩散模型）来填补几何空白。
*   **冗余性**：直接合并多视角预测的高斯点会导致重叠区域存在冗余表示，增加了计算负担。

（完）
