---
title: "CGHair: Compact Gaussian Hair Reconstruction with Card Clustering"
title_zh: CGHair：基于发片聚类的紧凑型高斯头发重建
authors: "Haimin Luo, Srinjay Sarkar, Albert Mosella-Montoro, Francisco Vicente Carrasco, Fernando De la Torre"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03716v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 将发卡结构与3DGS渲染相结合
tldr: 本研究针对多视图头发重建中3D高斯泼溅（3DGS）存储和渲染开销巨大的问题，提出了CGHair框架。该方法利用头发的结构相似性，将发丝聚类为代表性的发卡并共享纹理码本，结合3DGS渲染技术，在保持高保真度的同时显著降低了内存占用。此外，引入生成先验加速初始几何重建，实现了高效且紧凑的头发数字化。
selection_source: fresh_fetch
motivation: 现有的3D高斯泼溅头发重建方法因需要数百万个基元而导致存储和渲染成本极高。
method: 通过将发丝聚类为发卡并利用共享纹理码本，结合3DGS渲染和生成先验加速的几何重建流程。
result: 实验表明，该方法在保持渲染质量的同时，将发丝重建速度提升了4倍，且内存占用降低了200倍以上。
conclusion: CGHair成功实现了高效、紧凑且高保真的头发重建，为大规模3D头发数字化提供了可行的解决方案。
---

## 摘要
我们提出了一种从多视图图像进行高保真头发重建的紧凑型管线。虽然最近的 3D 高斯泼溅 (3DGS) 方法取得了逼真的效果，但它们通常需要数百万个基元，导致高昂的存储和渲染成本。观察到发型在结构和视觉上具有相似性，我们将发丝聚类为具有代表性的发片 (hair cards)，并将这些发片分组到共享的纹理码本中。我们的方法将这种结构与 3DGS 渲染相结合，在保持相当的视觉质量的同时，显著减少了重建时间和存储空间。此外，我们提出了一种生成先验加速方法，用于从一组图像中重建初始发丝几何结构。实验表明，我们的方法将发丝重建时间缩短了 4 倍，并在内存占用降低 200 倍以上的情况下实现了相当的渲染性能。

## Abstract
We present a compact pipeline for high-fidelity hair reconstruction from multi-view images. While recent 3D Gaussian Splatting (3DGS) methods achieve realistic results, they often require millions of primitives, leading to high storage and rendering costs. Observing that hair exhibits structural and visual similarities across a hairstyle, we cluster strands into representative hair cards and group these into shared texture codebooks. Our approach integrates this structure with 3DGS rendering, significantly reducing reconstruction time and storage while maintaining comparable visual quality. In addition, we propose a generative prior accelerated method to reconstruct the initial strand geometry from a set of images. Our experiments demonstrate a 4-fold reduction in strand reconstruction time and achieve comparable rendering performance with over 200x lower memory footprint.

---

## 论文详细总结（自动生成）

以下是对论文《CGHair: Compact Gaussian Hair Reconstruction with Card Clustering》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于 3D 高斯泼溅（3DGS）的头发重建方法虽然能达到高保真效果，但由于头发结构极其复杂，通常需要数百万个高斯基元。这导致了巨大的存储开销、缓慢的渲染速度以及极高的计算冗余。
*   **研究动机**：观察到发型在局部结构和视觉外观上具有极强的相似性（重复性）。作者希望借鉴传统图形学中的“发片（Hair Cards）”概念，通过层次化聚类和共享纹理码本，在不牺牲视觉质量的前提下，极大地压缩头发模型的体积并提升重建效率。

### 2. 方法论
CGHair 提出了一套从多视图图像到紧凑高斯表示的完整管线，核心步骤包括：
*   **生成先验加速的发丝生成**：利用预训练的参数化头发模型（PERM）作为几何先验。通过优化低维的引导图（Guide Map）和样式图（Style Map），结合 PCA 解码器快速生成初始 3D 发丝，比传统方法快 4 倍。
*   **发片几何构建（Card Clustering）**：
    *   **发丝聚类**：使用 K-means 算法将数万根发丝聚类成若干个发丝束。
    *   **发片拟合**：为每个聚类构建一个多边形带状网格（发片），通过优化法线和宽度，确保发片能包裹住该簇内的所有发丝。
*   **紧凑型高斯外观建模**：
    *   **共享纹理码本**：将发片进一步聚类，同一组内的发片共享一个“高斯外观纹理码本”。
    *   **潜在特征解码**：每根发丝不再独立存储高维的球谐函数（SH）系数，而是存储一个低维的潜在特征索引。通过一个全局共享的轻量级 MLP 解码器，将该特征映射为沿发丝分布的所有高斯点的 SH 参数。
*   **端到端优化**：在 3DGS 渲染框架下，联合优化发丝几何、不透明度、码本特征和解码器网络。

### 3. 实验设计
*   **数据集**：
    *   **真实场景**：GaussianHair 数据集（包含卷发、短发、长发等挑战性发型）、MonoHair 数据集、H3DS 数据集。
    *   **合成场景**：HairSalon 数据集（用于定量评估几何误差）。
*   **对比基准（Benchmarks）**：
    *   **GaussianHair**：目前最先进的基于高斯的头发建模方法。
    *   **GaussianHaircut**：另一种主流的基于发丝对齐高斯的重建方法。
*   **评估指标**：PSNR、SSIM、LPIPS（视觉质量）；存储空间（MB）；重建耗时。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX A4500 GPU**。
*   **训练时长**：
    *   **发丝生成阶段**：约 2 小时 40 分钟（对比 GaussianHaircut 的 11 小时，提速显著）。
    *   **端到端总时长**：约 5 小时（对比 GaussianHaircut 的 15 小时和 GaussianHair 的 10.5 小时）。
*   **存储优化**：外观特征从 500MB+ 压缩至约 1.2MB，整体模型参数量减少了 **200 倍以上**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了多种具有代表性的发型（长、短、直、卷），并在真实视频和合成数据上均进行了验证。
*   **消融实验**：非常充分。作者分别针对发丝生成模块（PCA 解码器、先验代码等）、发片聚类策略、码本大小（K）、潜在维度（D）以及纹理簇数量（Nt）进行了详细的定量分析。
*   **客观性**：通过与 SOTA 方法在相同硬件和数据集下的对比，证明了其在极高压缩比下依然能保持竞争力的渲染效果，实验设计客观公平。

### 6. 主要结论与发现
*   **极高压缩比**：通过发片级别的层次化聚类，成功消除了头发模型中 99% 以上的参数冗余。
*   **效率提升**：引入生成先验使得头发几何的初始化不再依赖于缓慢的从零优化，显著缩短了数字化流程。
*   **质量保持**：尽管参数量大幅减少，但在新视角合成任务中，其视觉细节（如发丝纹理、光泽）与全量模型相当。

### 7. 优点
*   **极度紧凑**：200 倍的压缩率使得高保真头发模型在移动端或实时应用中更具可行性。
*   **全自动管线**：相比需要手动干预的方法（如 GaussianHair），CGHair 实现了从视频到模型的自动化。
*   **即插即用**：该紧凑模块可以集成到现有的发丝重建框架中，具有良好的通用性。
*   **动态潜力**：由于保留了显式的发丝结构，该模型可以直接导入物理引擎进行动力学仿真。

### 8. 不足与局限
*   **极端发型失效**：对于结构极其复杂且难以观察内部的爆炸头（Afro）或紧密编织的发辫，自动化重建仍面临挑战。
*   **高频细节微损**：由于采用了硬聚类（Hard Clustering）和共享码本，在某些极端视角下可能会丢失极细微的外观变化。
*   **动态伪影**：虽然支持仿真，但由于模型是在静态视图上训练的，在大幅度运动导致的遮挡区域可能会出现渲染不自然的情况。

（完）
