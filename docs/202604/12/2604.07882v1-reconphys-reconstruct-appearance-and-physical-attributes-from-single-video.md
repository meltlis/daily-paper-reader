---
title: "ReconPhys: Reconstruct Appearance and Physical Attributes from Single Video"
title_zh: ReconPhys：从单段视频中重建外观与物理属性
authors: "Boyuan Wang, Xiaofeng Wang, Yongkang Li, Zheng Zhu, Yifan Chang, Angen Ye, Guosheng Zhao, Chaojun Ni, Guan Huang, Yijie Ren, Yueqi Duan, Xingang Wang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07882v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 联合学习物理属性估计与 3D 高斯泼溅重建
tldr: ReconPhys是首个从单目视频中联合学习物理属性估计与3D高斯泼溅（3DGS）重建的前馈框架。针对非刚性物体重建中优化耗时且需人工标注的难题，该方法采用双支路自监督架构，无需物理真值标签即可同时推断几何、外观及物理属性。相比传统优化方法，它将推理时间从数小时缩短至1秒以内，并在预测精度和几何还原度上显著领先，为机器人和图形学提供了高效的仿真资产生成方案。
selection_source: fresh_fetch
motivation: 现有的非刚性物体重建方法依赖耗时的逐场景优化和人工标注，限制了其实用性和泛化能力。
method: 提出一种双支路自监督前馈框架，结合3D高斯泼溅技术，从单视频中同步推断物体的几何、外观及物理属性。
result: 在合成数据集上，该方法将未来预测的PSNR提升至21.64，倒角距离降至0.004，且推理时间缩短至1秒以内。
conclusion: ReconPhys实现了高效且物理一致的非刚性物体重建，为快速生成可用于仿真的数字资产开辟了新路径。
---

## 摘要
重建具有物理合理性的非刚性物体仍然是一个重大挑战。现有方法利用可微分渲染进行逐场景优化，虽然能恢复几何形状和动力学，但需要昂贵的调优或手动标注，这限制了其实用性和泛化能力。为解决这一问题，我们提出了 ReconPhys，这是首个从单目视频中联合学习物理属性估计和 3D 高斯泼溅（3D Gaussian Splatting）重建的前馈框架。我们的方法采用双分支架构，通过自监督策略进行训练，消除了对物理属性真值标签的需求。给定一段视频序列，ReconPhys 能同时推断几何、外观和物理属性。在大规模合成数据集上的实验证明了其卓越的性能：在未来预测任务中，我们的方法达到了 21.64 的 PSNR，而最先进的优化基准方法仅为 13.27，同时将倒角距离（Chamfer Distance）从 0.349 降低到 0.004。至关重要的是，ReconPhys 实现了快速推理（小于 1 秒），而现有方法则需要数小时，这为机器人和图形学领域快速生成可用于模拟的资产提供了便利。

## Abstract
Reconstructing non-rigid objects with physical plausibility remains a significant challenge. Existing approaches leverage differentiable rendering for per-scene optimization, recovering geometry and dynamics but requiring expensive tuning or manual annotation, which limits practicality and generalizability. To address this, we propose ReconPhys, the first feedforward framework that jointly learns physical attribute estimation and 3D Gaussian Splatting reconstruction from a single monocular video. Our method employs a dual-branch architecture trained via a self-supervised strategy, eliminating the need for ground-truth physics labels. Given a video sequence, ReconPhys simultaneously infers geometry, appearance, and physical attributes. Experiments on a large-scale synthetic dataset demonstrate superior performance: our method achieves 21.64 PSNR in future prediction compared to 13.27 by state-of-the-art optimization baselines, while reducing Chamfer Distance from 0.349 to 0.004. Crucially, ReconPhys enables fast inference (<1 second) versus hours required by existing methods, facilitating rapid generation of simulation-ready assets for robotics and graphics.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **ReconPhys**，一个旨在从单目视频中同时恢复非刚性物体的外观、几何结构及物理属性的前馈神经网络框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不进行耗时的“逐场景优化”（per-scene optimization）且无需人工标注物理标签的情况下，仅凭一段单目视频就重建出既好看（视觉逼真）又好用（符合物理规律、可直接用于仿真）的非刚性 3D 资产。
*   **研究动机**：
    *   **现有视觉方法局限**：如 4DGS 等动态重建技术虽能生成逼真视频，但缺乏物理参数，无法预测物体在未见过的力作用下的反应。
    *   **现有物理方法局限**：如 Spring-Gaus 等方法依赖多视角输入，且每个新场景都需要数小时的优化，无法实现实时或大规模应用。
    *   **应用需求**：机器人操作非刚性物体（如抓取海绵、布料）需要快速获取物体的质量、刚度、摩擦力等物理参数。

### 2. 方法论
*   **核心思想**：将 **3D 高斯泼溅（3DGS）** 与 **可微分弹簧-质点系统（Spring-Mass System）** 结合，通过一个前馈神经网络（FFN）直接从视频特征中预测物理参数。
*   **关键技术细节**：
    *   **双支路架构**：
        1.  **3DGS 预测器**：提供物体的初始（规范空间）几何和外观。
        2.  **物理预测器**：使用 InternViT 提取时空特征，通过 MLP 预测质量 ($m$)、刚度 ($k$)、阻尼 ($d$) 和摩擦力 ($f$)。
    *   **绑定机制**：在物体体积内采样锚点（Anchors），通过反距离加权（IDW）插值将 3D 高斯球的中心位置与质点的运动绑定。
    *   **可微分仿真-渲染环路**：物理预测器输出参数驱动仿真器更新质点位置，进而更新高斯球位置，最后通过可微分渲染生成图像。
    *   **自监督训练策略**：
        *   **光度损失**：仅通过渲染图像与原始视频的像素差异来反向传播梯度，更新物理预测器。
        *   **Self-Forcing**：在训练模拟过程中，使用模型自身的预测状态而非真值状态作为下一步输入，以增强长程预测的稳定性。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：基于 Objaverse-XL 筛选出 500 个非刚性物体，利用 TRELLIS 生成 3DGS 资产，并用物理引擎生成 30 帧的自由落体与碰撞视频。
    *   **真实世界数据**：拍摄了真实物体的跌落视频进行定性验证。
*   **对比基准（Benchmark）**：
    *   **4DGS**：代表纯视觉动态重建方法。
    *   **Spring-Gaus**：代表基于优化的物理感知重建方法。
*   **评估指标**：视觉指标（PSNR, SSIM, LPIPS）、几何指标（CD 倒角距离, EMD 陆地移动距离）以及物理参数估计误差（MAE）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA RTX 4090 GPU**。
*   **训练细节**：训练迭代次数为 **100K 次**，Batch Size 为 8。
*   **推理效率**：推理时间 **小于 1 秒**，而对比方法 Spring-Gaus 需要 1 小时以上。

### 5. 实验数量与充分性
*   **实验规模**：在 450 个物体上训练，46 个完全未见过的物体上测试。
*   **实验类型**：
    *   **跨物体泛化实验**：验证模型对新几何形状的预测能力。
    *   **未来状态预测**：测试模型在已知前 20 帧后，预测后 10 帧物理运动的能力。
    *   **物理解耦实验**：对同一几何体赋予不同物理属性，验证模型能否准确区分。
    *   **机器人操作应用**：展示了重建资产在虚拟仿真环境中的交互效果。
*   **评价**：实验设计较为全面，涵盖了从合成到真实、从视觉到物理、从重建到应用的多个维度，对比实验在相同输入条件下进行，具有较好的客观性。

### 6. 主要结论与发现
*   **性能飞跃**：在未来预测任务中，ReconPhys 的 PSNR 达到 21.64（基准仅 13.27），倒角距离从 0.349 降至 0.004。
*   **高效性**：实现了前馈推理，彻底摆脱了逐场景优化的束缚，处理速度提升了数千倍。
*   **物理一致性**：通过引入物理约束，模型不仅能重建外观，还能学习到具有解释性的物理参数，使得预测的运动轨迹符合物理规律。

### 7. 优点
*   **首创性**：第一个实现单目视频下物理属性与 3DGS 联合学习的前馈框架。
*   **无需真值标签**：纯自监督学习，降低了数据获取成本。
*   **泛化能力强**：能够处理训练集中未见过的物体类别。
*   **实用价值高**：生成的“仿真就绪”（Simulation-ready）资产可直接用于机器人强化学习等下游任务。

### 8. 不足与局限
*   **物理模型简化**：目前采用弹簧-质点系统，虽然高效，但对于极度复杂的非线性材料（如流体或超弹性体）可能建模精度不足。
*   **依赖预训练 3DGS**：物理预测的效果受限于初始 3DGS 重建的质量。
*   **场景限制**：目前主要针对自由落体和简单碰撞场景，对于更复杂的受力情况（如撕裂、多体交互）尚待验证。
*   **合成与现实差异**：虽然有真实数据展示，但大规模的真实世界物理参数验证仍受限于缺乏真值测量手段。

（完）
