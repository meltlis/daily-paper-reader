---
title: Appearance Decomposition Gaussian Splatting for Multi-Traversal Reconstruction
title_zh: 面向多行程重建的外观分解高斯泼溅
authors: "Yangyi Xiao, Siting Zhu, Baoquan Yang, Tianchen Deng, Yongbo Chen, Hesheng Wang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05908v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 使用高斯泼溅进行外观分解以减轻不同遍历之间的耦合
tldr: 针对自动驾驶多序列场景重建中光照不一致的挑战，本文提出ADM-GS框架。该方法基于3D Gaussian Splatting，通过将静态背景的外观显式分解为序列无关的材质属性和序列相关的光照分量，并结合频率分离的混合编码神经光场来处理漫反射和镜面反射。实验证明，该方法在Argoverse 2和Waymo数据集上显著提升了重建质量和外观一致性。
selection_source: fresh_fetch
motivation: 解决多序列场景重建中，由于不同采集时间导致的光照和环境外观不一致而产生的渲染伪影问题。
method: 提出一种显式的外观分解方案，利用频率分离的混合编码神经光场，结合表面法线和反射向量分别建模低频漫反射和高频镜面反射。
result: 在Argoverse 2和Waymo Open数据集上，相比现有基于潜变量的方法，PSNR提升了0.98 dB，且外观一致性更强。
conclusion: ADM-GS通过解耦材质与光照，有效解决了多序列重建中的外观纠缠问题，为高保真自动驾驶仿真提供了有力支持。
---

## 摘要
多行程场景重建对于高保真自动驾驶仿真和数字孪生构建至关重要。该任务涉及整合在不同时间从同一地理区域采集的多条序列。在这种背景下，尽管底层几何结构相同，但由于光照和环境条件的变化，不同行程之间显著的外观不一致性是一个主要挑战。本文提出了 ADM-GS（面向多行程重建的外观分解高斯泼溅），这是一个对静态背景应用显式外观分解的框架，旨在缓解不同行程之间的外观纠缠。对于静态背景，我们将外观分解为代表固有材料属性的行程不变材料（traversal-invariant material）和捕捉光照变化的行程相关光照（traversal-dependent illumination）。具体而言，我们提出了一种利用频率分离混合编码策略的神经光场。通过结合表面法线和显式反射向量，该设计分别捕捉低频漫反射光照和高频镜面反射。在 Argoverse 2 和 Waymo Open 数据集上的定量评估证明了 ADM-GS 的有效性。在多行程实验中，我们的方法比现有的基于隐变量（latent-based）的基准方法在 PSNR 上提升了 0.98 dB，同时在不同行程间产生了更一致的外观。代码将在 https://github.com/IRMVLab/ADM-GS 发布。

## Abstract
Multi-traversal scene reconstruction is important for high-fidelity autonomous driving simulation and digital twin construction. This task involves integrating multiple sequences captured from the same geographical area at different times. In this context, a primary challenge is the significant appearance inconsistency across traversals caused by varying illumination and environmental conditions, despite the shared underlying geometry. This paper presents ADM-GS (Appearance Decomposition Gaussian Splatting for Multi-Traversal Reconstruction), a framework that applies an explicit appearance decomposition to the static background to alleviate appearance entanglement across traversals. For the static background, we decompose the appearance into traversal-invariant material, representing intrinsic material properties, and traversal-dependent illumination, capturing lighting variations. Specifically, we propose a neural light field that utilizes a frequency-separated hybrid encoding strategy. By incorporating surface normals and explicit reflection vectors, this design separately captures low-frequency diffuse illumination and high-frequency specular reflections. Quantitative evaluations on the Argoverse 2 and Waymo Open datasets demonstrate the effectiveness of ADM-GS. In multi-traversal experiments, our method achieves a +0.98 dB PSNR improvement over existing latent-based baselines while producing more consistent appearance across traversals. Code will be available at https://github.com/IRMVLab/ADM-GS.

---

## 论文详细总结（自动生成）

这是一份关于论文《Appearance Decomposition Gaussian Splatting for Multi-Traversal Reconstruction》（ADM-GS）的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在自动驾驶场景中，同一区域往往会被多次采集（多行程/Multi-traversal）。虽然道路、建筑等底层几何结构保持不变，但由于采集时间、天气和光照条件的差异，图像外观存在剧烈的不一致性。
*   **研究动机**：现有的基于 3D 高斯泼溅（3DGS）的方法（如 4DGF、MTGS）通常使用隐式潜变量（Latent codes）或颜色残差来处理外观变化。这种方式容易导致**外观纠缠**——即高频的光照效果（如阴影、镜面高光）被错误地吸收进静态材质表示中，导致在跨行程融合时出现模糊或伪影。
*   **整体含义**：本文旨在通过显式的“材质-光照”分解，构建一个能够解耦环境因素与物体固有属性的重建框架，从而实现更高保真度、更具一致性的数字孪生场景构建。

### 2. 方法论：核心思想与关键技术
ADM-GS 的核心思想是将静态背景的外观显式分解为**行程不变的材质（Material）**和**行程相关的光照（Illumination）**。

*   **外观分解公式**：将渲染图像 $I$ 建模为材质场 $M$ 与神经光场 $L$ 的哈达玛积（逐像素相乘）：$I = M \odot L$。
    *   **材质场 (Material Field)**：仅依赖空间几何特征，预测 0 到 1 之间的反射率，确保在不同行程间保持恒定。
    *   **神经光场 (Neural Light Field)**：依赖法线、视角、反射向量及行程嵌入（Embedding），捕捉阴影和高光。
*   **几何正则化（Surface-Aware Regularization）**：
    *   为了获得准确的法线，引入了**各向异性惩罚（Flatness Constraint）**，强制 3D 高斯椭球体在最短轴方向压缩，使其更接近扁平的“面片”（Surfels）。
    *   直接利用高斯球的最短轴作为几何法线，用于后续的光照计算。
*   **频率分离的混合编码**：
    *   **低频编码**：对表面法线使用 1 阶球面谐波（SH）编码，用于建模平滑的漫反射。
    *   **高频编码**：引入**反射向量重参数化**（借鉴 Ref-NeRF），并使用 4 阶 SH 编码，专门捕捉金属、玻璃等表面的尖锐镜面反射。
*   **监督策略**：除了 RGB 损失外，还利用单目估计模型（UniDepthV2 和 Ouroboros）生成“伪深度”、“伪法线”和“伪材质”作为辅助监督，引导分解过程。

### 3. 实验设计
*   **数据集**：
    *   **Argoverse 2**：用于单行程和多行程评估，包含丰富的城市驾驶场景。
    *   **Waymo Open Dataset**：用于验证在大规模、高分辨率场景下的泛化能力。
*   **Benchmark 任务**：新视角合成（NVS）和场景重建质量。
*   **对比方法**：
    *   **单行程基准**：StreetGS, OmniRe, Bilateral-Driving, 4DGF, PVG。
    *   **多行程基准**：4DGF, MTGS（目前该领域的 SOTA）。

### 4. 资源与算力
*   **硬件环境**：使用了工作站级别的算力，配备 **4 张 NVIDIA RTX 4090 GPU**。
*   **训练时长**：
    *   Argoverse 2 单行程：30,000 次迭代。
    *   Waymo 数据集：60,000 次迭代。
    *   Argoverse 2 多行程：100,000 次迭代。
*   文中未明确给出具体的总训练小时数，但基于 3DGS 的特性和 100k 的迭代量，预计单次多行程实验耗时在数小时至十余小时之间。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流自动驾驶数据集上进行了详尽测试，涵盖了从单序列到多序列的多种配置。
*   **消融实验**：
    *   **系统级消融**：验证了从隐式建模到显式分解，再到加入几何约束的性能增益。
    *   **模块级消融**：深入分析了法线输入、反射向量输入以及法线损失函数对最终渲染质量的影响。
*   **充分性评价**：实验设计较为全面，不仅有定量指标（PSNR, SSIM, LPIPS），还有定性的外观分解可视化和**场景重光照（Relighting）**应用展示。对比实验选择了最新的 SOTA 方法，确保了评价的客观性。

### 6. 主要结论与发现
*   **性能提升**：在多行程重建任务中，ADM-GS 相比强基准 4DGF 实现了 **+0.98 dB 的 PSNR 提升**。
*   **解耦效果**：显式分解能有效提取出一致的材质图，而将阴影和高光准确地归因于光照分量。
*   **几何重要性**：通过各向异性约束获得的稳定法线是实现高质量外观分解的前提。
*   **反射向量的作用**：引入反射向量作为输入显著降低了 LPIPS（感知损失），证明其在捕捉高频视觉细节方面的优越性。

### 7. 优点（亮点）
*   **显式解耦**：解决了多行程数据中长期存在的外观纠缠问题，使得重建结果在不同时间采集的序列间具有极高的一致性。
*   **频率感知设计**：巧妙地利用不同阶数的 SH 编码处理漫反射和镜面反射，符合物理光学特性。
*   **应用潜力**：分解出的材质和光照场使得“场景重光照”成为可能，为自动驾驶仿真中的天气/时段变换提供了技术基础。

### 8. 不足与局限
*   **依赖伪标签**：训练过程高度依赖单目深度和材质估计模型的质量（如 UniDepthV2），如果预训练模型在某些特殊场景失效，会影响分解效果。
*   **动态物体限制**：目前的外观分解主要应用于静态背景，动态物体（如车辆、行人）仍采用传统的变形/颜色建模，未实现统一的重光照。
*   **计算开销**：相比原始 3DGS，增加了多个 MLP 分支和复杂的编码计算，虽然保持了实时渲染潜力，但训练成本有所增加。
*   **极端环境验证**：实验主要集中在光照变化，对于雨、雪、雾等导致材质表面属性发生物理变化的极端情况，文中未做深入探讨。

（完）
