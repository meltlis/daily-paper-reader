---
title: "AnchorSplat: Feed-Forward 3D Gaussian Splatting with 3D Geometric Priors"
title_zh: AnchorSplat：基于 3D 几何先验的前馈 3D 高斯泼溅
authors: "Xiaoxue Zhang, Xiaoxu Zheng, Yixuan Yin, Tiao Zhao, Kaihua Tang, Michael Bi Mi, Zhan Xu, Dave Zhenyu Chen"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07053v2"
tags: ["query:3dgsir"]
score: 9.0
evidence: 用于场景级重建的前馈3D高斯泼溅框架
tldr: 本文针对现有前馈3D高斯泼溅（3DGS）模型中高斯表示与输入图像过度耦合的问题，提出了AnchorSplat框架。该方法摒弃了传统的像素对齐方式，转而利用3D几何先验（如稀疏点云或体素）作为锚点，在3D空间中直接构建场景表示。这种设计使模型能够独立于图像分辨率和视角数量，显著减少了所需的高斯基元数量，并提升了计算效率。通过引入高斯精炼器进一步优化细节，AnchorSplat在ScanNet++ v2基准上实现了SOTA性能，提供了更具视角一致性的高质量重建结果。
selection_source: fresh_fetch
motivation: 现有的前馈3DGS模型采用像素对齐方式，导致高斯表示与输入图像紧密耦合，限制了其在复杂场景下的几何感知能力和效率。
method: 提出一种由3D几何先验引导的锚点对齐高斯表示方法，并结合高斯精炼器通过少量前馈计算对高斯参数进行微调。
result: 在ScanNet++ v2测试中表现优异，不仅超越了现有SOTA方法，还大幅减少了高斯基元数量并增强了视角一致性。
conclusion: AnchorSplat证明了直接在3D空间利用几何先验构建高斯表示，是实现高效、高质量场景级三维重建的有效途径。
---

## 摘要
最近的前馈高斯重建模型采用像素对齐的表述方式，将每个 2D 像素映射到 3D 高斯，使得高斯表示与输入图像紧密耦合。在本文中，我们提出了 AnchorSplat，这是一种用于场景级重建的新型前馈 3DGS 框架，它直接在 3D 空间中表示场景。AnchorSplat 引入了由 3D 几何先验（如稀疏点云、体素或 RGB-D 点云）引导的锚点对齐高斯表示，从而实现了更具几何感知能力的可渲染 3D 高斯，且该表示独立于图像分辨率和视图数量。这种设计显著减少了所需的高斯数量，在提高计算效率的同时增强了重建保真度。除了锚点对齐设计外，我们还利用高斯精炼器（Gaussian Refiner）通过仅几次前向传递来调整中间高斯。在 ScanNet++ v2 NVS 基准测试上的实验表明，该方法达到了 SOTA 性能，在视图一致性更高且高斯原语数量显著减少的情况下优于以往的方法。

## Abstract
Recent feed-forward Gaussian reconstruction models adopt a pixel-aligned formulation that maps each 2D pixel to a 3D Gaussian, entangling Gaussian representations tightly with the input images. In this paper, we propose AnchorSplat, a novel feed-forward 3DGS framework for scene-level reconstruction that represents the scene directly in 3D space. AnchorSplat introduces an anchor-aligned Gaussian representation guided by 3D geometric priors (e.g., sparse point clouds, voxels, or RGB-D point clouds), enabling a more geometry-aware renderable 3D Gaussians that is independent of image resolution and number of views. This design substantially reduces the number of required Gaussians, improving computational efficiency while enhancing reconstruction fidelity. Beyond the anchor-aligned design, we utilize a Gaussian Refiner to adjust the intermediate Gaussiansy via merely a few forward passes. Experiments on the ScanNet++ v2 NVS benchmark demonstrate the SOTA performance, outperforming previous methods with more view-consistent and substantially fewer Gaussian primitives.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AnchorSplat** 的新型前馈 3D 高斯泼溅（3DGS）框架，旨在解决现有模型在场景级重建中的效率与几何一致性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的前馈 3DGS 模型大多采用“像素对齐（Pixel-aligned）”方案，即每个 2D 像素映射为一个 3D 高斯。
*   **研究动机**：
    *   **冗余性**：高斯数量与图像分辨率和视角数成线性增长，导致计算开销巨大。
    *   **几何不一致**：由于缺乏 3D 空间的显式约束，容易产生浮点伪影（floaters）、表面破碎和模糊。
    *   **视角敏感**：在遮挡或低纹理区域，像素对齐方式难以保证跨视角的一致性。
*   **整体含义**：AnchorSplat 提出“锚点对齐（Anchor-aligned）”思路，直接在 3D 空间中利用几何先验构建表示，使高斯分布独立于图像分辨率。

### 2. 方法论
AnchorSplat 包含三个核心组件，通过两阶段训练实现：
*   **锚点预测器（Anchor Predictor）**：
    *   利用预训练的多视图立体（MVS）模型（如 MapAnything）预测深度图和相机位姿。
    *   将像素反投影到 3D 空间，并使用**最远点采样（FPS）**算法进行下采样，生成稀疏且均匀分布的 3D 锚点。
*   **高斯解码器（Gaussian Decoder）**：
    *   使用 CNN 提取多视图特征，并将其投影到 3D 锚点上。
    *   采用 **Transformer** 架构处理锚点特征，建模空间交互。
    *   每个锚点预测 4 个高斯原语的属性（中心偏移、不透明度、缩放、旋转、球谐系数）。
*   **高斯精炼器（Gaussian Refiner）**：
    *   这是一个即插即用的模块，通过计算渲染图像与真实图像之间的误差（Rendering Error）来调整高斯属性。
    *   利用梯度引导和点云 Transformer（Point Transformer）进一步提升几何精度和色彩一致性。

### 3. 实验设计
*   **数据集**：主要在 **ScanNet++ v2**（高质量室内场景）上进行评估。此外还测试了 **Replica**、**ARKitScenes**（室内）和 **Tanks & Temples**（室外）。
*   **Benchmark**：新视角合成（NVS）任务，评估指标包括 PSNR、SSIM、LPIPS 以及深度精度指标（AbsRel, $\delta_1$）。
*   **对比方法**：
    *   **优化类方法**：3DGS、Mip-Splatting。
    *   **前馈类方法**：AnySplat（目前主流的体素对齐 SOTA 方法）。

### 4. 资源与算力
*   **硬件**：使用了 **64 台 华为 Ascend 910B3 (64GB) NPU**。
*   **训练时长**：
    *   第一阶段（解码器）：5,000 步。
    *   第二阶段（精炼器）：5,000 步。
*   **实现细节**：使用 PyTorch 和 Ascend Flash Attention 算子，采用 bfloat16 精度。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从极稀疏视角（3-5 视图）到极密集视角（128-256 视图）的多种设置。
*   **消融实验**：非常充分，分别验证了：
    1.  输入模态（RGB、深度、相机射线）的影响。
    2.  每个锚点生成高斯数量的影响（1 到 16 个）。
    3.  不同 MVS 后端（MapAnything vs. DA3）的鲁棒性。
    4.  特征聚合策略（均值池化、最大池化等）。
*   **客观性**：通过在多个不同类型的公开数据集上测试，并与 SOTA 方法 AnySplat 进行多维度（质量、速度、高斯数量）对比，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **高效性**：AnchorSplat 使用的高斯数量仅为 AnySplat 的 **1/20** 左右，重建时间缩短了约一半。
*   **高质量**：在 ScanNet++ 上，其 PSNR 和深度精度显著优于 AnySplat，且生成的几何结构更清晰，无明显浮点伪影。
*   **稳定性**：由于锚点固定，模型性能不会随输入视角数量增加而导致计算量爆炸，表现出极佳的扩展性。

### 7. 优点（亮点）
*   **架构创新**：从“像素对齐”转向“锚点对齐”，从根本上解决了前馈 3DGS 的冗余问题。
*   **几何感知**：引入 3D 几何先验，使得模型在处理弱纹理或复杂遮挡区域时比纯图像驱动的方法更鲁棒。
*   **即插即用精炼器**：精炼器模块无需重新训练主网络即可提升细节，具有很强的实用性。

### 8. 不足与局限
*   **先验依赖**：模型高度依赖初始几何先验（MVS 的输出）。如果初始深度预测存在严重缺失或错误，受限于高斯增长的约束，模型可能难以覆盖这些“空洞”区域。
*   **动态场景限制**：目前主要针对静态场景重建，尚未讨论在动态或 4D 场景中的应用。
*   **算力门槛**：虽然推理高效，但训练阶段使用了 64 块高性能 NPU，对于普通研究者来说复现成本较高。

（完）
