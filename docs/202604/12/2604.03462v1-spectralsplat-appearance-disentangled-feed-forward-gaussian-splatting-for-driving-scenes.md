---
title: "SpectralSplat: Appearance-Disentangled Feed-Forward Gaussian Splatting for Driving Scenes"
title_zh: SpectralSplat：用于驾驶场景的外观解耦前馈高斯泼溅
authors: "Quentin Herau, Tianshuo Xu, Depu Meng, Jiezhi Yang, Chensheng Peng, Spencer Sherk, Yihan Hu, Wei Zhan"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03462v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 将外观与几何解耦以实现基于高斯泼溅的重光照
tldr: 针对自动驾驶场景中前馈3D高斯泼溅（3DGS）方法存在的几何与外观（光照、天气等）耦合问题，本文提出SpectralSplat。该方法通过将颜色预测分解为外观无关的基础流和外观相关的适配流，并利用混合重光照流水线生成的数据进行训练，实现了外观与几何的解耦。SpectralSplat在保持高质量重建的同时，支持可控的外观迁移和时序一致的重光照。
selection_source: fresh_fetch
motivation: 现有的前馈3DGS方法将几何与瞬态外观属性耦合，限制了在不同环境条件下的重光照和外观迁移能力。
method: 提出一种双流颜色预测架构，结合DINOv2外观嵌入和混合重光照训练流水线，并引入外观可适配的时序历史机制。
result: 实验表明该方法在维持基础模型重建质量的前提下，实现了优异的外观迁移效果和跨序列的时序一致性。
conclusion: SpectralSplat成功实现了驾驶场景中3DGS的外观解耦，为自动驾驶仿真中的环境编辑和多轨迹数据融合提供了有效方案。
---

## 摘要
前馈 3D 高斯泼溅（3D Gaussian Splatting）方法在自动驾驶场景中取得了显著的重建质量，但它们往往将场景几何与光照、天气和时间等瞬态外观属性耦合在一起。这种耦合限制了重光照、外观迁移以及在不同环境条件下捕获的多轨迹数据之间的一致渲染。我们提出了 SpectralSplat，这是一种在前馈高斯泼溅框架内实现外观与几何解耦的方法。我们的核心见解是将颜色预测分解为外观无关的基础流和外观条件的自适应流，两者均由共享的 MLP 生成，该 MLP 以源自 DINOv2 特征的全局外观嵌入为条件。为了强制实现解耦，我们利用结合了基于物理的内在分解与基于扩散的生成式细化的混合重光照流水线生成成对观测数据进行训练，并使用互补的一致性、重建、跨外观和基础颜色损失进行监督。我们还引入了外观可自适应的时间历史来存储外观无关特征，使得累积的高斯分布能够在任意目标外观下进行重新渲染。实验表明，SpectralSplat 在保持底层骨干网络重建质量的同时，实现了可控的外观迁移和驾驶序列中时间一致的重光照。

## Abstract
Feed-forward 3D Gaussian Splatting methods have achieved impressive reconstruction quality for autonomous driving scenes, yet they entangle scene geometry with transient appearance properties such as lighting, weather, and time of day. This coupling prevents relighting, appearance transfer, and consistent rendering across multi-traversal data captured under varying environmental conditions. We present SpectralSplat, a method that disentangles appearance from geometry within a feed-forward Gaussian Splatting framework. Our key insight is to factor color prediction into an appearance-agnostic base stream and and appearance-conditioned adapted stream, both produced by a shared MLP conditioned on a global appearance embedding derived from DINOv2 features. To enforce disentanglement, we train with paired observations generated by a hybrid relighting pipeline that combines physics-based intrinsic decomposition with diffusion based generative refinement, and supervise with complementary consistency, reconstruction, cross-appearance, and base color losses. We further introduce an appearance-adaptable temporal history that stores appearance-agnostic features, enabling accumulated Gaussians to be re-rendered under arbitrary target appearances. Experiments demonstrate that SpectralSplat preserves the reconstruction quality of the underlying backbone while enabling controllable appearance transfer and temporally consistent relighting across driving sequences.

---

## 论文详细总结（自动生成）

以下是对论文《SpectralSplat: Appearance-Disentangled Feed-Forward Gaussian Splatting for Driving Scenes》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的前馈 3D 高斯泼溅（3DGS）方法虽然重建速度快，但将场景的**几何结构**与**瞬态外观**（如光照、天气、曝光）深度耦合。这种“烘焙”在模型中的颜色导致了三个局限：无法进行重光照编辑、时序累积时颜色不一致、难以利用不同时间捕获的多轨迹数据。
*   **核心目标**：在无需针对每个场景进行耗时优化的前提下（前馈式），实现自动驾驶场景中几何与外观的解耦，从而支持可控的外观迁移和一致的场景渲染。

### 2. 方法论
*   **核心思想**：将颜色预测分解为“外观无关的基础流”和“外观相关的适配流”，并利用混合重光照流水线生成的成对数据进行监督训练。
*   **关键技术细节**：
    *   **全局外观嵌入（Global Appearance Embedding）**：利用 DINOv2 提取图像特征，通过全局平均池化和轻量级 MLP 编码成一个 64 维的向量 $a$，代表场景的整体光照氛围。
    *   **因子化颜色预测**：共享的颜色 MLP 被调用两次。一次输入零向量得到基础颜色 $c_{base}$（外观无关）；一次输入外观嵌入 $a$ 得到适配颜色 $c_{ada}$（用于最终渲染）。
    *   **外观可适配的时序历史**：在缓存历史高斯点时存储特征而非固定颜色。在渲染时，根据当前帧的外观嵌入重新计算历史点的颜色，消除色差。
    *   **混合重光照流水线**：结合了**基于物理的内在分解**（MVInverse，保证多视角几何一致性）和**基于扩散的生成模型**（IC-Light，保证照片级的真实感），通过频率感知潜空间引导（Frequency-Aware Latent Guidance）生成高质量的训练对。
    *   **损失函数**：包括基础不变性损失（$L_{inv}$）、增强重建损失（$L_{aug}$）、外观交换一致性损失（$L_{swap}$）和基础颜色对齐损失（$L_{base}$）。

### 3. 实验设计
*   **数据集**：Waymo Open Dataset（798 训练 / 202 验证序列）和 nuScenes（约 13.6 万训练 / 3 万验证样本）。
*   **Benchmark 任务**：重建（Reconstruction）和新视角合成（NVS）。
*   **对比方法**：
    *   **前馈方法**：UniSplat（基准骨干）、MVSplat、DepthSplat、PixelSplat、Omni-Scene。
    *   **优化方法**：WildGaussians（带外观嵌入的 3DGS 优化方法）。
*   **评价指标**：PSNR、SSIM、LPIPS 以及跨外观迁移的定量评估。

### 4. 资源与算力
*   **硬件**：使用了 **64 块 NVIDIA A100 GPU**。
*   **训练配置**：Batch size 为 64，训练 10 个 epoch。
*   **推理效率**：处理 10 帧场景约需 18 秒，比需要逐场景优化的 WildGaussians（约 31 分钟）快约 **100 倍**。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流的大规模自动驾驶数据集上进行了详尽测试，涵盖了数千个序列。
*   **消融实验**：针对 $L_{swap}$ 和 $L_{base}$ 等核心损失函数进行了消融研究，验证了各组件对解耦效果的贡献。
*   **充分性评价**：实验设计较为全面，不仅对比了重建质量，还通过 t-SNE 可视化证明了外观嵌入的聚类特性，并通过“跨外观交换渲染”验证了解耦的彻底性。实验结果客观地展示了在保持重建精度的同时获得编辑能力。

### 6. 主要结论与发现
*   **性能保持**：SpectralSplat 在引入解耦机制后，重建质量（PSNR/SSIM）不仅没有下降，在某些指标上甚至优于原始 UniSplat。
*   **解耦有效性**：通过交换外观嵌入，模型能够成功将“黄昏”的外观迁移到“白天”的几何结构上，且基础颜色保持高度一致。
*   **时序一致性**：改进后的时序历史机制显著减少了长序列渲染中的颜色闪烁和不一致问题。

### 7. 优点
*   **高效性**：实现了首个支持外观解耦的**前馈式**驾驶场景 3DGS 框架，极具实用价值。
*   **混合流水线**：巧妙地结合了物理先验和扩散模型，解决了训练数据缺失的问题，同时保证了生成数据的 3D 一致性。
*   **架构优雅**：通过简单的零向量输入实现基础色锚定，无需复杂的解耦架构即可获得良好效果。

### 8. 不足与局限
*   **全局性限制**：目前使用全局外观嵌入，可能无法精细处理局部光照变化（如路灯、局部阴影）。
*   **合成数据依赖**：训练高度依赖于重光照流水线生成的合成数据，若生成数据存在伪影，可能会影响模型的上限。
*   **动态物体**：虽然继承了骨干网络处理动态物体的能力，但针对动态物体在不同光照下的阴影变化等复杂交互处理仍有提升空间。

（完）
