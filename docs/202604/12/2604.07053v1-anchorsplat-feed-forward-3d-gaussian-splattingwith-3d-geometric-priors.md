---
title: "AnchorSplat: Feed-Forward 3D Gaussian SplattingWith 3D Geometric Priors"
title_zh: AnchorSplat：基于 3D 几何先验的前馈 3D 高斯泼溅
authors: "Xiaoxue Zhang, Xiaoxu Zheng, Yixuan Yin, Tiao Zhao, Kaihua Tang, Michael Bi Mi, Zhan Xu, Dave Zhenyu Chen"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07053v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 用于场景级重建的前馈3D高斯泼溅框架
tldr: AnchorSplat 是一种新型的前馈式 3D 高斯泼溅框架，旨在解决现有像素对齐模型过度依赖输入图像的问题。它引入了由 3D 几何先验（如稀疏点云或体素）引导的锚点对齐表示，使 3D 高斯生成独立于图像分辨率和视角数量。通过减少高斯基元数量并结合高斯精炼器，该方法在提高计算效率的同时增强了重建保真度，在 ScanNet++ v2 基准测试中达到了 SOTA 性能。
selection_source: fresh_fetch
motivation: 现有的前馈 3DGS 模型采用像素对齐方式，导致高斯表示与输入图像紧密耦合，限制了其在处理不同分辨率和多视角场景时的灵活性与效率。
method: 提出锚点对齐的高斯表示，利用 3D 几何先验直接在 3D 空间中表示场景，并配合高斯精炼器通过少量前向传递优化结果。
result: 在 ScanNet++ v2 渲染基准上超越了现有方法，实现了更强的视角一致性，且使用的高斯基元数量显著减少。
conclusion: AnchorSplat 证明了引入 3D 几何先验能有效提升前馈 3D 重建的效率与质量，为场景级 3DGS 重建提供了新思路。
---

## 摘要
最近的前馈高斯重建模型采用像素对齐的方案，将每个 2D 像素映射到 3D 高斯，使得高斯表示与输入图像紧密耦合。在本文中，我们提出了 AnchorSplat，这是一种用于场景级重建的新型前馈 3DGS 框架，它直接在 3D 空间中表示场景。AnchorSplat 引入了由 3D 几何先验（如稀疏点云、体素或 RGB-D 点云）引导的锚点对齐高斯表示，从而实现了更具几何感知能力且独立于图像分辨率和视图数量的可渲染 3D 高斯。这种设计显著减少了所需的高斯数量，在提高计算效率的同时增强了重建保真度。除了锚点对齐设计外，我们还利用高斯精炼器（Gaussian Refiner）仅通过少量前馈过程来调整中间高斯。在 ScanNet++ v2 NVS 基准测试上的实验表明，该方法达到了 SOTA 性能，在视图一致性更高且高斯原语显著减少的情况下优于以往的方法。

## Abstract
Recent feed-forward Gaussian reconstruction models adopt a pixel-aligned formulation that maps each 2D pixel to a 3D Gaussian, entangling Gaussian representations tightly with the input images. In this paper, we propose AnchorSplat, a novel feed-forward 3DGS framework for scene-level reconstruction that represents the scene directly in 3D space. AnchorSplat introduces an anchor-aligned Gaussian representation guided by 3D geometric priors (e.g., sparse point clouds, voxels, or RGB-D point clouds), enabling a more geometry-aware renderable 3D Gaussians that is independent of image resolution and number of views. This design substantially reduces the number of required Gaussians, improving computational efficiency while enhancing reconstruction fidelity. Beyond the anchor-aligned design, we utilize a Gaussian Refiner to adjust the intermediate Gaussiansy via merely a few forward passes. Experiments on the ScanNet++ v2 NVS benchmark demonstrate the SOTA performance, outperforming previous methods with more view-consistent and substantially fewer Gaussian primitives.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AnchorSplat** 的新型前馈 3D 高斯泼溅（3DGS）框架，旨在解决现有模型在场景级重建中的效率与几何一致性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的前馈 3DGS 模型大多采用“像素对齐（Pixel-aligned）”方案，即每个 2D 像素映射为一个 3D 高斯。
*   **研究背景与痛点**：
    *   **冗余性**：高斯数量与图像分辨率和视角数成线性增长，导致计算开销巨大。
    *   **几何不一致**：由于缺乏 3D 空间的显式约束，容易产生浮点伪影（floaters）、表面破碎和重影。
    *   **视角依赖**：重建质量高度依赖输入视角的覆盖范围，在稀疏视角下表现较差。
*   **整体含义**：论文提出将高斯表示从 2D 像素解耦，转而采用基于 3D 空间“锚点（Anchors）”的对齐方式，利用几何先验引导重建。

### 2. 方法论
AnchorSplat 由三个核心模块组成：
*   **锚点预测器（Anchor Predictor）**：利用预训练的多视图立体（MVS）模型（如 MapAnything）预测深度和位姿，将像素反投影到 3D 空间，并通过最远点采样（FPS）下采样生成稀疏的 3D 锚点。
*   **高斯解码器（Gaussian Decoder）**：
    *   使用 CNN 提取多视图图像特征。
    *   将 2D 特征投影到 3D 锚点上。
    *   通过 Transformer 建模锚点间的空间交互，并利用 MLP 为每个锚点预测 4 个高斯原语的属性（偏移、不透明度、缩放、旋转、球谐系数）。
*   **高斯精炼器（Gaussian Refiner）**：这是一个即插即用的模块。它计算渲染图像与真值的误差，将误差反投影回 3D 空间，利用 Point Transformer 进一步微调高斯属性，提升细节保真度。

### 3. 实验设计
*   **数据集**：主要在 **ScanNet++ v2**（高保真室内场景）上进行评估。此外还在 **ARKitScenes**、**Replica**（室内）和 **Tanks and Temples**（室外）上进行了泛化性测试。
*   **Benchmark 与对比方法**：
    *   **优化类方法**：原始 3DGS、Mip-Splatting。
    *   **前馈类方法**：**AnySplat**（主要的 SOTA 对比对象，属于体素对齐方案）。
*   **评估指标**：PSNR、SSIM、LPIPS（图像质量）；AbsRel、$\delta_1$（深度/几何准确性）；高斯数量（NumGS）及重建耗时。

### 4. 资源与算力
*   **硬件**：使用了 **64 台华为昇腾 (Ascend) 910B3 (64GB) NPU**。
*   **训练细节**：
    *   采用两阶段训练：第一阶段训练解码器（5k 步），第二阶段冻结解码器训练精炼器（5k 步）。
    *   使用 AdamW 优化器，计算精度为 bfloat16。
*   **效率**：重建时间约为 3.9s - 6.1s（视输入视角数而定），显著快于优化类方法。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括不同输入视角数量（3, 5, 32, 48, 64, 128, 256 视图）的对比。
*   **消融实验**：
    *   输入模态的影响（RGB、深度、相机射线嵌入）。
    *   每个锚点生成高斯数量的影响（1 到 16 个）。
    *   特征聚合策略（均值池化、最大池化等）。
    *   不同 MVS 后端（MapAnything vs. DA3）的影响。
*   **充分性评价**：实验设计非常全面，涵盖了从极端稀疏到极端稠密视角的场景，且在多个不同类型的公开数据集上验证了泛化能力，对比实验客观且具有说服力。

### 6. 主要结论与发现
*   **高效性**：AnchorSplat 使用的高斯数量仅为 AnySplat 的 **1/20** 左右，但渲染质量更高。
*   **几何一致性**：锚点对齐设计显著减少了重影和浮点伪影，生成的深度图更清晰、更符合物理真实。
*   **稳定性**：该方法对输入图像分辨率不敏感，且在视角增加时，高斯数量保持恒定，计算成本增长受控。
*   **精炼器作用**：Gaussian Refiner 能有效修复边界模糊和空洞，显著提升视觉效果。

### 7. 优点（亮点）
*   **架构创新**：从像素对齐转向锚点对齐，本质上解决了前馈 3DGS 的冗余问题。
*   **即插即用精炼器**：引入误差反馈机制进行属性修正，是一种提升通用模型上限的有效手段。
*   **端到端一致性**：能够同时输出高质量的 RGB 图像和精确的深度几何，适用于下游导航或理解任务。

### 8. 不足与局限
*   **依赖先验质量**：AnchorSplat 的表现高度依赖于初始 MVS 模型提供的几何先验。如果先验点云存在大规模缺失或严重错误，模型难以在完全空白的区域“生长”出正确的高斯。
*   **静态场景限制**：目前主要针对静态场景重建，未讨论在动态场景或大规模城市级场景中的表现。
*   **内存开销**：虽然高斯数量减少了，但 Transformer 处理大规模锚点特征时仍存在一定的显存压力。

（完）
