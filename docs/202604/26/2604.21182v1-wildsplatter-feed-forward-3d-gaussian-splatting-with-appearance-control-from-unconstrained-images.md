---
title: "WildSplatter: Feed-forward 3D Gaussian Splatting with Appearance Control from Unconstrained Images"
title_zh: WildSplatter：基于无约束图像且具有外观控制的前馈 3D 高斯泼溅
authors: "Yuki Fujimura, Takahiro Kushida, Kazuya Kitano, Takuya Funatomi, Yasuhiro Mukaigawa"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21182v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 3D高斯泼溅中的外观控制与光照变化
tldr: WildSplatter 是一种前馈式 3D 高斯泼溅（3DGS）模型，专门用于处理相机参数未知且光照条件多变的非约束图像集。该模型通过联合学习 3D 高斯参数和外观嵌入，实现了从稀疏视图到高质量 3D 场景的秒级重建，并支持灵活的外观控制。实验证明，该方法在处理具有显著光照变化的真实世界数据集时，性能显著优于现有的无位姿 3DGS 方法。
selection_source: fresh_fetch
motivation: 传统的 3DGS 依赖迭代优化、已知相机参数和一致的光照，难以应对现实中光照多变且位姿未知的非约束图像。
method: 提出一种前馈模型，通过输入图像联合预测 3D 高斯和外观嵌入，利用外观调制机制实现对不同光照条件的动态适应。
result: 该方法能在不到一秒内完成 3D 重建，并在具有挑战性的变光照真实数据集上超越了现有的无位姿 3DGS 技术。
conclusion: WildSplatter 证明了前馈 3DGS 在处理非约束图像和外观控制方面的有效性，为快速、鲁棒的 3D 场景重建提供了新方案。
---

## 摘要
我们提出了 WildSplatter，这是一种针对具有未知相机参数和多变光照条件的无约束图像的前馈 3D 高斯泼溅（3DGS）模型。3DGS 是一种有效的场景表示，能够实现高质量、实时的渲染；然而，它通常需要迭代优化，以及在已知相机参数和一致光照条件下捕获的多视图图像。WildSplatter 在无约束的照片集合上进行训练，并以输入图像为条件，联合学习 3D 高斯和外观嵌入。这种设计能够灵活地调节高斯颜色，以表示光照和外观的显著变化。我们的方法可以在不到一秒的时间内从稀疏的输入视图中重建 3D 高斯，同时还能在不同的光照条件下实现外观控制。实验结果表明，在具有不同光照的挑战性真实世界数据集上，我们的方法优于现有的无位姿 3DGS 方法。

## Abstract
We propose WildSplatter, a feed-forward 3D Gaussian Splatting (3DGS) model for unconstrained images with unknown camera parameters and varying lighting conditions. 3DGS is an effective scene representation that enables high-quality, real-time rendering; however, it typically requires iterative optimization and multi-view images captured under consistent lighting with known camera parameters. WildSplatter is trained on unconstrained photo collections and jointly learns 3D Gaussians and appearance embeddings conditioned on input images. This design enables flexible modulation of Gaussian colors to represent significant variations in lighting and appearance. Our method reconstructs 3D Gaussians from sparse input views in under one second, while also enabling appearance control under diverse lighting conditions. Experimental results demonstrate that our approach outperforms existing pose-free 3DGS methods on challenging real-world datasets with varying illumination.

---

## 论文详细总结（自动生成）

以下是对论文《WildSplatter: Feed-forward 3D Gaussian Splatting with Appearance Control from Unconstrained Images》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 3D 高斯泼溅（3DGS）在处理“野外”（in-the-wild）图像集时面临三大挑战：1) 需要耗时的迭代优化；2) 依赖精确的相机位姿（已知参数）；3) 假设光照条件一致，难以处理互联网照片中常见的剧烈光照变化。
*   **研究动机**：旨在开发一种**前馈式（Feed-forward）**模型，能够从少量、无位姿、光照不一致的稀疏图像中，在秒级时间内重建 3D 场景，并实现对渲染外观（如光照、天气）的灵活控制。

### 2. 方法论
*   **核心思想**：将场景的**几何结构**与**外观表现**解耦。假设几何结构在不同光照下保持不变，而外观变化通过低维潜空间（外观嵌入）进行调制。
*   **关键技术细节**：
    *   **架构基础**：基于 Depth Anything 3 的 Vision Transformer (ViT) 骨干网络，利用其强大的几何先验。
    *   **几何估计**：从上下文图像（Context Images）中预测无色的 3D 高斯参数（中心、不透明度、旋转、缩放）以及局部特征图。
    *   **外观估计**：引入一个外观编码器，从目标图像中提取全局外观嵌入（Appearance Embedding）。
    *   **颜色调制**：将局部特征图与全局外观嵌入拼接，通过卷积层预测球谐函数（SH）系数，从而生成带颜色的 3D 高斯。
    *   **可见性掩码（Visibility Mask）**：在训练阶段利用深度一致性生成掩码，以过滤掉遮挡区域和瞬态物体（如行人），增强鲁棒性。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：MegaScenes（包含全球地标的互联网照片集），经过严格筛选得到约 1.48 万个视图组。
    *   **评估集**：NeRF-OSR（户外场景重光照数据集），包含不同日期和光照下的真实场景。
*   **对比方法（Baselines）**：
    *   **无位姿前馈模型**：SPFSplat、AnySplat、Depth Anything 3。
    *   **优化类模型**：WildGaussians（针对非约束图像的 3DGS 优化方法）。
*   **评价指标**：PSNR（峰值信噪比）和 LPIPS（感知相似度）。

### 4. 资源与算力
*   **训练配置**：使用 **4 台 NVIDIA A100 GPU** 进行训练，总计 1.5 万次迭代，耗时约 **2 天**。
*   **推理性能**：在单张 **NVIDIA RTX 6000 Ada GPU** 上，处理两张输入图像的推理时间约为 **0.375 秒**，远快于优化类方法的分钟级耗时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2、3、4 个输入视图下的新视角合成对比，并进行了外观插值、跨数据集外观控制、t-SNE 嵌入空间可视化以及消融实验。
*   **充分性评价**：实验设计较为充分，对比了当前最先进的无位姿前馈模型，并验证了模型在极端光照变化下的泛化能力。消融实验验证了外观嵌入维度（32 vs 256）对结果的影响，证明了低维空间的有效性。

### 6. 主要结论与发现
*   **性能领先**：WildSplatter 在处理具有显著光照变化的户外场景时，定量和定性指标均优于现有的无位姿前馈 3DGS 方法。
*   **外观可控性**：学习到的外观嵌入具有良好的平滑性，支持在不同光照风格之间进行线性插值，甚至可以将 A 场景的光照迁移到 B 场景的几何结构上。
*   **自动去噪**：模型在训练过程中通过降低不透明度，能够隐式地学会抑制上下文图像中的瞬态物体（如行人）。

### 7. 优点
*   **高效性**：实现了真正的秒级前馈重建，无需推理时优化。
*   **鲁棒性**：成功解决了无位姿和非一致光照的联合挑战，扩展了 3DGS 的应用边界。
*   **灵活性**：全局外观嵌入的设计不仅提升了渲染质量，还赋予了用户对场景氛围的控制权。

### 8. 不足与局限
*   **局部光照缺失**：由于采用全局嵌入，模型在处理复杂的局部光照效果（如特定角度的阴影、高光）时可能不够精确，存在轻微的颜色漂移。
*   **依赖先验**：高度依赖预训练的深度估计模型（Depth Anything 3），若骨干网络失效，几何重建质量将受限。
*   **动态物体处理**：虽然能抑制瞬态物体，但尚未实现对动态场景的显式建模。

（完）
