---
title: Instant Colorization of Gaussian Splats
title_zh: 高斯泼溅的即时着色
authors: "Daniel Lieber, Alexander Mock, Nils Wandel"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17155v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 将图像信息映射回高斯以进行重光照
tldr: 针对高斯泼溅（Gaussian Splatting）在3D场景重建中的应用，本文提出了一种将2D图像信息（如颜色、特征或分割掩码）高效映射回3D高斯点云的新方法。该方法通过正规方程解决可见性加权的最小二乘问题，有效处理了视角依赖和遮挡挑战。相比传统的梯度下降法，该方法在场景重照明、特征增强和语义分割任务中实现了高达一个数量级的加速，显著提升了3D场景属性编辑的效率。
selection_source: fresh_fetch
motivation: 旨在解决如何将2D图像信息高效且准确地反向映射到3D高斯点云中，以克服视角依赖和遮挡处理带来的挑战。
method: 采用正规方程求解每个高斯点的可见性加权最小二乘问题，并利用现有的微分渲染器进行高效实现。
result: 在场景重照明、特征富化和3D语义分割等任务上，该方法比传统的梯度下降基准方法快了近十倍。
conclusion: 该研究为3D高斯泼溅场景的属性编辑和语义理解提供了一个极速且通用的反向映射框架。
---

## 摘要
高斯泼溅（Gaussian Splatting）近期已成为照片级真实感 3D 场景重建与渲染中最受欢迎的框架之一。虽然目前的光栅化器能够实现 3D 高斯泼溅到 2D 相机视图的高效映射，但本研究专注于将 2D 图像信息（如颜色、神经特征或分割掩码）高效地反向映射到现有的高斯泼溅场景中。这种“相反”的方向使得从场景重照明、风格化到 3D 语义分割的各种应用成为可能，但也带来了诸如视点相关着色和遮挡处理等挑战。我们的方法通过使用正规方程为每个高斯求解可见性加权最小二乘问题来应对这些挑战，并且可以利用现有的可微光栅化器高效实现。我们在场景重照明、特征增强和 3D 语义分割任务上证明了该方法的有效性，与基于梯度下降的基准方法相比，实现了高达一个数量级的加速。

## Abstract
Gaussian Splatting has recently become one of the most popular frameworks for photorealistic 3D scene reconstruction and rendering. While current rasterizers allow for efficient mappings of 3D Gaussian splats onto 2D camera views, this work focuses on mapping 2D image information (e.g. color, neural features or segmentation masks) efficiently back onto an existing scene of Gaussian splats. This 'opposite' direction enables applications ranging from scene relighting and stylization to 3D semantic segmentation, but also introduces challenges, such as view-dependent colorization and occlusion handling.   Our approach tackles these challenges using the normal equation to solve a visibility-weighted least squares problem for every Gaussian and can be implemented efficiently with existing differentiable rasterizers. We demonstrate the effectiveness of our approach on scene relighting, feature enrichment and 3D semantic segmentation tasks, achieving up to an order of magnitude speedup compared to gradient descent-based baselines.

---

## 论文详细总结（自动生成）

这篇论文《Instant Colorization of Gaussian Splats》提出了一种高效的方法，旨在将2D图像信息（如颜色、神经特征或分割掩码）快速映射回已有的3D高斯泼溅（Gaussian Splatting, GS）场景中。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然3D高斯泼溅在从3D到2D的渲染（正向）上非常高效，但将2D图像信息反向映射回3D高斯点（反向）仍然具有挑战性。
*   **研究动机**：现有的反向映射方法（如场景重照明、语义分割）通常依赖于基于梯度的优化（如Adam），这需要大量的迭代和计算时间。作者希望寻找一种解析解法，以实现“即时”的属性映射，从而支持场景重照明、特征增强和3D分割等下游任务。

### 2. 方法论
*   **核心思想**：将着色问题建模为一个**可见性加权的最小二乘问题（Visibility-Weighted Least Squares）**，并利用**正规方程（Normal Equation）**直接求解。
*   **关键技术细节**：
    *   **可见性计算**：利用可微渲染器计算像素对高斯颜色系数的偏导数，以此作为可见性权重，自动处理遮挡和透明度。
    *   **视角依赖处理**：通过引入球谐函数（Spherical Harmonics, SH）基函数，将视角相关的颜色建模为线性系统。
    *   **正规方程求解**：为每个高斯点构建一个小型线性方程组，通过 GPU 并行求解。
    *   **正则化**：应用 Tikhonov 正则化（岭回归）来处理观察视角不足导致的病态矩阵问题。
    *   **迭代细化**：提出一种细化方案，通过计算渲染图像与目标图像的残差来更新系数，以补偿高斯点之间透明度叠加产生的误差。

### 3. 实验设计
*   **实验场景与任务**：
    1.  **场景重照明（Relighting）**：使用自采的真实世界数据集（Duck, Food, Bathroom），在不同光照条件下拍摄。
    2.  **特征增强（Feature Enrichment）**：将 DINOv2 的神经特征投影到 3D 场景中。
    3.  **3D 语义分割**：将 SAM 2 生成的 2D 掩码映射到 3D 高斯点。
*   **对比基准（Benchmark）**：
    *   对比了主流的梯度下降优化器：**Adam, AdamW, Adagrad, RMSprop**。
    *   在分割任务中对比了 SOTA 方法 **SAGA**。
*   **评估指标**：L1、L2 损失，PSNR（峰值信噪比）以及处理时间。

### 4. 资源与算力
*   **硬件配置**：Intel Core i9-14900KF CPU，**NVIDIA GeForce RTX 4090 GPU** (24GB)，64GB DDR5 RAM。
*   **耗时情况**：
    *   该方法在约 **1秒** 内即可达到比 Adam 优化 40-50秒 更好的效果。
    *   特征映射和分割任务通常在 1秒 内完成。
    *   相比梯度下降法，实现了约 **10倍** 的加速。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了多个数据集（自采数据集、Mip-NeRF 360 的 Garden 场景、LLFF 数据集）。
*   **充分性评价**：实验设计较为全面，不仅有定量指标（损失曲线、PSNR），还有丰富的定性视觉对比。通过不同任务（重照明、特征、分割）验证了算法的通用性。对比实验考虑了多种优化器和不同的学习率设置，确保了对比的客观性。

### 6. 主要结论与发现
*   **速度优势**：该方法显著快于基于梯度的优化方法，能够实现近乎即时的属性投影。
*   **质量优势**：在重照明任务中，该方法在更短的时间内达到了更低的测试损失和更高的 PSNR。
*   **功能性**：成功实现了 3D 场景的线性光场组合（通过组合不同光照下的 SH 系数）和精细的 3D 语义分割（尤其在复杂结构如 T-Rex 骨架上表现优于 SAGA）。

### 7. 优点
*   **高效性**：利用解析解代替迭代优化，极大地提升了效率。
*   **易集成**：可以轻松集成到现有的可微高斯渲染框架中，无需修改底层光栅化逻辑。
*   **多功能**：一个框架同时解决了颜色、特征和掩码的投影问题。
*   **稳定性**：正则化项有效解决了稀疏视角下的伪影问题。

### 8. 不足与局限
*   **非实时性**：虽然比 Adam 快得多，但目前还未达到真正的实时（Real-time）处理速度（仍需约 1秒）。
*   **边界伪影**：在分割任务中，有时会错误地分割出穿透边界的高斯点（受限于原始几何重建的精度）。
*   **模型限制**：目前仅适用于线性颜色模型（如 SH），对于非线性或更复杂的材质模型（如带有空间变化颜色场的模型）可能不直接适用。

（完）
