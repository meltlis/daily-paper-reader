---
title: "SGS-Intrinsic: Semantic-Invariant Gaussian Splatting for Sparse-View Indoor Inverse Rendering"
title_zh: SGS-Intrinsic：面向稀疏视角室内逆渲染的语义不变高斯泼溅
authors: "Jiahao Niu, Rongjia Zheng, Wenju Xu, Wei-Shi Zheng, Qing Zhang"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27516v2"
tags: ["query:3dgsir"]
score: 10.0
evidence: 利用3DGS进行室内逆向渲染以实现材质和光照分解
tldr: 针对室内场景在稀疏视角下逆渲染质量差的问题，本文提出SGS-Intrinsic框架。该方法利用语义和几何先验构建稠密且一致的高斯语义场，为逆渲染奠定基础。通过结合混合光照模型、材质先验及去阴影模型，实现了材质与光照的精确解耦，有效解决了阴影干扰问题，在稀疏视角下显著提升了室内场景的几何重建与逆渲染精度。
selection_source: fresh_fetch
motivation: 现有的3D高斯泼溅逆渲染方法主要针对物体中心场景，在稀疏视角下的室内环境重建和材质光照解耦方面表现不佳。
method: 利用语义和几何先验构建一致的高斯语义场，并结合混合光照模型、光照不变材质约束及去阴影模型进行材质与光照的解耦。
result: 在基准数据集上的实验证明，该方法在重建保真度和逆渲染质量上均显著优于现有的基于3DGS的逆渲染技术。
conclusion: SGS-Intrinsic通过引入语义一致性和光照不变性约束，为稀疏视角下的室内场景逆渲染提供了一种高效且鲁棒的解决方案。
---

## 摘要
我们提出了 SGS-Intrinsic，这是一个适用于稀疏视角图像的室内逆渲染框架。与现有的基于 3D 高斯泼溅（3DGS）的方法不同，这些方法通常侧重于以物体为中心的重建，且在稀疏视角设置下表现不佳，而我们的方法能够实现高质量的几何重建以及材质与光照的准确解耦。其核心思想是在语义和几何先验的指导下，构建一个稠密且几何一致的高斯语义场，为后续的逆渲染提供可靠基础。在此基础上，我们通过结合混合光照模型和材质先验来进行材质-光照解耦，从而有效地捕捉光照与材质之间的相互作用。为了减轻投射阴影的影响并增强材质恢复的鲁棒性，我们引入了光照不变的材质约束以及一个去阴影模型。在基准数据集上的广泛实验表明，与现有的基于 3DGS 的逆渲染方法相比，我们的方法在重建保真度和逆渲染质量方面均有一致的提升。我们的代码可在 https://github.com/GrumpySloths/SGS_Intrinsic.github.io 获取。

## Abstract
We present SGS-Intrinsic, an indoor inverse rendering framework that works well for sparse-view images. Unlike existing 3D Gaussian Splatting (3DGS) based methods that focus on object-centric reconstruction and fail to work under sparse view settings, our method allows to achieve high-quality geometry reconstruction and accurate disentanglement of material and illumination. The core idea is to construct a dense and geometry-consistent Gaussian semantic field guided by semantic and geometric priors, providing a reliable foundation for subsequent inverse rendering. Building upon this, we perform material-illumination disentanglement by combining a hybrid illumination model and material prior to effectively capture illumination-material interactions. To mitigate the impact of cast shadows and enhance the robustness of material recovery, we introduce illumination-invariant material constraint together with a deshadowing model. Extensive experiments on benchmark datasets show that our method consistently improves both reconstruction fidelity and inverse rendering quality over existing 3DGS-based inverse rendering approaches. Our code is available at https://github.com/GrumpySloths/SGS_Intrinsic.github.io.

---

## 论文详细总结（自动生成）

以下是对论文《SGS-Intrinsic: Semantic-Invariant Gaussian Splatting for Sparse-View Indoor Inverse Rendering》的结构化深度总结：

### 1. 论文的核心问题与整体含义
该论文针对**室内场景在稀疏视角（Sparse-View）下的逆渲染（Inverse Rendering）**问题展开研究。逆渲染旨在从图像中分解出几何、材质和光照，但室内场景面临三大挑战：
*   **几何重建难**：稀疏视角下 3D 高斯泼溅（3DGS）容易过拟合，导致几何结构不可靠。
*   **光照模型复杂**：传统模型难以捕捉室内近场、高频的光照效果。
*   **材质与阴影耦合**：阴影常被错误地“烘焙”到材质（反射率）中，导致解耦不准确。
论文提出的 **SGS-Intrinsic** 框架通过引入语义一致性和光照不变性约束，实现了高质量的几何重建与精确的材质-光照解耦。

### 2. 方法论
论文采用了**两阶段训练框架**：

*   **第一阶段：几何与语义重建**
    *   **初始化**：利用 VGGT 模型构建稠密的场景布局点云，而非传统的稀疏 SfM 点云。
    *   **先验引导**：引入预训练模型（StableNormal 和 LSEG）提供的法线和语义先验。
    *   **语义一致性约束**：提出 $L_{corresponding}$ 损失，通过在训练视角和虚拟（伪）视角之间实施语义均匀化操作（Majority Voting），缓解稀疏视角下的过拟合。

*   **第二阶段：固定几何下的逆渲染**
    *   **混合光照模型**：结合环境贴图（处理环境光）和球面高斯混合模型（SGMs，处理高频点光源），增强对复杂室内光照的表达能力。
    *   **轻量化去阴影模块**：利用多分辨率哈希编码器（Hash Encoder）构建遮挡场，预测表面点与光源间的可见性（Visibility），防止阴影干扰材质恢复。
    *   **材质一致性正则化**：
        *   **光照不变性**：通过对光照进行扰动，强制要求同一视角下的渲染反射率（Albedo）保持不变（$L_{relight}$）。
        *   **跨视角语义一致性**：基于“相似语义区域具有相似材质”的假设，约束不同视角间的材质属性（$L_{cross\_semantic}$）。

### 3. 实验设计
*   **数据集**：
    *   **合成数据**：Interiorverse（场景级）、TensoIR（物体级）。
    *   **真实数据**：Mip-NeRF 360、FIPT、DL3DV（包含复杂光照环境）。
*   **基准测试（Benchmark）**：在稀疏视角设置下（如 Interiorverse 采样 12 视角，TensoIR 采样 8 视角，真实场景采样 24 视角）进行评估。
*   **对比方法**：GSIR、GIGS、R3DG、IRGS、SVGIR、GeoSplat 等最先进的基于 3DGS 的逆渲染方法。
*   **评估指标**：PSNR、SSIM、LPIPS（图像质量）；MAE（法线精度）；MSE（粗糙度/反射率精度）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090 GPU**。
*   **训练时长**：
    *   第一阶段：7000 次迭代。
    *   第二阶段：3000 次迭代。
    *   **总耗时**：约 **40 分钟**（预处理如语义/法线提取约需 3 分钟）。
*   **推理速度**：FPS 约为 15.05，在同类逆渲染方法中处于较高水平。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个不同类型的数据集上进行了测试，涵盖了从单个物体到复杂室内场景、从合成到真实的多种情况。
*   **消融实验**：非常详尽。分别验证了法线先验、语义一致性、混合光照（SGM）、去阴影模块、光照不变损失和跨视角语义损失的有效性。
*   **鲁棒性测试**：专门测试了模型对几何噪声、法线噪声、语义噪声以及相机位姿误差的容忍度。
*   **客观性**：通过定量指标和定性视觉对比（如重光照效果、材质编辑效果）全面展示了优势，实验设计严谨且公平。

### 6. 主要结论与发现
*   **语义引导至关重要**：在稀疏视角下，单纯依靠 RGB 监督无法获得正确的几何，语义一致性约束能显著提升纹理缺失区域的重建质量。
*   **显式建模遮挡可优化材质**：引入去阴影模块能有效分离投射阴影，使恢复的反射率贴图更加干净、准确。
*   **混合光照的优越性**：相比单一的环境贴图，结合 SGMs 能更好地模拟室内灯具产生的高频高光和局部照明。

### 7. 优点
*   **两阶段策略稳健**：先解几何再解材质，降低了逆渲染这一高度病态问题的优化难度。
*   **去阴影设计巧妙**：利用轻量化遮挡场解决 3DGS 在稀疏视角下难以通过光线追踪计算阴影的痛点。
*   **应用潜力广阔**：展示了在光照控制、材质编辑和虚拟物体插入等下游任务中的卓越效果。

### 8. 不足与局限
*   **依赖预训练模型**：虽然论文证明了其对噪声有一定鲁棒性，但整体性能仍受限于 VGGT、StableNormal 等前端模型生成的先验质量。
*   **计算开销**：虽然 40 分钟的训练时间在逆渲染领域不算长，但相比于原生 3DGS 的快速训练，引入多个大模型先验增加了预处理的复杂性。
*   **动态场景限制**：目前仅适用于静态室内场景，尚未讨论动态环境下的表现。

（完）
