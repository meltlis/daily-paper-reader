---
title: "SGS-Intrinsic: Semantic-Invariant Gaussian Splatting for Sparse-View Indoor Inverse Rendering"
title_zh: SGS-Intrinsic：面向稀疏视角室内逆渲染的语义不变高斯泼溅
authors: "Jiahao Niu, Rongjia Zheng, Wenju Xu, WeiShi Zheng, Qing Zhang"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27516v1"
tags: ["query:3dgsir"]
score: 10.0
evidence: 使用3DGS实现材质与光照的准确解耦
tldr: SGS-Intrinsic 针对室内场景在稀疏视角下的逆渲染难题，提出了一种基于 3D 高斯泼溅（3DGS）的新框架。该方法通过引入语义和几何先验构建一致的语义场，并结合混合光照模型与材质先验实现解耦。此外，利用光照不变材质约束和去阴影模型增强了材质恢复的鲁棒性，在稀疏视角下实现了高质量的几何重建和材质光照解耦。
selection_source: fresh_fetch
motivation: 现有的 3DGS 逆渲染方法多侧重于以物体为中心的重建，在稀疏视角的室内场景中难以实现高质量的几何恢复和材质解耦。
method: 通过语义和几何先验构建稠密一致的语义场，并结合混合光照模型、材质先验及去阴影模型进行材质与光照的精确解耦。
result: 在基准数据集上的实验表明，该方法在重建保真度和逆渲染质量上均显著优于现有的 3DGS 逆渲染技术。
conclusion: SGS-Intrinsic 有效解决了稀疏视角室内逆渲染中的几何不一致和阴影干扰问题，为复杂场景的材质恢复提供了可靠方案。
---

## 摘要
我们提出了 SGS-Intrinsic，这是一个适用于稀疏视角图像的室内逆渲染框架。与现有的基于 3D 高斯泼溅 (3DGS) 的方法不同，这些方法通常侧重于以物体为中心的重建，且在稀疏视角设置下表现不佳，而我们的方法能够实现高质量的几何重建以及材质和光照的准确解耦。其核心思想是在语义和几何先验的指导下，构建一个稠密且几何一致的高斯语义场，为后续的逆渲染提供可靠基础。在此基础上，我们通过结合混合光照模型和材质先验来进行材质-光照解耦，从而有效地捕捉光照与材质之间的相互作用。为了减轻投射阴影的影响并增强材质恢复的鲁棒性，我们引入了光照不变的材质约束以及一个去阴影模型。在基准数据集上的广泛实验表明，与现有的基于 3DGS 的逆渲染方法相比，我们的方法在重建保真度和逆渲染质量方面均有一致的提升。我们的代码可在 https://github.com/GrumpySloths/SGS_Intrinsic.github.io 获取。

## Abstract
We present SGS-Intrinsic, an indoor inverse rendering framework that works well for sparse-view images. Unlike existing 3D Gaussian Splatting (3DGS) based methods that focus on object-centric reconstruction and fail to work under sparse view settings, our method allows to achieve high-quality geometry reconstruction and accurate disentanglement of material and illumination. The core idea is to construct a dense and geometry-consistent Gaussian semantic field guided by semantic and geometric priors, providing a reliable foundation for subsequent inverse rendering. Building upon this, we perform material-illumination disentanglement by combining a hybrid illumination model and material prior to effectively capture illumination-material interactions. To mitigate the impact of cast shadows and enhance the robustness of material recovery, we introduce illumination-invariant material constraint together with a deshadowing model. Extensive experiments on benchmark datasets show that our method consistently improves both reconstruction fidelity and inverse rendering quality over existing 3DGS-based inverse rendering approaches. Our code is available at https://github.com/GrumpySloths/SGS_Intrinsic.github.io.

---

## 论文详细总结（自动生成）

以下是对论文《SGS-Intrinsic: Semantic-Invariant Gaussian Splatting for Sparse-View Indoor Inverse Rendering》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文针对**室内场景在稀疏视角（Sparse-View）下的逆渲染（Inverse Rendering）**难题提出了 SGS-Intrinsic 框架。
*   **研究动机**：逆渲染旨在从图像中分解出几何、材质和光照。现有的基于 3D 高斯泼溅（3DGS）的方法在处理室内场景时面临三大挑战：
    1.  **几何重建不稳定**：稀疏视角下 3DGS 容易过拟合，导致几何结构不可靠。
    2.  **光照模型局限**：传统模型多假设远场光照，难以捕捉室内复杂的近场和高频光照。
    3.  **材质与光照耦合**：阴影和高光常被错误地“烘焙”到材质（反照率）中，尤其是在缺乏监督信号的稀疏视角下。

### 2. 方法论
论文提出了一个**两阶段训练框架**，核心在于构建一致的语义场并结合物理启发的光照模型。

*   **第一阶段：几何与语义重建**
    *   **初始化**：利用 VGGT 模型生成稠密点云，而非依赖稀疏的 SfM 点云。
    *   **先验引导**：引入预训练模型（StableNormal 和 LSEG）提供法线和语义监督。
    *   **语义一致性约束**：提出在训练视角与伪视角（Pseudo-views）之间建立语义一致性损失（$L_{corresponding}$），通过大数投票机制确保语义场的稳定性，防止过拟合。

*   **第二阶段：固定几何下的逆渲染**
    *   **混合光照模型**：结合环境贴图（处理低频环境光）和球形高斯混合模型（SGMs，处理高频点光源）。
    *   **轻量化去阴影模块**：使用多分辨率哈希编码器（Hash Encoder）和 MLP 预测空间点与光源间的可见性（Visibility），显式建模阴影，防止其污染反照率。
    *   **材质不变性约束**：引入光照扰动下的反照率不变性损失（$L_{relight}$）和跨视角语义材质一致性损失（$L_{cross\_semantic}$），利用“相同语义区域应具有相似材质”的先验进行正则化。

### 3. 实验设计
*   **数据集**：
    *   合成数据集：**Interiorverse**（场景级）、**TensoIR**（物体级）。
    *   真实数据集：**Mip-NeRF 360**、**FIPT**、**DL3DV**。
*   **基准测试（Benchmark）**：在稀疏视角设置下（如 Interiorverse 采样 12 视角，TensoIR 采样 8 视角）进行评估。
*   **对比方法**：对比了 GSIR、GIGS、R3DG、IRGS、SVGIR 和 GeoSplat 等最先进的基于 3DGS 的逆渲染方法。
*   **评价指标**：PSNR、SSIM、LPIPS（图像质量）；MAE（法线）；MSE（粗糙度、反照率）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090 GPU**。
*   **训练时长**：总计约 **40 分钟**。
    *   第一阶段：7000 次迭代。
    *   第二阶段：3000 次迭代。
*   **预处理**：包括 VGGT、LSeg、StableNormal 等前序处理，累计耗时约 3 分钟。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个不同类型的数据集上进行了测试，涵盖了从物体到复杂室内场景、从合成到真实的多种情况。
*   **消融实验**：针对法线先验、语义一致性、SGM 光源、去阴影模块、材质不变性损失等关键组件均做了详尽的消融分析。
*   **鲁棒性测试**：专门测试了模型对几何噪声、法线/语义噪声以及相机位姿误差的容忍度。
*   **评价**：实验设计全面，对比基准线清晰，通过定量指标和定性视觉对比（如重光照、物体插入应用）验证了方法的有效性，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **语义引导的有效性**：引入语义一致性不仅提升了语义分割质量，还显著辅助了稀疏视角下的几何对齐。
*   **去阴影的重要性**：显式建模可见性场能有效解决室内逆渲染中常见的“阴影烘焙”问题，使反照率（Albedo）预测更干净。
*   **混合光照的优越性**：SGMs 与环境贴图的结合能够比单一模型更真实地还原室内复杂的光影交互。
*   **性能领先**：在所有测试指标上，SGS-Intrinsic 均显著优于现有的 3DGS 逆渲染方法，尤其在 PBR 新视角合成方面表现突出。

### 7. 优点（亮点）
*   **解耦彻底**：通过去阴影模块和材质不变性约束，实现了比以往方法更彻底的材质与光照解耦。
*   **稀疏视角鲁棒性**：成功将 3DGS 逆渲染的应用范围从密集视角扩展到了极具挑战性的稀疏室内场景。
*   **两阶段策略**：先稳固几何再优化材质的策略，避免了多参数同时优化时的病态竞争。
*   **应用潜力**：展示了高质量的材质编辑、物体插入和光照控制效果，具有很强的实用价值。

### 8. 不足与局限
*   **依赖预训练先验**：方法的性能在一定程度上受限于 StableNormal、LSeg 等预训练模型的质量，若先验模型在某些极端场景失效，可能影响最终效果。
*   **计算开销**：虽然 40 分钟的训练时间在逆渲染领域算快，但相比于原生 3DGS 的实时性，其预处理和两阶段训练仍有优化空间。
*   **间接光照简化**：虽然引入了去阴影和混合光照，但对于极其复杂的多次反弹间接光照（Indirect Illumination），模型仍采用了一定的近似处理，可能在极高复杂度场景下存在保真度损失。

（完）
