---
title: "PointSplat: Efficient Geometry-Driven Pruning and Transformer Refinement for 3D Gaussian Splatting"
title_zh: PointSplat：针对 3D 高斯泼溅的高效几何驱动剪枝与 Transformer 细化
authors: "Anh Thuan Tran, Jana Kosecka"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09903v1"
tags: ["query:3dgsir"]
score: 8.0
evidence: 3D高斯泼溅的几何驱动剪枝与精细化
tldr: 本文提出PointSplat，一个结合几何驱动剪枝与Transformer细化的3D高斯泼溅（3DGS）框架。针对3DGS存储开销大的问题，该方法通过仅基于3D几何属性的剪枝策略摆脱了对2D图像的依赖，并利用双分支编码器平衡几何与外观特征。在无需逐场景优化的前提下，PointSplat在多个数据集上实现了高效的场景压缩与高质量的实时渲染。
selection_source: fresh_fetch
motivation: 现有的3DGS剪枝方法通常依赖2D图像计算重要性且需要繁琐的逐场景微调，限制了处理效率和通用性。
method: 引入一种仅基于3D属性的几何驱动剪枝策略，并配合双分支Transformer编码器对几何与外观特征进行解耦重加权细化。
result: 在ScanNet++和Replica数据集上的实验证明，该方法在不同稀疏度下均能保持竞争力的渲染质量，且显著提升了处理效率。
conclusion: PointSplat证明了通过纯几何驱动的剪枝与特征细化，可以在不进行逐场景优化的情况下实现高效且紧凑的3D场景表示。
---

## 摘要
3D 高斯泼溅 (3DGS) 最近通过使用显式 3D 基元表示场景，实现了实时、高保真的新视角合成。然而，传统方法通常需要数百万个高斯函数来捕捉复杂场景，导致了巨大的内存和存储需求。最近的方法通过剪枝和对高斯参数进行逐场景微调来解决这一问题，从而在保持视觉质量的同时减小了模型尺寸。这些策略通常依赖 2D 图像来计算重要性得分，随后进行特定场景的优化。在这项工作中，我们提出了 PointSplat，这是一个 3D 几何驱动的剪枝与细化框架，它桥接了此前互不相关的两个研究方向：高斯剪枝与 Transformer 细化。我们的方法包含两个关键组件：(1) 一种高效的几何驱动策略，仅根据高斯函数的 3D 属性对其进行排序，从而在剪枝阶段消除了对 2D 图像的依赖；(2) 一个双分支编码器，将几何与外观特征分离并重新加权，以避免特征不平衡。在 ScanNet++ 和 Replica 数据集上针对不同稀疏度水平进行的广泛实验表明，PointSplat 在无需额外逐场景优化的情况下，始终能实现具有竞争力的渲染质量和卓越的效率。

## Abstract
3D Gaussian Splatting (3DGS) has recently unlocked real-time, high-fidelity novel view synthesis by representing scenes using explicit 3D primitives. However, traditional methods often require millions of Gaussians to capture complex scenes, leading to significant memory and storage demands. Recent approaches have addressed this issue through pruning and per-scene fine-tuning of Gaussian parameters, thereby reducing the model size while maintaining visual quality. These strategies typically rely on 2D images to compute important scores followed by scene-specific optimization. In this work, we introduce PointSplat, 3D geometry-driven prune-and-refine framework that bridges previously disjoint directions of gaussian pruning and transformer refinement. Our method includes two key components: (1) an efficient geometry-driven strategy that ranks Gaussians based solely on their 3D attributes, removing reliance on 2D images during pruning stage, and (2) a dual-branch encoder that separates, re-weights geometric and appearance to avoid feature imbalance. Extensive experiments on ScanNet++ and Replica across varying sparsity levels demonstrate that PointSplat consistently achieves competitive rendering quality and superior efficiency without additional per-scene optimization.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PointSplat** 的新框架，旨在解决 3D 高斯泼溅（3DGS）在存储和计算上的冗余问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 3DGS 为了捕捉复杂场景通常需要数百万个高斯基元，导致巨大的内存占用和存储需求。
*   **研究动机**：
    *   **现有剪枝方法的局限**：目前的剪枝策略（如 LightGaussian, PUP-3DGS）通常依赖 2D 图像来计算重要性得分，涉及复杂的射线-像素相交计算，且剪枝后通常需要繁琐的“逐场景（per-scene）”微调。
    *   **特征不平衡问题**：现有的 Transformer 细化方法（如 SplatFormer）在处理高斯参数时，高维的外观特征（球谐系数）往往会掩盖低维的几何特征，导致学习效果不佳。
*   **整体含义**：PointSplat 试图通过“纯几何驱动”的剪枝和“双分支”Transformer 细化，实现无需逐场景优化的、高效且紧凑的 3D 场景表示。

### 2. 方法论
PointSplat 包含两个核心组件：
*   **几何驱动剪枝得分（Geometry-Driven Pruning Score）**：
    *   **核心思想**：完全摆脱对 2D 图像的依赖，仅利用高斯函数的 3D 固有属性进行排序。
    *   **计算方式**：得分由高斯函数的**不透明度（Opacity）**和**体积（Volume）**的加权 z-score 归一化值组成。公式平衡了空间覆盖范围和视觉贡献度，优先保留体积适中且不透明度高的关键基元。
*   **双分支编码器（Dual-Branch Encoder）**：
    *   **核心思想**：将几何属性（位置、缩放、旋转）与高维外观属性（球谐系数、不透明度）解耦处理。
    *   **技术细节**：
        1.  **几何分支**：处理 3D 坐标、四元数和缩放向量。
        2.  **外观分支**：通过 MLP 压缩高维球谐系数，避免其主导特征空间。
        3.  **位置编码与融合**：引入位置编码（Positional Encoding）来强化空间上下文，通过注意力机制将外观特征作为权重作用于几何特征。
*   **Transformer 细化**：采用 **Point Transformer V3** 架构，利用局部注意力机制在全局范围内协调高斯参数的更新，修复剪枝带来的结构损伤（如模糊或边缘破碎）。

### 3. 实验设计
*   **数据集**：
    *   **ScanNet++**：高保真室内场景数据集（选取 5 个测试场景）。
    *   **Replica**：室内环境数据集（选取 3 个测试场景）。
*   **Benchmark 与对比方法**：
    *   **基准**：原始 Dense 3DGS。
    *   **场景特定/图像驱动方法**：LightGaussian, PUP-3DGS（包含剪枝后微调与不微调的对比）。
    *   **几何驱动方法**：SplatFormer。
*   **评估指标**：PSNR, SSIM, LPIPS（渲染质量）；FPS, 存储大小（效率）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 (80GB)** GPU。
*   **训练细节**：
    *   使用 Adam 优化器训练 10,000 次迭代。
    *   初始学习率为 1e-5，在 6,000 次迭代时降低。
    *   Point Transformer 采用 5 阶段分层架构。
*   **耗时**：文中未明确给出总训练时长，但强调了推理阶段的实时性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 **50%、30%、10%** 三种不同的高斯保留率（稀疏度）进行了详尽对比。
    *   进行了**消融实验**，验证了剪枝权重 $\lambda_\alpha$ 的敏感度以及双分支编码器中各组件（几何、外观、位置编码）的有效性。
*   **充分性评价**：实验设计较为充分，涵盖了主流的室内数据集，并与当前最先进的（SOTA）剪枝和 Transformer 方法进行了直接对比。通过可视化高斯分布（体积与不透明度分布图），客观地展示了该方法与图像驱动方法的本质区别。

### 6. 主要结论与发现
*   **性能优异**：在无需逐场景优化的前提下，PointSplat 在 50% 稀疏度下的表现接近原始 Dense 3DGS，且在极高稀疏度（10%）下显著优于其他非微调方法。
*   **效率提升**：由于 PointSplat 倾向于选择体积小、不透明度高的高斯，减少了像素重叠（Overdraw），其渲染速度（FPS）显著高于 LightGaussian 和 PUP-3DGS。例如在 50% 剪枝下，FPS 从 200+ 提升至 390+。
*   **结构修复**：Transformer 细化能有效恢复剪枝造成的纹理模糊和几何边缘断裂。

### 7. 优点
*   **高效剪枝**：无需计算射线-像素相交，剪枝过程极快且不依赖测试视角图像。
*   **泛化能力**：通过 Transformer 学习通用的结构特征，实现了“即插即用”的细化，无需针对每个新场景重新训练。
*   **特征平衡**：双分支设计巧妙解决了 3DGS 参数维度极度不平衡导致的训练不稳定问题。

### 8. 不足与局限
*   **场景局限性**：目前主要针对结构相似的**室内环境**。在无边界（Unbounded）的复杂室外场景中，该方法的泛化能力尚未得到验证。
*   **训练依赖**：虽然部署时不需要图像，但 Transformer 模型的**训练阶段**仍需要少量图像（文中为 2 张）来确保外观一致性。
*   **感知质量权衡**：在极高压缩比下，虽然 PSNR 和 SSIM 较高，但 LPIPS（感知损失）有时略逊于某些经过特定微调的方法。

（完）
