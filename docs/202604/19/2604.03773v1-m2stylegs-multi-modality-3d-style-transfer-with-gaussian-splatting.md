---
title: "M2StyleGS: Multi-Modality 3D Style Transfer with Gaussian Splatting"
title_zh: M2StyleGS：基于高斯泼溅的多模态 3D 风格迁移
authors: "Xingyu Miao, Xueqi Qiu, Haoran Duan, Yawen Huang, Xian Wu, Jingjing Deng, Yang Long"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03773v1"
tags: ["query:3dgsir"]
score: 8.0
evidence: 利用3D高斯泼溅作为风格迁移的3D表示
tldr: "针对传统3D风格迁移仅支持单一参考图像的局限，本文提出M2StyleGS，一种基于3D高斯泼溅（3DGS）的多模态实时风格迁移技术。该方法利用CLIP提取文本或图像的风格特征，通过细分流（subdivisive flow）实现精确的特征对齐，并引入观察损失和抑制损失来优化风格匹配与颜色稳定性。实验证明，该方法在视觉质量上表现优异，且在视图一致性上比前人工作提升了32.92%。"
selection_source: carryover_cache
motivation: 现有的3D风格迁移方法受限于固定参考图像，无法满足虚拟现实等应用中对文本描述或多样化输入的需求。
method: 结合3DGS表示与CLIP多模态知识，利用细分流强化特征投影，并配合观察损失与抑制损失提升风格化效果。
result: "实验结果显示M2StyleGS在视觉质量上更具优势，且在视图一致性指标上显著超越前人工作达32.92%。"
conclusion: M2StyleGS实现了支持文本和图像参考的高质量、高一致性实时3D场景风格迁移。
---

## 摘要
传统的 3D 风格迁移方法依赖于固定的参考图像将艺术纹理应用到 3D 场景中。然而，在虚拟现实或增强现实等实际应用中，用户通常更倾向于更灵活的输入方式，包括文本描述和多样化的图像。在这项工作中，我们提出了一种名为 M2StyleGS 的新型实时风格化技术，用于生成一系列精确颜色映射的视图。该技术利用 3D 高斯泼溅（3DGS）作为 3D 表示，并使用经 CLIP 提炼的多模态知识作为参考风格。M2StyleGS 通过采用一种精确的特征对齐方法（即细分流，subdivisive flow）解决了异常变换问题，增强了映射后的 CLIP 文本-视觉组合特征向 VGG 风格特征的投影。此外，我们引入了观测损失（observation loss）以辅助风格化场景在生成过程中更好地匹配参考风格，以及抑制损失（suppression loss）以在整个解码过程中抑制参考颜色信息的偏移。通过整合这些方法，M2StyleGS 可以利用文本或图像作为参考，生成一组风格增强的新视角视图。实验表明，M2StyleGS 实现了更好的视觉质量，并在一致性方面比以往工作提升了高达 32.92%。

## Abstract
Conventional 3D style transfer methods rely on a fixed reference image to apply artistic patterns to 3D scenes. However, in practical applications such as virtual or augmented reality, users often prefer more flexible inputs, including textual descriptions and diverse imagery. In this work, we introduce a novel real-time styling technique M2StyleGS to generate a sequence of precisely color-mapped views. It utilizes 3D Gaussian Splatting (3DGS) as a 3D presentation and multi-modality knowledge refined by CLIP as a reference style. M2StyleGS resolves the abnormal transformation issue by employing a precise feature alignment, namely subdivisive flow, it strengthens the projection of the mapped CLIP text-visual combination feature to the VGG style feature. In addition, we introduce observation loss, which assists in the stylized scene better matching the reference style during the generation, and suppression loss, which suppresses the offset of reference color information throughout the decoding process. By integrating these approaches, M2StyleGS can employ text or images as references to generate a set of style-enhanced novel views. Our experiments show that M2StyleGS achieves better visual quality and surpasses the previous work by up to 32.92% in terms of consistency.

---

## 论文详细总结（自动生成）

这是一份关于论文《M2StyleGS: Multi-Modality 3D Style Transfer with Gaussian Splatting》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的 3D 风格迁移方法通常局限于使用单一的**参考图像**来提取艺术风格。然而，在 VR/AR 等实际应用中，用户往往希望通过**文本描述**或多样化的图像来定义风格。
*   **研究动机**：现有的多模态方法（如 ConRF）在将 CLIP 特征空间映射到 VGG 风格空间时，由于特征分布不一致，容易导致颜色异常、纹理模糊以及图文风格不匹配。
*   **核心目标**：利用 3D 高斯泼溅（3DGS）的实时渲染优势，结合 CLIP 的多模态知识，实现高质量、高一致性的 3D 场景风格迁移。

### 2. 方法论
M2StyleGS 提出了一套显式风格迁移框架，核心技术包括：
*   **3DGS 骨干网络**：以 3D 高斯点云作为场景表示，固定几何参数（位置、形状、不透明度），仅对颜色属性进行风格化处理。
*   **细分流（Subdivisive Flow）特征对齐**：这是本文的核心创新。作者认为简单的 MSE 损失无法处理 CLIP 与 VGG 之间复杂的特征分布差异。该方法引入**常微分方程（ODE）**，通过多个细分步骤（Euler 方法）将 CLIP 特征流向 VGG 风格空间，实现更精确的域对齐。
*   **AdaIN 迁移模块**：利用对齐后的特征，通过自适应实例归一化（AdaIN）调整 3D 高斯的颜色嵌入。
*   **增强型辅助损失**：
    *   **观察损失（Observation Loss）**：引入预训练 2D 风格迁移模型作为“观察先验”，约束 3D 渲染视图与 2D 艺术作品在语义层面上保持一致。
    *   **抑制损失（Suppression Loss）**：利用多尺度判别器，在解码过程中抑制颜色偏移，确保全局颜色分布的稳定性。

### 3. 实验设计
*   **数据集/场景**：使用了经典的 **LLFF** 数据集和 **Tanks & Temples** 数据集。
*   **Benchmark（基准指标）**：主要评估**多视图一致性**，包括短程（相邻视图）和长程（远距离视图）的 **LPIPS**（感知相似度）和 **RMSE**（均方根误差）。
*   **对比方法**：
    *   图像引导方法：StyleRF, StyleGaussian, ConRF。
    *   文本引导方法：CLIPStyler (2D), CLIPNeRF, ConRF。

### 4. 资源与算力
*   **文中说明**：论文强调了 3DGS 的“实时渲染”和“即时（Instant）”特性。
*   **缺失信息**：论文正文部分**未明确列出**具体的 GPU 型号、数量、具体的训练时长或推理延迟数值（通常此类信息会出现在附录中，但根据提取文本，正文未提及）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多场景（如 Fern, Trex 等）的定性视觉对比。
    *   针对 LLFF 数据集进行了详尽的定量一致性测试。
    *   **消融实验**：分别验证了“细分流”的轮数（Round）对特征相似度（SIM）和 FID 的影响，以及观察损失和抑制损失对最终效果的贡献。
*   **评价**：实验设计较为全面，涵盖了从 2D 到 3D、从图像到文本的多种对比维度，消融实验逻辑清晰，能够客观证明各模块的有效性。

### 6. 主要结论与发现
*   **性能领先**：M2StyleGS 在一致性指标上显著超越前人工作，短程 LPIPS 相比 ConRF 和 StyleRF 分别降低了 32.92% 和 25.68%。
*   **对齐效果**：通过 ODE 细分流，CLIP 特征与 VGG 风格特征的余弦相似度显著提升，有效解决了颜色异常问题。
*   **多模态灵活性**：该方法能够同时处理文本提示（如“梵高的星空”）和参考图像，且生成的 3D 场景在不同视角下保持了极高的视觉稳定性。

### 7. 优点
*   **特征对齐创新**：引入 ODE 细分流解决了多模态特征空间映射不精确的痛点。
*   **高一致性**：通过 3DGS 显式表示和辅助损失，克服了 3D 风格迁移中常见的闪烁和纹理漂移问题。
*   **实时性潜力**：基于 3DGS 架构，相比基于 NeRF 的方法，在渲染速度上具有天然优势。

### 8. 不足与局限
*   **文本引导的挑战**：虽然优于前人，但实验数据显示文本引导的一致性指标仍略逊于图像引导，说明文本到 3D 风格的精确映射仍有提升空间。
*   **依赖 2D 先验**：观察损失依赖于预训练的 2D 风格迁移模型，如果 2D 模型本身存在伪影，可能会传导至 3D 场景。
*   **计算开销**：虽然渲染快，但引入 ODE 细分流和多尺度判别器可能会增加训练阶段的复杂度和内存占用。

（完）
