---
title: "VIRGi: View-dependent Instant Recoloring of 3D Gaussians Splats"
title_zh: VIRGi：3D 高斯泼溅的视点相关即时重着色
authors: "Alessio Mazzucchelli, Ivan Ojeda-Martin, Fernando Rivas-Manzaneque, Elena Garces, Adrian Penate-Sanchez, Francesc Moreno-Noguer"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02986v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 在3DGS中将颜色分解为漫反射和视点相关分量
tldr: 3D Gaussian Splatting (3DGS) 在场景建模和渲染上表现优异，但外观编辑仍具挑战。本文提出 VIRGi，一种能快速重着色 3DGS 场景并保留视点相关效果（如高光）的方法。通过分离漫反射与视点相关颜色，并结合多视图训练策略，用户仅需编辑一张图像，即可在两秒内将颜色修改传播至整个场景，实现了高效且保真的交互式编辑。
selection_source: fresh_fetch
motivation: 现有的 3DGS 方法缺乏一种既高效又能保持真实视点相关效果（如镜面反射）的场景外观编辑手段。
method: 提出一种将颜色分离为漫反射和视点相关组件的新架构，并利用单张编辑图像通过微调 MLP 实现快速颜色传播。
result: 该方法仅需两秒即可完成全场景重着色，且在定量和定性评估上均显著优于基于 NeRF 的竞争对手。
conclusion: VIRGi 为 3DGS 场景提供了实时、可控且保真的重着色方案，极大地提升了 3D 场景编辑的效率和交互性。
---

## 摘要
3D 高斯泼溅 (3DGS) 近期因其能够精确建模复杂 3D 场景以及前所未有的渲染性能，彻底改变了新视角合成和 3D 重建领域。然而，一个重大挑战依然存在：缺乏一种高效且具有照片级真实感的方法来编辑场景内容的表观。在本文中，我们提出了 VIRGi，这是一种用于快速编辑 3DGS 建模场景颜色，同时保留镜面高光等视点相关效应的新方法。我们方法的关键在于一种将颜色分解为漫反射和视点相关组件的新型架构，以及一种整合了来自多个视点的图像块的多视图训练策略。相比于传统的单视图批次训练，我们的 3DGS 表示提供了更准确的重建，并为重着色任务提供了坚实的表示基础。针对 3DGS 重着色，我们随后引入了一种快速方案，仅需终端用户提供一张手动编辑过的场景图像。通过微调单个 MLP 的权重，并结合一个用于可编辑区域单样本分割的模块，颜色编辑可在仅两秒内无缝传播到整个场景，从而促进了实时交互，并提供了对视点相关效应强度的控制。在多样化数据集上的详尽验证表明，与基于神经辐射场 (NeRF) 表示的竞争对手相比，该方法在定量和定性方面均取得了显著进步。

## Abstract
3D Gaussian Splatting (3DGS) has recently transformed the fields of novel view synthesis and 3D reconstruction due to its ability to accurately model complex 3D scenes and its unprecedented rendering performance. However, a significant challenge persists: the absence of an efficient and photorealistic method for editing the appearance of the scene's content. In this paper we introduce VIRGi, a novel approach for rapidly editing the color of scenes modeled by 3DGS while preserving view-dependent effects such as specular highlights. Key to our method are a novel architecture that separates color into diffuse and view-dependent components, and a multi-view training strategy that integrates image patches from multiple viewpoints. Improving over the conventional single-view batch training, our 3DGS representation provides more accurate reconstruction and serves as a solid representation for the recoloring task. For 3DGS recoloring, we then introduce a rapid scheme requiring only one manually edited image of the scene from the end-user. By fine-tuning the weights of a single MLP, alongside a module for single-shot segmentation of the editable area, the color edits are seamlessly propagated to the entire scene in just two seconds, facilitating real-time interaction and providing control over the strength of the view-dependent effects. An exhaustive validation on diverse datasets demonstrates significant quantitative and qualitative advancements over competitors based on Neural Radiance Fields representations.

---

## 论文详细总结（自动生成）

以下是对论文《VIRGi: View-dependent Instant Recoloring of 3D Gaussians Splats》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在保持 3D 高斯泼溅（3DGS）高渲染性能的同时，实现**快速、照片级真实感且具有视点相关性（如镜面高光）**的场景重着色。
*   **研究背景**：3DGS 在重建和渲染效率上优于 NeRF，但在编辑（尤其是重着色）方面研究较少。现有的 3DGS 编辑方法（如基于文本的编辑）往往速度慢、容易产生颜色伪影，且难以处理复杂的视点相关光影效果。
*   **整体含义**：VIRGi 旨在提供一个交互式工具，让用户只需修改一张 2D 图像，即可在几秒钟内将颜色更改一致地传播到整个 3D 场景，并保留或调整原有的光影质感。

### 2. 方法论
*   **核心思想**：通过**漫反射/镜面反射解耦架构**和**多视图训练策略**，构建一个易于编辑的 3DGS 表示。
*   **关键技术细节**：
    *   **颜色解耦架构**：将颜色分解为漫反射分量 $C_{diff}$（仅依赖空间特征 $f$）和镜面反射分量 $C_{spec}$（依赖特征 $f$ 和视点方向 $\theta$）。使用两个独立的 MLP 分别建模，并通过残差连接增强镜面反射分支对场景内容的理解。
    *   **多视图训练策略**：修改了 CUDA 光栅化器，允许在同一个训练批次（Batch）中包含来自不同视点的图像块（Tiles）。这强制模型学习跨视图的一致性，从而更有效地分离出不随视角变化的漫反射颜色。
    *   **快速编辑流程**：
        1.  用户编辑单张视图。
        2.  **微调（Fine-tuning）**：仅优化漫反射 MLP 的最后一层权重。
        3.  **软分割（Soft Segmentation）**：使用一个小型 MLP 生成编辑区域的掩码 $\alpha$，将新颜色与原颜色进行平滑混合。
        4.  **传播**：利用 3DGS 的显式表示，将修改瞬间应用到所有视角。

### 3. 实验设计
*   **数据集/场景**：
    *   **Mip-NeRF 360**：复杂、无边界的真实 360 度场景。
    *   **LLFF**：前向捕获的真实场景。
    *   **NeRF Synthetic**：具有复杂反光和几何形状的合成场景。
*   **Benchmark 与对比方法**：
    *   **对比 NeRF 类方法**：PaletteNeRF、RecolorNeRF、IReNe（目前 NeRF 重着色的 SOTA）。
    *   **对比 3DGS 类方法**：GaussianEditor（基于文本指令的编辑）。
    *   **评估指标**：PSNR、SSIM、LPIPS。

### 4. 资源与算力
*   **编辑耗时**：论文明确指出，颜色传播过程仅需 **2 秒** 左右，达到了交互级速度。
*   **算力细节**：文中提到了对 CUDA 光栅化器的底层修改以支持多视图批处理，并提及训练批次大小受限于 **GPU VRAM**。虽然未明确给出具体的 GPU 型号（如 RTX 4090 或 A100），但强调了其方法在显存占用和处理速度上具有显著优势。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流数据集上进行了详尽的定量对比。
    *   **消融实验**：验证了“解耦架构（DC）”、“多视图训练（MV）”和“分割网络输入优化（DS）”三个核心组件的有效性。
    *   **鲁棒性测试**：针对同一场景，分别使用 11 张不同的编辑图像作为输入进行测试，证明了结果的稳定性。
*   **充分性评价**：实验设计较为全面，不仅对比了同类任务的 SOTA，还通过消融实验证明了每个技术改进的必要性。对比实验涵盖了合成与真实、前向与环绕等多种场景，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：VIRGi 在重着色质量（PSNR 等指标）上全面超越了基于 NeRF 的方法。
*   **多视图训练的重要性**：证明了在 3DGS 训练中引入多视图批处理不仅有助于颜色解耦，还能提升原始场景的重建精度。
*   **交互性**：实现了从 2D 编辑到 3D 场景的近乎实时的传播（2 秒 vs NeRF 方法的数分钟或 GaussianEditor 的 10 分钟）。
*   **可控性**：通过解耦架构，用户甚至可以独立增强或减弱场景的镜面高光强度。

### 7. 优点
*   **极速响应**：2 秒的编辑传播速度是该领域的重要突破。
*   **高质量解耦**：成功解决了 3DGS 在重着色时容易丢失高光或产生颜色漂移的问题。
*   **易用性**：仅需单张图像编辑，降低了用户操作门槛。
*   **底层优化**：通过修改 CUDA 算子提升了 3DGS 的训练一致性。

### 8. 不足与局限
*   **材质模型简化**：采用的漫反射/镜面反射模型较为简单，难以处理透明物体、各向异性材质或具有复杂彩色反射的表面。
*   **分割风险**：如果用户编辑的单张视图中未包含某些特定区域的信息，或者场景极其复杂，单视图分割可能会失效（虽然可以通过增加第二张编辑图解决）。
*   **极端反射处理**：在处理类似镜面的强反射表面（如 Counter 场景）时，解耦效果会下降，可能导致颜色溢出到非目标物体。

（完）
