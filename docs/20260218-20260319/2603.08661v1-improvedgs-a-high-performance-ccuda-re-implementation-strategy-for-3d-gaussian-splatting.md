---
title: "ImprovedGS+: A High-Performance C++/CUDA Re-Implementation Strategy for 3D Gaussian Splatting"
title_zh: ImprovedGS+：一种针对 3D 高斯泼溅的高性能 C++/CUDA 重新实现策略
authors: Jordi Muñoz Vicente
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08661v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 3D高斯泼溅的高性能C++/CUDA实现
tldr: 本研究提出ImprovedGS+，一种针对3D高斯泼溅（3DGS）的高性能C++/CUDA原生重构方案。针对现有方法在Python逻辑下的同步延迟问题，该方案通过硬件优化的内核实现了显著的训练加速。核心贡献包括长轴分割（LAS）内核、基于拉普拉斯算子的重要性评估及自适应缩放调度器。实验表明，ImprovedGS+在保持更高视觉质量的同时，大幅减少了训练时间和参数量，为场景重建建立了新的性能基准。
selection_source: fresh_fetch
motivation: 旨在解决3DGS在重建精度与计算效率之间的平衡问题，通过消除Python层逻辑来降低主机与设备间的同步延迟。
method: 采用C++/CUDA原生重构，引入了长轴分割内核、带非极大值抑制的拉普拉斯重要性内核以及自适应指数缩放调度器。
result: "在Mip-NeRF360数据集上，该方法比MCMC基准缩短了26.8%的训练时间，且在减少13.3%高斯数量的情况下实现了更高的PSNR。"
conclusion: ImprovedGS+证明了通过底层硬件优化和改进的启发式策略，可以在速度、质量和参数效率上同时实现显著提升。
---

## 摘要
近期 3D 高斯泼溅（3DGS）的进展已将重点转向平衡重建保真度与计算效率。在这项工作中，我们提出了 ImprovedGS+，这是对 ImprovedGS 策略的一种高性能、底层重新设计，并在 LichtFeld-Studio 框架内原生实现。通过从高级 Python 逻辑转向硬件优化的 C++/CUDA 内核，我们显著减少了主机与设备间的同步开销和训练延迟。我们的实现引入了长轴分割（LAS）CUDA 内核、带有非极大值抑制（NMS）边缘评分的自定义拉普拉斯重要性内核，以及自适应指数缩放调度器。在 Mip-NeRF360 数据集上的实验结果表明，ImprovedGS+ 为场景重建建立了新的帕累托最优前沿。我们的 1M 预算变体优于最先进的 MCMC 基准，训练时间减少了 26.8%（每场训练节省 17 分钟），且在保持卓越视觉质量的同时减少了 13.3% 的高斯点使用量。此外，我们的完整变体相比 ADC 基准在 PSNR 上提升了 1.28 dB，同时参数复杂度降低了 38.4%。这些结果验证了 ImprovedGS+ 是一种可扩展、高速的解决方案，维护了 LichtFeld-Studio 生态系统中速度、质量和易用性这三大核心支柱。

## Abstract
Recent advancements in 3D Gaussian Splatting (3DGS) have shifted the focus toward balancing reconstruction fidelity with computational efficiency. In this work, we propose ImprovedGS+, a high-performance, low-level reinvention of the ImprovedGS strategy, implemented natively within the LichtFeld-Studio framework. By transitioning from high-level Python logic to hardware-optimized C++/CUDA kernels, we achieve a significant reduction in host-device synchronization and training latency. Our implementation introduces a Long-Axis-Split (LAS) CUDA kernel, custom Laplacian-based importance kernels with Non-Maximum Suppression (NMS) for edge scores, and an adaptive Exponential Scale Scheduler. Experimental results on the Mip-NeRF360 dataset demonstrate that ImprovedGS+ establishes a new Pareto-optimal front for scene reconstruction. Our 1M-budget variant outperforms the state-of-the-art MCMC baseline by achieving a 26.8% reduction in training time (saving 17 minutes per session) and utilizing 13.3% fewer Gaussians while maintaining superior visual quality. Furthermore, our full variant demonstrates a 1.28 dB PSNR increase over the ADC baseline with a 38.4% reduction in parametric complexity. These results validate ImprovedGS+ as a scalable, high-speed solution that upholds the core pillars of Speed, Quality, and Usability within the LichtFeld-Studio ecosystem.

---

## 论文详细总结（自动生成）

以下是对论文《ImprovedGS+: A High-Performance C++/CUDA Re-Implementation Strategy for 3D Gaussian Splatting》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证 3D 高斯泼溅（3DGS）重建质量的同时，最大限度地提升计算效率并降低参数冗余。
*   **研究背景**：现有的 3DGS 改进方案（如 ImprovedGS）多依赖 Python/PyTorch 的高级逻辑，这导致了频繁的主机-设备（Host-Device）同步开销和训练延迟。
*   **研究动机**：通过将核心逻辑下沉到原生的 C++/CUDA 内核，消除框架层面的性能瓶颈，并引入更精细的几何启发式策略，实现速度、质量和易用性的平衡。

### 2. 方法论核心思想与技术细节
ImprovedGS+ 的核心在于将 Python 逻辑重构为高性能 CUDA 内核，并优化了加密（Densification）策略：
*   **拉普拉斯边缘重要性内核**：
    *   实现了一套类似于 Canny 边缘检测的 CUDA 流水线：灰度转换 -> 5x5 高斯滤波 -> Sobel 梯度提取 -> **非极大值抑制（NMS）**。
    *   **作用**：通过 NMS 提取极细的结构骨架，避免在平坦表面产生噪声点，防止“加密漂移”。
*   **原生 CUDA 长轴分割（LAS）内核**：
    *   将复杂的分割逻辑封装在单个内核中，直接在对数空间操作缩放参数，确保数值一致性。
    *   **坐标变换优化**：利用主轴方向的局部特性，将全局位移计算从 9 次乘法简化为 3 次，显著提升并行效率。
*   **自适应细化策略**：
    *   **指数缩放调度器**：分为两个阶段。第一阶段使用高学习率（0.020）快速扩张填补空间；第二阶段通过指数衰减（至 0.002）冻结结构细节。
    *   **位置学习率优化**：大幅提升初始位置学习率（从基准的 0.00004 提升至 0.000128），增强模型捕捉复杂几何的能力。
    *   **热身阶段**：在前三次加密中挂起梯度阈值掩码，确保室外场景的低梯度表面也能得到充分填充。

### 3. 实验设计
*   **数据集**：专注于 **Mip-NeRF360** 数据集，涵盖 9 个典型场景（如 Garden, Bicycle, Bonsai, Kitchen 等），包含室内和室外环境。
*   **基准对比（Benchmarks）**：
    *   **MCMC**：基于马尔可夫链蒙特卡洛的 SOTA 策略。
    *   **ADC**：基于 3DGS 的常规改进方案。
    *   **ImprovedGS (Baseline)**：作者自行实现的 C++/CUDA 版原始 ImprovedGS 逻辑。
*   **评估指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、训练时长（Time）、高斯点数量（#G）。

### 4. 资源与算力
*   **硬件平台**：单张 **NVIDIA RTX A4500** GPU。
*   **训练时长**：
    *   1M 预算变体：在 Mip-NeRF360 全集上的平均训练总时长约为 45 分钟。
    *   完整版变体：约为 62 分钟。
    *   相比 MCMC 基准，在相同预算下每场训练平均节省约 17 分钟。

### 5. 实验数量与充分性
*   **实验规模**：对 9 个场景进行了详尽测试，每个场景的指标均取自 **6 次独立运行的平均值**，以确保结果的统计客观性。
*   **消融实验**：针对“指数缩放调度器”和“位置学习率”在 Garden（室外）和 Bonsai（室内）场景下进行了对比，验证了各组件对最终 PSNR 提升的贡献。
*   **公平性**：所有对比方法均在相同的 LichtFeld-Studio 框架和硬件环境下运行，确保了对比的公正性。

### 6. 主要结论与发现
*   **帕累托最优**：ImprovedGS+ 在重建质量和速度之间达到了新的最优平衡。
*   **高效性**：1M 预算变体比 MCMC 训练速度快 **26.8%**，且高斯点数量减少了 **13.3%**，同时视觉质量更优。
*   **高质量**：完整版变体比 ADC 方案 PSNR 提升了 **1.28 dB**，而参数复杂度降低了 **38.4%**。
*   **自适应能力**：在室内场景（如 Room）中，模型能自动优化参数，仅需 0.59M 高斯点即可达到 SOTA 效果，无需耗尽预设预算。

### 7. 优点与亮点
*   **底层优化彻底**：通过自定义 CUDA 内核（LAS、NMS）极大降低了 Python 框架带来的延迟。
*   **几何感知力强**：引入 NMS 边缘评分，使高斯点的分布更加贴合真实的几何流形。
*   **调度策略精巧**：两阶段学习率调度成功解决了“快速覆盖”与“精细细化”之间的矛盾。

### 8. 不足与局限
*   **数据集覆盖有限**：由于 LichtFeld-Studio 框架的验证架构问题，作者主动忽略了 Tanks & Temples 和 Deep Blending 数据集，这可能限制了方法在更广泛场景下的泛化性验证。
*   **初期指标波动**：消融实验显示，由于采用了较高的初始位置学习率，模型在训练初期的光度精度（7000 次迭代时）略低于基准，需依赖后期细化阶段追回。
*   **应用限制**：该方法深度绑定 LichtFeld-Studio 框架，对于其他基于 PyTorch 的 3DGS 变体，迁移其底层 CUDA 优化的难度较大。

（完）
