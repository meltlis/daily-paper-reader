---
title: "BrepGaussian: CAD reconstruction from Multi-View Images with Gaussian Splatting"
title_zh: BrepGaussian：基于高斯泼溅的多视图图像 CAD 重建
authors: "Jiaxing Yu, Dongyang Ren, Hangyu Xu, Zhouyuxiao Yang, Yuanqi Li, Jie Guo, Zhengkang Zhou, Yanwen Guo"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21105v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 利用高斯泼溅从图像中进行CAD和表面重建
tldr: 本研究针对从非结构化数据恢复B-rep模型的挑战，提出了BrepGaussian框架。该方法利用高斯泼溅（Gaussian Splatting）技术，直接从多视图图像中学习3D参数化表示。通过引入两阶段学习策略，首先捕捉几何形状与边缘，随后细化面片特征，实现了几何重建与特征学习的解耦。该方法克服了以往对高密度点云的依赖，在保持几何清晰度的同时实现了连贯的实例表示，显著提升了CAD重建的性能。
selection_source: fresh_fetch
motivation: 现有的B-rep重建方法过度依赖高质量点云且泛化能力有限，难以直接从多视图图像中恢复复杂的CAD几何结构。
method: 提出BrepGaussian框架，利用带可学习特征的高斯泼溅渲染器，通过两阶段学习策略分别捕捉几何边缘和细化面片特征。
result: 实验证明该方法在从2D图像恢复3D参数化表示方面优于现有最先进技术，实现了更清晰的几何和连贯的实例表示。
conclusion: BrepGaussian为从非结构化图像数据进行CAD模型重建提供了一种高效且泛化性强的新途径。
---

## 摘要
边界表示（B-rep）将三维实体建模为其显式边界：修剪后的顶点、边和面。从非结构化数据中恢复 B-rep 表示是计算机视觉和图形学中一项具有挑战性且极具价值的任务。深度学习的最新进展极大地提高了三维形状几何的恢复能力，但仍依赖于密集且干净的点云，且难以泛化到新形状。我们提出了 BrepGaussian（B-rep Gaussian Splatting），这是一个从二维图像中学习三维参数化表示的新型框架。我们采用了一个具有可学习特征的高斯泼溅渲染器，并辅以特定的拟合策略。为了解耦几何重建和特征学习，我们引入了一个两阶段学习框架，首先捕捉几何和边缘，然后细化面片特征，以实现干净的几何形状和连贯的实例表示。大量实验证明，我们的方法优于目前最先进的方法。我们将在论文被接收后发布代码和数据集。

## Abstract
The boundary representation (B-rep) models a 3D solid as its explicit boundaries: trimmed corners, edges, and faces. Recovering B-rep representation from unstructured data is a challenging and valuable task of computer vision and graphics. Recent advances in deep learning have greatly improved the recovery of 3D shape geometry, but still depend on dense and clean point clouds and struggle to generalize to novel shapes. We propose B-rep Gaussian Splatting (BrepGaussian), a novel framework that learns 3D parametric representations from 2D images. We employ a Gaussian Splatting renderer with learnable features, followed by a specific fitting strategy. To disentangle geometry reconstruction and feature learning, we introduce a two-stage learning framework that first captures geometry and edges and then refines patch features to achieve clean geometry and coherent instance representations. Extensive experiments demonstrate the superior performance of our approach to state-of-the-art methods. We will release our code and datasets upon acceptance.

---

## 论文详细总结（自动生成）

以下是对论文《BrepGaussian: CAD reconstruction from Multi-View Images with Gaussian Splatting》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的 CAD 重建（逆向工程）主要依赖于高密度的 3D 点云输入。然而，高质量点云的获取成本高昂，且现有的 B-rep（边界表示）重建方法高度依赖人工标注和特定的网络设计，泛化能力有限。
**本文的核心动机**是消除对原始点云输入的依赖，直接利用更易获取的**多视图图像**来重建具有参数化表面和拓扑一致性的 B-rep 模型。这是首个实现从图像到完整 B-rep 模型转换的框架。

### 2. 方法论：核心思想与技术细节
BrepGaussian 结合了 **2D 高斯泼溅（2DGS）** 和 **参数化基元拟合** 技术，主要流程如下：

*   **数据预处理**：利用边缘检测器和 SAM（Segment Anything Model）从多视图图像中提取 2D 边缘掩码和面片（Patch）掩码。
*   **两阶段高斯学习策略**：
    *   **第一阶段（几何与边缘学习）**：优化 2D 高斯的几何属性（位置、缩放、旋转）和边缘特征（标量值），通过渲染损失和边缘感知损失监督，获得准确的物体轮廓。
    *   **第二阶段（面片实例学习）**：冻结几何参数，为每个高斯分配高维特征向量。采用**对比学习（Triplet Loss）**，通过 2D 掩码引导，使同一面片内的高斯特征聚集，不同面片间的高斯特征相互排斥，从而解决跨视图标签不一致的问题。
*   **点云生成与采样**：根据学习到的高斯形状进行非均匀采样，在边缘区域增加采样密度，生成带有边缘和面片标签的稠密点云。
*   **约束引导的基元拟合**：
    *   使用 RANSAC 算法拟合平面、圆柱面和球面。
    *   通过基元求交提取候选直线和曲线。
    *   利用边缘点云约束和拓扑调整，通过布尔运算组装成封闭、防水的 B-rep 模型。

### 3. 实验设计
*   **数据集**：
    *   **ABC-NEF**：ABC 数据集的子集，包含 50 个多视图图像/物体，具有精确的参数化标注。
    *   **ABO**：真实世界场景数据集，用于验证泛化能力。
*   **对比方法**：
    *   **分割任务**：ParSeNet, HPNet, PCER-Net, SED-Net。
    *   **重建任务**：Point2CAD, Split-and-Fit。
*   **评估指标**：
    *   分割：Precision（精确率）、Recall（召回率）、F1 分数。
    *   重建：Chamfer Distance (CD)、Hausdorff Distance (HD)。

### 4. 资源与算力
论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **提及的细节**：在重建阶段，作者提到使用了一个脚本进行手动精细化处理，每个物体大约耗时 3 分钟。
*   **特征维度**：第二阶段的面片特征维度设置为 16。

### 5. 实验数量与充分性
*   **实验规模**：在 ABC-NEF 挑战性子集上进行了大规模测试，并扩展到了 ABO 真实数据集。
*   **消融实验**：针对“两阶段学习”、“三元组损失（Triplet Loss）”、“边缘分割”和“点云稠密化”分别做了消融对比，验证了各模块的必要性。
*   **视图数量分析**：测试了 5、10、30、50 个不同输入视图数量对重建质量的影响。
*   **客观性**：为了公平对比，作者将基线方法运行在由其 GS 生成的“稠密点云”上，证明了即使输入相同，其分割和拟合策略依然占优。

### 6. 主要结论与发现
*   **无需点云监督**：BrepGaussian 是首个仅通过图像实现完整 B-rep 重建的框架，且性能优于许多需要点云输入的方法。
*   **两阶段策略的有效性**：解耦几何与特征学习能显著提升复杂面片实例的分割准确度。
*   **视图依赖性**：实验发现至少需要 30-50 个视图才能生成足够干净、稠密的点云以支持高质量的 B-rep 拟合。
*   **几何精度**：在曲线重建指标上达到了最优，表面重建指标与 Point2CAD 相当，但生成的模型更简洁、无冗余。

### 7. 优点（亮点）
*   **创新性**：成功将 3DGS 的显式表示优势引入到结构化的 CAD 重建领域。
*   **特征解耦**：通过冻结几何优化特征，解决了神经渲染中几何噪声干扰语义标签的问题。
*   **鲁棒性**：对比学习策略有效处理了 2D 分割掩码在多视图间的不一致性。
*   **实用性**：生成的 B-rep 模型具有参数化属性，可直接用于下游工程应用。

### 8. 不足与局限
*   **依赖前端模型**：重建质量高度依赖于预训练的边缘检测器和 SAM 的表现，若 2D 掩码质量极差，后续重建会受限。
*   **计算流程较长**：包含 2D 提取、两阶段 GS 训练、采样、拟合、拓扑调整等多个步骤，非端到端实时系统。
*   **人工干预**：文中提到每个物体需约 3 分钟的手动脚本精细化，说明自动化程度在复杂拓扑下仍有提升空间。
*   **基元限制**：目前主要支持平面、球面、圆柱面，对于更复杂的自由曲面（如 NURBS）支持有限。

（完）
