---
title: "TokenSplat: Token-aligned 3D Gaussian Splatting for Feed-forward Pose-free Reconstruction"
title_zh: TokenSplat：面向前馈式无位姿重建的令牌对齐 3D 高斯泼溅
authors: "Yihui Li, Chengxin Lv, Zichen Tang, Hongyu Yang, Di Huang"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00697v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 从无位姿图像进行前馈3D高斯重建
tldr: TokenSplat 是一种前馈框架，旨在从无位姿的多视图图像中同时进行 3D 高斯重建和相机位姿估计。它通过 Token 对齐的高斯预测模块在特征空间对齐跨视图语义，并利用非对称双流解码器（ADF-Decoder）和可学习相机 Token 实现位姿与语义的解耦。该方法无需迭代优化即可实现高保真重建和稳定的位姿估计，在位姿未知场景下表现优于现有方法。
selection_source: fresh_fetch
motivation: 旨在解决从无位姿多视图图像中进行高效、准确的 3D 重建和相机位姿估计的挑战。
method: 引入 Token 对齐的高斯预测模块和非对称双流解码器，通过特征空间对齐和定向通信实现相机位姿与场景语义的解耦。
result: 在无位姿设定下，该方法在重建保真度、新视角合成质量以及位姿估计精度方面均显著优于此前的先进方法。
conclusion: TokenSplat 证明了通过 Token 对齐和特征解耦，可以在前馈架构中实现稳健且高质量的无位姿 3D 高斯重建。
---

## 摘要
我们提出了 TokenSplat，这是一个用于从无位姿多视图图像中联合进行 3D 高斯重建和相机位姿估计的前馈框架。其核心是，TokenSplat 引入了一个令牌对齐的高斯预测模块，该模块直接在特征空间中对齐跨视图的语义对应信息。在粗略令牌位置和融合置信度的指导下，它聚合多尺度上下文特征，以实现长程跨视图推理并减少重叠高斯带来的冗余。为了进一步增强位姿鲁棒性并将视点线索与场景语义解耦，TokenSplat 采用了可学习的相机令牌和非对称双流解码器 (ADF-Decoder)，该解码器强制执行相机令牌与图像令牌之间的方向约束通信。这在前馈架构中保持了清晰的因子分解，从而在无需迭代优化的的情况下实现连贯的重建和稳定的位姿估计。大量实验表明，TokenSplat 在无位姿设置下实现了更高的重建保真度和新视图合成质量，并且与之前的无位姿方法相比，显著提高了位姿估计精度。项目页面：https://kidleyh.github.io/tokensplat/。

## Abstract
We present TokenSplat, a feed-forward framework for joint 3D Gaussian reconstruction and camera pose estimation from unposed multi-view images. At its core, TokenSplat introduces a Token-aligned Gaussian Prediction module that aligns semantically corresponding information across views directly in the feature space. Guided by coarse token positions and fusion confidence, it aggregates multi-scale contextual features to enable long-range cross-view reasoning and reduce redundancy from overlapping Gaussians. To further enhance pose robustness and disentangle viewpoint cues from scene semantics, TokenSplat employs learnable camera tokens and an Asymmetric Dual-Flow Decoder (ADF-Decoder) that enforces directionally constrained communication between camera and image tokens. This maintains clean factorization within a feed-forward architecture, enabling coherent reconstruction and stable pose estimation without iterative refinement. Extensive experiments demonstrate that TokenSplat achieves higher reconstruction fidelity and novel-view synthesis quality in pose-free settings, and significantly improves pose estimation accuracy compared to prior pose-free methods. Project page: https://kidleyh.github.io/tokensplat/.

---

## 论文详细总结（自动生成）

以下是对论文《TokenSplat: Token-aligned 3D Gaussian Splatting for Feed-forward Pose-free Reconstruction》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 3D 高斯泼溅（3DGS）重建方法大多依赖精确的相机位姿（通常由 COLMAP 等 SfM 工具预先计算），这在实际应用中计算昂贵且易失败。现有的“无位姿”（Pose-free）前馈方法存在两个主要痛点：
    1.  **特征纠缠**：相机位姿线索与场景语义特征在嵌入空间中混杂，导致位姿误差干扰重建质量。
    2.  **高斯冗余**：基于像素对齐的预测方式在多视图下会产生大量重叠的高斯点，导致几何模糊和计算低效。
*   **研究背景**：旨在开发一种能够从任意数量的无位姿图像中，直接通过一次前馈推理（无需迭代优化）同时实现高质量 3D 重建和精确位姿估计的框架。

### 2. 方法论
TokenSplat 采用基于 Transformer 的前馈架构，核心组件包括：
*   **非对称双流解码器 (ADF-Decoder)**：
    *   **核心思想**：通过方向受限的通信机制解耦位姿与语义。
    *   **技术细节**：引入可学习的“相机令牌”（Camera Tokens）。在解码过程中，相机令牌从图像令牌中提取几何特征用于位姿回归，而图像令牌仅接收来自相机令牌的稳定低频对齐信号，防止视点特定信息污染场景语义。
*   **令牌对齐的高斯预测模块 (TGP)**：
    *   **核心思想**：在特征空间（而非像素空间）进行跨视图对齐与融合。
    *   **技术细节**：首先预测每个令牌的粗略 3D 位置和融合置信度，然后基于空间邻近度对令牌进行分组融合。这种“多对一”的融合策略显著减少了重叠区域的冗余高斯点。
*   **高斯预测头**：采用一到多的映射，从融合后的令牌生成密集且具有表达力的高斯属性（中心、不透明度、旋转、缩放、球谐系数）。
*   **损失函数**：结合了图像渲染损失（$L_2$ + LPIPS）和相机位姿损失（MSE + 单位对偶四元数 DQ 对齐损失），实现端到端训练。

### 3. 实验设计
*   **数据集**：
    *   **RealEstate10K (RE10K)**：室内外大场景视频序列。
    *   **ScanNet**：室内场景数据集。
*   **Benchmark 与对比方法**：
    *   **有位姿需求方法**：MVSplat, FreeSplat。
    *   **无位姿方法**：NoPoSplat, VicaSplat, SPFSplat, AnySplat。
*   **评估指标**：
    *   **NVS（新视角合成）**：PSNR, SSIM, LPIPS。
    *   **位姿估计**：ATE（绝对平移误差）, RPE-t（相对平移误差）, RPE-r（相对旋转误差）。

### 4. 资源与算力
*   **硬件环境**：实验主要在单张 **NVIDIA A800 GPU** 上完成。作者提到通过调整 Batch Size，该模型也可在单张 A6000 或 A100 上运行。
*   **训练时长**：
    *   2 视图预训练：30,000 次迭代（Batch Size 16）。
    *   多视图微调：100,000 次迭代（Batch Size 1）。
*   **推理效率**：由于采用了令牌融合机制，其推理时间随视图数量增加的增长速度远慢于其他高斯融合方法。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3、4、8、10 视图的常规测试，并特别设计了 **28 视图** 的长序列压力测试以验证可扩展性。
    *   进行了**跨数据集泛化实验**（在 RE10K 上训练，直接在 ScanNet 上测试），验证了模型的零样本泛化能力。
    *   **消融实验**：共 5 组，分别验证了令牌对齐头、融合策略、ADF 解码器及相机内参嵌入的有效性。
*   **客观性**：对比了当前最先进的 Pose-free 方案，并统一了评估协议（如视图对齐方式），实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能领先**：TokenSplat 在所有无位姿设定下的 NVS 质量和位姿精度均显著优于现有方法。在 8 视图设定下，其 PSNR 比 FreeSplat 高出 0.95 dB。
*   **位姿鲁棒性**：ADF-Decoder 成功实现了特征解耦，使得位姿估计误差大幅降低（如 RPE-r 降低了约 25%-40%）。
*   **长序列稳定性**：在 28 视图测试中，TokenSplat 保持了稳定的重建质量，而其他方法（如 AnySplat, FreeSplat）因高斯冗余累积出现了明显的性能下降。

### 7. 优点（亮点）
*   **高效的令牌融合**：通过在特征空间融合 Token，从根本上解决了多视图 3DGS 重建中的冗余和碎片化问题。
*   **创新的解耦设计**：ADF-Decoder 的非对称设计为前馈网络处理位姿-语义对齐提供了新思路。
*   **强大的泛化性**：无需针对新场景进行优化，且在跨数据集测试中表现出极强的鲁棒性。
*   **良好的扩展性**：能够处理从少量（2-3张）到大量（28张）不等的输入视图。

### 8. 不足与局限
*   **初始化依赖**：模型部分权重（如 Encoder）依赖于 MASt3R 的预训练，这可能限制了其在完全脱离该先验时的表现。
*   **计算开销**：虽然推理时间相对稳定，但 Transformer 架构在处理极高分辨率图像或超长序列时，内存开销仍是潜在瓶颈。
*   **极端场景**：论文未详细讨论在纹理极度缺失或光照剧烈变化场景下的位姿估计鲁棒性。

（完）
