---
title: Radiometrically Consistent Gaussian Surfels for Inverse Rendering
title_zh: 用于逆渲染的辐射一致性高斯面元
authors: "Kyu Beom Han, Jaeyoon Kim, Woo Jae Kim, Jinhwan Seo, Sung-eui Yoon"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01491v1"
tags: ["query:3dgsir"]
score: 10.0
evidence: 基于物理的逆向渲染，用于解耦材质和全局光照
tldr: 针对高斯泼溅在逆向渲染中难以准确分离材质与间接光照的问题，本文提出了辐射一致性约束。该方法通过最小化高斯原语学习到的辐射度与物理渲染结果之间的残差，为未观测视角提供监督，建立了自校正反馈循环。基于此构建的RadioGS框架利用高斯面元和2D射线追踪实现了高效的间接光建模，在保持高效率的同时显著提升了材质解耦与重光照的精度。
selection_source: fresh_fetch
motivation: 现有高斯泼溅方法因缺乏对未观测视角间接辐射的有效监督，导致难以从复杂全局光照中准确分离材质属性。
method: 引入辐射一致性物理约束，结合高斯面元与2D高斯射线追踪技术，通过最小化学习辐射度与物理渲染值的残差来优化模型。
result: 实验表明RadioGS在逆向渲染基准上优于现有方法，且支持极速重光照适配，渲染耗时低于10毫秒。
conclusion: 通过物理渲染与新视角合成的协同监督，RadioGS成功解决了高斯面元在处理复杂光影交互时的准确性与效率平衡问题。
---

## 摘要
基于高斯泼溅（Gaussian Splatting）的逆渲染技术发展迅速，但从复杂的全局光照效应（特别是间接光照）中准确解耦材质属性仍然是一个重大挑战。现有方法通常从为新视角合成预训练的高斯原语中查询间接辐射度。然而，这些预训练的高斯原语仅针对有限的训练视角进行监督，因此缺乏对未观察视角间接辐射度建模的监督。为解决这一问题，我们引入了辐射一致性，这是一种新型的基于物理的约束，通过最小化每个高斯原语的学习辐射度与其基于物理渲染的对应值之间的残差，为未观察视角提供监督。最小化未观察视角的残差建立了一个自校正反馈循环，该循环提供了来自基于物理渲染和新视角合成的双重监督，从而实现了对相互反射的准确建模。随后，我们提出了辐射一致性高斯面元（RadioGS），这是一个基于上述原理的逆渲染框架，通过利用高斯面元和二维高斯光线追踪高效地集成了辐射一致性。我们还提出了一种基于微调的重打光策略，可以在几分钟内使高斯面元辐射度适应新的光照，并实现极低的渲染成本（小于10毫秒）。在现有逆渲染基准测试上的大量实验表明，RadioGS 在逆渲染方面优于现有的基于高斯的方法，同时保持了计算效率。

## Abstract
Inverse rendering with Gaussian Splatting has advanced rapidly, but accurately disentangling material properties from complex global illumination effects, particularly indirect illumination, remains a major challenge. Existing methods often query indirect radiance from Gaussian primitives pre-trained for novel-view synthesis. However, these pre-trained Gaussian primitives are supervised only towards limited training viewpoints, thus lack supervision for modeling indirect radiances from unobserved views. To address this issue, we introduce radiometric consistency, a novel physically-based constraint that provides supervision towards unobserved views by minimizing the residual between each Gaussian primitive's learned radiance and its physically-based rendered counterpart. Minimizing the residual for unobserved views establishes a self-correcting feedback loop that provides supervision from both physically-based rendering and novel-view synthesis, enabling accurate modeling of inter-reflection. We then propose Radiometrically Consistent Gaussian Surfels (RadioGS), an inverse rendering framework built upon our principle by efficiently integrating radiometric consistency by utilizing Gaussian surfels and 2D Gaussian ray tracing. We further propose a finetuning-based relighting strategy that adapts Gaussian surfel radiances to new illuminations within minutes, achieving low rendering cost (<10ms). Extensive experiments on existing inverse rendering benchmarks show that RadioGS outperforms existing Gaussian-based methods in inverse rendering, while retaining the computational efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Radiometrically Consistent Gaussian Surfels for Inverse Rendering》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在逆渲染（Inverse Rendering）任务中，如何从复杂的全局光照（尤其是间接光照和表面间的相互反射）中准确地解耦出场景的几何、材质和环境光照。
*   **研究背景**：虽然高斯泼溅（Gaussian Splatting, GS）在实时渲染和新视角合成（NVS）上取得了巨大成功，但现有的基于 GS 的逆渲染方法存在一个关键缺陷：**缺乏对未观测视角（Unobserved Views）的监督**。
*   **动机**：预训练的高斯原语仅在训练视角下受监督，导致其在其他方向上的辐射度可能是错误的。这会导致光照效应被错误地“烘焙”到材质属性（如反照率 Albedo）中，从而影响重打光（Relighting）的效果。

### 2. 方法论
*   **核心思想**：引入**辐射一致性（Radiometric Consistency）**物理约束。该约束要求高斯面元学习到的辐射度（由球谐函数 SH 表示）必须与其通过渲染方程计算出的物理辐射度保持一致。
*   **关键技术细节**：
    *   **辐射一致性损失 ($L_{rad}$)**：通过最小化学习辐射度 $L_G$ 与基于物理渲染（PBR）辐射度 $L_{PBR}$ 之间的残差，建立自校正反馈循环。
    *   **2D 高斯射线追踪**：利用微分高斯射线追踪技术高效计算可见性（Visibility）和间接光照（Indirect Illumination），确保梯度可以回传至高斯参数。
    *   **RadioGS 框架**：采用两阶段优化。第一阶段进行几何初始化（结合简化的辐射约束）；第二阶段进行完整的逆渲染优化，利用蒙特卡洛采样估计复杂的相互反射。
    *   **快速重打光策略**：提出一种基于微调（Finetuning）的方法，在改变光照后仅需几分钟微调 SH 系数，即可实现无需射线追踪的实时渲染（<10ms）。

### 3. 实验设计
*   **数据集/场景**：
    *   **TensoIR**：合成数据集，包含复杂的几何和光照。
    *   **Synthetic4Relight**：用于评估材质和重打光精度。
    *   **Stanford-ORB**：真实世界物体数据集，验证泛化能力。
    *   **MipNeRF360**：用于测试在大规模复杂场景下的渲染效率。
*   **对比方法（Baselines）**：
    *   **NeRF 系**：TensoIR。
    *   **GS 系**：GS-IR, GI-GS, R3DG, IRGS, SVG-IR（均为近年顶会的前沿方法）。
*   **评估指标**：PSNR, SSIM, LPIPS（图像质量）；MAE（法线精度）；MSE（粗糙度精度）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 4090 GPU**。
*   **训练时长**：总优化时间约 **60 分钟**（30 分钟初始化 + 30 分钟逆渲染优化）。
*   **重打光微调**：针对新光照环境的微调仅需约 **2 分钟**。
*   **推理效率**：微调后的渲染耗时低于 **10 毫秒**，支持实时交互。

### 5. 实验数量与充分性
*   **实验规模**：论文在多个标准合成数据集上进行了定量对比，并在真实数据集上进行了定性展示。
*   **消融实验**：非常充分。作者分别验证了辐射一致性损失的必要性、梯度流的影响、初始化策略的作用，以及超参数（采样点数 $N_g, N_s$）对精度和速度的平衡。
*   **客观性**：作者甚至自行生成了一个带有**间接光照真值（GT Indirect Illumination）**的新评估数据集（基于 Blender Cycles），填补了现有基准缺乏间接光照真值的空白，增强了说服力。

### 6. 主要结论与发现
*   **物理约束的重要性**：单纯依靠图像重建损失不足以解耦材质，物理一致性约束能显著提升未观测方向的辐射度准确性。
*   **性能领先**：RadioGS 在法线重建、反照率估计和重打光质量上均优于现有的基于高斯的方法。
*   **效率优势**：通过微调 SH 系数而非实时运行射线追踪，RadioGS 解决了高质量重打光与实时渲染速度之间的矛盾。

### 7. 优点（亮点）
*   **理论完备**：将神经辐射缓存（Neural Radiance Caching）的自监督思想引入高斯面元，理论框架优雅且符合物理规律。
*   **解耦彻底**：通过解决“未观测视角”问题，有效减少了材质与光照的混淆（Ambiguity）。
*   **实用性强**：训练时间（1小时）和重打光微调时间（2分钟）在实际应用中非常具有吸引力。

### 8. 不足与局限
*   **材质限制**：目前主要针对**绝缘体（Dielectric）**材质建模，使用了简化的 Disney BRDF。对于金属、各向异性材质或高度透明/折射物体的处理能力有限。
*   **几何依赖**：逆渲染的质量高度依赖于初始化的几何精度，如果初始几何存在严重拓扑错误，辐射一致性损失可能难以纠正。
*   **环境假设**：假设环境光照为远景（Distant Lighting），对于近场复杂光源的建模尚不完善。

（完）
