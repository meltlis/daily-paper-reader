---
title: "InstantHDR: Single-forward Gaussian Splatting for High Dynamic Range 3D Reconstruction"
title_zh: InstantHDR：用于高动态范围 3D 重建的单次前向高斯泼溅
authors: "Dingqiang Ye, Jiacong Xu, Jianglu Ping, Yuxiang Guo, Chao Fan, Vishal M. Patel"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11298v2"
tags: ["query:3dgsir"]
score: 9.0
evidence: 使用3DGS进行外观建模和HDR 3D重建
tldr: 本研究提出InstantHDR，一种基于高斯泼溅的单次前馈网络，旨在从多曝光低动态范围（LDR）图像中高效重建高动态范围（HDR）3D场景。针对现有方法依赖已知位姿且优化耗时的问题，该方法通过几何引导的外观建模和元网络色调映射，实现了无需校准的快速重建。实验表明，其在保持与优化方法相当的合成质量的同时，重建速度提升了约700倍，显著增强了HDR 3D重建的效率。
selection_source: fresh_fetch
motivation: 现有的HDR三维重建方法通常依赖已知相机位姿且优化过程极其耗时，而现有的前馈模型往往忽略了曝光变化带来的外观差异。
method: 提出了一种结合几何引导外观建模和通用场景色调映射元网络的前馈框架，并构建了包含168个场景的HDR-Pretrain数据集用于训练。
result: 在单次前馈模式下，重建速度比传统优化方法快约700倍，且合成质量与当前最先进的优化方法相当。
conclusion: InstantHDR成功实现了从非校准多曝光图像到高质量HDR场景的高效前馈重建，为实时HDR 3D重建提供了新的解决方案。
---

## 摘要
高动态范围（HDR）新视角合成（NVS）旨在从多曝光低动态范围（LDR）图像中重建 HDR 场景。现有的 HDR 流水线严重依赖已知的相机位姿、初始化良好的稠密点云以及耗时的逐场景优化。目前的前馈替代方案通常假设外观与曝光无关，从而忽略了 HDR 问题。为了填补这一空白，我们提出了 InstantHDR，这是一个前馈网络，能够通过单次前向传递从未经校准的多曝光 LDR 图像集中重建 3D HDR 场景。具体而言，我们设计了一种用于多曝光融合的几何引导外观建模，以及一个用于可泛化的场景特定色调映射的元网络。由于缺乏 HDR 场景数据，我们构建了一个名为 HDR-Pretrain 的预训练数据集，用于训练可泛化的前馈 HDR 模型，该数据集包含 168 个 Blender 渲染场景、多样的光照类型和多种相机响应函数。综合实验表明，InstantHDR 的合成性能与最先进的基于优化的 HDR 方法相当，同时在单次前向和后优化设置下，重建速度分别提升了约 700 倍和 20 倍。所有代码、模型和数据集将在评审结束后公开发布。

## Abstract
High dynamic range (HDR) novel view synthesis (NVS) aims to reconstruct HDR scenes from multi-exposure low dynamic range (LDR) images. Existing HDR pipelines heavily rely on known camera poses, well-initialized dense point clouds, and time-consuming per-scene optimization. Current feed-forward alternatives overlook the HDR problem by assuming exposure-invariant appearance. To bridge this gap, we propose InstantHDR, a feed-forward network that reconstructs 3D HDR scenes from uncalibrated multi-exposure LDR collections in a single forward pass. Specifically, we design a geometry-guided appearance modeling for multi-exposure fusion, and a meta-network for generalizable scene-specific tone mapping. Due to the lack of HDR scene data, we build a pre-training dataset, called HDR-Pretrain, for generalizable feed-forward HDR models, featuring 168 Blender-rendered scenes, diverse lighting types, and multiple camera response functions. Comprehensive experiments show that our InstantHDR delivers comparable synthesis performance to the state-of-the-art optimization-based HDR methods while enjoying $\sim700\times$ and $\sim20\times$ reconstruction speed improvement with our single-forward and post-optimization settings. All code, models, and datasets will be released after the review process.

---

## 论文详细总结（自动生成）

以下是对论文《InstantHDR: Single-forward Gaussian Splatting for High Dynamic Range 3D Reconstruction》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从一组**未经校准（无相机位姿）**且**曝光程度不同**的低动态范围（LDR）图像中，快速重建出高质量的高动态范围（HDR）3D场景。
*   **研究动机**：
    *   **现有方法效率低**：传统的 HDR 新视角合成（NVS）方法（如基于 NeRF 或 3DGS 的优化方法）依赖精确的相机位姿和耗时的逐场景优化（通常需数十分钟）。
    *   **前馈模型缺陷**：现有的快速前馈 3D 重建模型（如 AnySplat）假设场景外观随曝光是不变的，导致在处理多曝光输入时会出现严重的重影和伪影。
    *   **数据匮乏**：缺乏大规模的多视角、多曝光 HDR 数据集来训练通用的前馈模型。

### 2. 方法论
InstantHDR 采用前馈网络架构，通过单次前向传递生成 HDR 3D 高斯点云。
*   **核心思想**：将重建任务分解为几何分支（冻结）和外观分支（可训练），利用几何信息引导不同曝光图像间的特征融合。
*   **关键技术细节**：
    *   **几何引导外观建模**：
        1.  **曝光归一化 (FE)**：使用 FiLM 层将不同曝光的特征对齐到统一的参考亮度空间。
        2.  **几何引导交叉视图注意力 (FA)**：复用几何编码器中的注意力图来建立跨视图的像素对应关系，解决大曝光差异下的匹配难题。
        3.  **高分辨率上采样 (FU)**：利用高斯差分（DoG）提取高频细节，补偿补丁式编码带来的纹理损失。
    *   **元网络 (MetaNet)**：预测场景特定的色调映射器（Tone Mapper）参数。由于不同相机的响应函数（CRF）不同，MetaNet 使模型能自适应地将 HDR 辐射度映射回特定相机的 LDR 图像。
    *   **HDR 3D 高斯表示**：高斯球的颜色由球谐函数（SH）系数表示，这些系数在对数空间编码辐射度。

### 3. 实验设计
*   **数据集**：
    *   **HDR-Pretrain（自建）**：包含 168 个 Blender 渲染的室内场景，提供多曝光 LDR、32 位 HDR 真值、深度图和法线图。
    *   **HDR-NeRF Benchmark**：包含 8 个合成场景和 4 个真实场景，用于验证泛化能力。
*   **对比方法**：
    *   **前馈类**：AnySplat（目前最先进的通用 3DGS 重建模型）。
    *   **优化类**：HDR-GS、GaussianHDR（基于 3DGS 的 SOTA 优化方法）。
*   **评价指标**：PSNR、SSIM、LPIPS 以及重建耗时。

### 4. 资源与算力
*   **算力设备**：使用了 **8 张 NVIDIA A6000 GPU**。
*   **训练时长**：在自建数据集上训练约 **2 天**（30,000 次迭代）。
*   **推理效率**：单次前向重建仅需 **1 秒左右**，带 1000 步后优化的版本需约 30-40 秒。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 视图、8 视图和 18 视图等多种稀疏/稠密输入设置。
*   **消融实验**：针对元网络、曝光归一化、交叉视图注意力和上采样模块分别进行了消融测试，验证了各组件的必要性。
*   **客观性**：实验在合成和真实数据集上均进行了验证，并对比了零样本（Zero-shot）和微调（Post-optimization）两种模式，实验设计全面且客观。

### 6. 主要结论与发现
*   **速度飞跃**：InstantHDR 的单次前向重建速度比 GaussianHDR 快约 **700 倍**，即使加上后优化也快约 **20 倍**。
*   **性能优异**：在稀疏视图（如 4 视图）下，InstantHDR 的表现显著优于传统优化方法，因为它利用了强大的前馈几何先验。
*   **HDR 恢复能力**：尽管只用 LDR 图像训练，模型能隐式学习 HDR 辐射度，在 $\mu$-law 域的 HDR 评估中达到了与优化方法相当的水平。

### 7. 优点
*   **首创性**：提出了首个用于 HDR 3D 重建的通用前馈框架。
*   **鲁棒性**：通过几何引导注意力机制，有效解决了多曝光图像间的“重影”问题。
*   **泛化性**：MetaNet 的设计解决了不同相机 CRF 不一致的问题，增强了模型的通用性。
*   **资源贡献**：发布了 HDR-Pretrain 大规模数据集，填补了领域空白。

### 8. 不足与局限
*   **HDR 亮度偏差**：在零样本推理模式下，生成的 HDR 结果有时会出现整体亮度偏高的问题，准确预测极端辐射度值仍具挑战。
*   **对后优化的依赖**：虽然前馈结果已很出色，但要达到 SOTA 级别的 PSNR 精度，仍需少量的后优化步骤。
*   **领域间隙**：在真实场景上表现良好，但仍需在少量真实数据上进行微调以获得最佳效果。

（完）
