---
title: "S2GS: Streaming Semantic Gaussian Splatting for Online Scene Understanding and Reconstruction"
title_zh: S2GS：用于在线场景理解与重建的流式语义高斯泼溅
authors: "Renhe Zhang, Yuyang Tan, Jingyu Gong, Zhizhong Zhang, Lizhuang Ma, Yuan Xie, Xin Tan"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14232v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 用于场景重建和外观的流式3D高斯泼溅
tldr: 针对长视频流场景理解与重建中全局计算导致的内存和耗时激增问题，本文提出S2GS框架。该框架采用严格因果且增量的3D高斯语义场设计，无需重新处理历史帧即可持续更新几何、外观和实例语义。通过几何-语义解耦的双骨干架构及实例内存机制，S2GS在保持高性能的同时显著提升了长时序列的扩展性，支持处理千帧以上数据，有效解决了在线场景感知的瓶颈。
selection_source: fresh_fetch
motivation: 现有离线方法在处理长图像流时因重复全局计算导致显存和耗时随序列长度激增，限制了大规模场景的在线应用。
method: 采用几何-语义解耦的双骨干设计，通过因果建模驱动高斯增量更新，并结合2D基础模型与实例内存实现稳定的语义关联。
result: S2GS在联合重建与理解任务中达到或超过了离线基线水平，且在同等显存下处理帧数从80帧提升至1000帧以上。
conclusion: 该研究证明了增量式流处理在提升大规模场景在线重建与语义理解扩展性方面的有效性，为实时三维感知提供了高效方案。
---

## 摘要
现有的针对长图像流进行联合场景理解与重建的离线前馈方法，通常会对不断增长的历史观测集重复进行全局计算，导致运行时间和 GPU 显存随序列长度增加而迅速增长，从而限制了可扩展性。我们提出了流式语义高斯泼溅（Streaming Semantic Gaussian Splatting，简称 S2GS），这是一个严格因果且增量的 3D 高斯语义场框架：它不利用未来帧，并在不重新处理历史帧的情况下持续更新场景几何、外观和实例级语义，从而实现了可扩展的在线联合重建与理解。S2GS 采用了几何-语义解耦的双骨干设计：几何分支进行因果建模以驱动增量式高斯更新，而语义分支则利用 2D 视觉基础模型和查询驱动的解码器来预测分割掩码和身份嵌入，并通过查询级对比对齐以及与实例存储器的轻量级在线关联进一步增强稳定性。实验表明，S2GS 在联合重建与理解基准测试中达到或超过了强大的离线基线模型，同时显著提升了长时程的可扩展性：它在处理 1,000 多帧图像时，运行时间和 GPU 显存的增长速度要慢得多，而离线全局处理基线在相同设置下通常在 80 帧左右就会耗尽显存。

## Abstract
Existing offline feed-forward methods for joint scene understanding and reconstruction on long image streams often repeatedly perform global computation over an ever-growing set of past observations, causing runtime and GPU memory to increase rapidly with sequence length and limiting scalability. We propose Streaming Semantic Gaussian Splatting (S2GS), a strictly causal, incremental 3D Gaussian semantic field framework: it does not leverage future frames and continuously updates scene geometry, appearance, and instance-level semantics without reprocessing historical frames, enabling scalable online joint reconstruction and understanding. S2GS adopts a geometry-semantic decoupled dual-backbone design: the geometry branch performs causal modeling to drive incremental Gaussian updates, while the semantic branch leverages a 2D foundation vision model and a query-driven decoder to predict segmentation masks and identity embeddings, further stabilized by query-level contrastive alignment and lightweight online association with an instance memory. Experiments show that S2GS matches or outperforms strong offline baselines on joint reconstruction-and-understanding benchmarks, while significantly improving long-horizon scalability: it processes 1,000+ frames with much slower growth in runtime and GPU memory, whereas offline global-processing baselines typically run out of memory at around 80 frames under the same setting.

---

## 论文详细总结（自动生成）

### 论文总结：S2GS (Streaming Semantic Gaussian Splatting)

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的联合场景理解与 3D 重建方法（如 SIU3R、Uni3R）大多采用“离线前馈”模式。随着输入图像序列变长，这些方法需要对不断增长的历史观测进行重复的全局计算，导致运行时间和 GPU 显存随帧数呈指数级或快速增长。
*   **研究背景**：在实际的在线应用（如机器人导航、AR/VR）中，系统需要处理长时程的视频流。现有方法在处理约 80 帧时就会因显存溢出（OOM）而崩溃，无法实现真正的在线、可扩展的场景感知。
*   **整体含义**：本文提出了 **S2GS**，旨在通过“严格因果”且“增量更新”的机制，在不重新处理历史帧的前提下，实现 3D 几何、外观和实例语义的同步流式构建。

#### 2. 提出方法论
S2GS 采用**几何-语义解耦的双骨干架构**，核心思想是将稳定的几何重建与动态的语义感知分离：
*   **因果几何流（Causal Geometry Stream）**：
    *   使用 **Causal Transformer** 编码器，通过时间注意力掩码确保当前帧仅能访问历史信息。
    *   引入 **3D 基础模型蒸馏**：从预训练的流式 3D 模型中提取深度和相机位姿作为伪标签进行监督，以稳定在线重建。
    *   **增量构建**：将预测的深度和属性反投影为 3D 高斯点，并持续累积到全局场景表示中。
*   **解耦语义流（Decoupled Semantic Stream）**：
    *   利用冻结的 **2D 视觉基础模型（如 SigLIP2）** 提取多尺度特征。
    *   采用**查询驱动（Query-based）解码器**生成掩码和身份嵌入（Identity Embeddings）。
*   **语义与身份稳定机制**：
    *   **查询级对比学习（CL）**：在训练时拉近同一实例在不同帧中的查询嵌入，推开不同实例。
    *   **在线实例存储器（Memory Bank）**：推理时通过余弦相似度和二分匹配进行轻量级关联，并使用动量更新（EMA）维护实例原型。
*   **开放词汇支持**：设计了一个轻量级的**查询语义投影器（Projector）**，将查询嵌入映射到 SigLIP2 的视觉-语言空间，支持文本驱动的物体检索。

#### 3. 实验设计
*   **数据集**：主要在 **ScanNet** 上进行训练和评估；在 **ScanNet++** 和 **Replica** 上进行零样本（Zero-shot）跨数据集泛化测试。
*   **Benchmark 与指标**：
    *   **3D 重建**：PSNR、SSIM（评估新视角合成质量）。
    *   **语义理解**：mIoU（语义分割）、T-mIoU 和 T-SR（评估跨帧实例一致性）。
*   **对比方法**：
    *   **联合方法**：SIU3R、Uni3R、LSM。
    *   **重建专用**：pixelSplat、MVSplat、NoPoSplat。
    *   **2D 分割**：LSeg、Mask2Former。

#### 4. 资源与算力
*   **硬件使用**：论文在对比实验中提到了使用 **NVIDIA H200 GPU (140GB VRAM)**。
*   **效率表现**：S2GS 在处理 1024 帧时显存占用约为 76.88GB，而基线方法 SIU3R 在 128 帧之前就已 OOM。
*   **训练时长**：文中未明确给出具体的总训练小时数，但提到了训练设置（如学习率、Batch Size 等）详见附录。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了不同输入规模（2, 8, 14, 32 帧）的短序列对比。
    *   进行了长序列（64, 256, 1024 帧）的可扩展性测试。
    *   包含 3 组核心消融实验：解耦架构的必要性、对比学习的作用、几何蒸馏的影响。
    *   包含 2 个额外数据集的泛化性实验。
*   **充分性评价**：实验设计较为全面，涵盖了从短时到长时的性能演变，且通过消融实验验证了每个模块的贡献。对比实验在相同的在线设置下进行，具有较高的客观性和公平性。

#### 6. 主要结论与发现
*   **性能持平或超越**：在短序列任务中，S2GS 的表现与顶尖离线方法相当；在长序列任务中，由于离线方法无法运行，S2GS 展现了绝对优势。
*   **极强的扩展性**：S2GS 的推理时间和显存增长远慢于离线方法，支持 1000 帧以上的连续处理。
*   **解耦的优势**：几何与语义的解耦有效防止了几何更新过程中的噪声干扰语义表示，显著提升了实例跟踪的稳定性。
*   **开放词汇能力**：通过投影器蒸馏，S2GS 能够准确响应自由文本查询（如“椅子”、“地板”）。

#### 7. 优点
*   **严格因果性**：符合真实世界在线感知的逻辑，不依赖未来帧。
*   **内存高效**：通过 KV Cache 和增量更新解决了长视频流处理的瓶颈。
*   **多功能统一**：在一个框架内同时实现了高质量重建、语义分割、实例跟踪和开放词汇检索。
*   **稳定性高**：引入对比学习和实例存储器，有效缓解了在线处理中的身份切换（ID Switch）问题。

#### 8. 不足与局限
*   **极稀疏视角下的弱势**：在仅有 2 帧输入时，由于缺乏全局优化和未来帧校正，其重建质量（PSNR）略低于某些离线全局方法。
*   **依赖基础模型**：几何稳定性高度依赖于预训练的 3D 基础模型（如 Stream3R）的质量。
*   **累积误差风险**：作为一种增量式方法，虽然论文展示了长序列的稳定性，但在极端长时程或复杂运动下，可能存在几何漂移或误差累积的潜在风险（文中未深入讨论长期漂移的校正机制）。

（完）
