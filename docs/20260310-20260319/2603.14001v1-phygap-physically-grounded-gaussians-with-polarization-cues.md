---
title: "PhyGaP: Physically-Grounded Gaussians with Polarization Cues"
title_zh: PhyGaP：结合偏振线索的物理驱动高斯
authors: "Jiale Wu, Xiaoyang Bai, Zongqi He, Weiwei Xu, Yifan Peng"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.14001v1"
tags: ["query:3dgsir"]
score: 10.0
evidence: 用于反射分解和重光照的物理基础3DGS
tldr: 针对现有3D高斯泼溅（3DGS）在处理反射物体时难以准确重建物理属性和支持高保真重光照的问题，本文提出了PhyGaP。该方法引入偏振线索，通过偏振延迟渲染（PolarDR）和自遮挡感知环境图（GridMap）技术，实现了精确的反射分解和物理一致的重光照，在合成和真实场景中均表现出卓越的重建与逆渲染能力。
selection_source: fresh_fetch
motivation: 现有的基于RGB图像的3DGS方法因缺乏形状和材质信息，难以准确分解反照率和反射率，导致重光照效果不佳。
method: 提出偏振延迟渲染过程（PolarDR）来建模反射偏振，并结合自遮挡感知环境图（GridMap）处理非凸物体的间接光照。
result: "在多个场景中，PhyGaP在外观和法线重建质量上显著优于现有方法，PSNR提升约2dB，余弦距离优化45.7%。"
conclusion: PhyGaP通过结合偏振信息，有效解决了反射物体的物理属性重建难题，实现了最先进的逆渲染和重光照效果。
---

## 摘要
近期 3D 高斯泼溅（3DGS）的进展在通过延迟渲染（DR）建模反光 3D 物体及其与环境的交互方面取得了巨大成功。然而，现有方法往往难以准确重建反照率（albedo）和反射率（reflectance）等物理属性，因此不支持高保真重光照。观察到这一局限性源于 RGB 图像中缺乏形状和材质信息，我们提出了 PhyGaP，这是一种物理驱动的 3DGS 方法，利用偏振线索来促进精确的反射分解，并实现重建对象在视觉上一致的重光照。具体而言，我们设计了一个偏振延迟渲染（PolarDR）过程来建模反射引起的偏振，并提出了一种自遮挡感知的环境贴图构建技术（GridMap）来解决非凸物体的间接光照问题。我们在多个合成和真实场景（包括仅具有部分偏振线索的场景）中进行了验证，结果表明 PhyGaP 不仅在重建反光 3D 物体的外观和表面法线方面表现出色（PSNR 平均提高约 2 dB，余弦距离平均提高 45.7%），而且实现了最先进的反演渲染和重光照能力。我们的代码将很快发布。

## Abstract
Recent advances in 3D Gaussian Splatting (3DGS) have demonstrated great success in modeling reflective 3D objects and their interaction with the environment via deferred rendering (DR). However, existing methods often struggle with correctly reconstructing physical attributes such as albedo and reflectance, and therefore they do not support high-fidelity relighting. Observing that this limitation stems from the lack of shape and material information in RGB images, we present PhyGaP, a physically-grounded 3DGS method that leverages polarization cues to facilitate precise reflection decomposition and visually consistent relighting of reconstructed objects. Specifically, we design a polarimetric deferred rendering (PolarDR) process to model polarization by reflection, and a self-occlusion-aware environment map building technique (GridMap) to resolve indirect lighting of non-convex objects. We validate on multiple synthetic and real-world scenes, including those featuring only partial polarization cues, that PhyGaP not only excels in reconstructing the appearance and surface normal of reflective 3D objects (~2 dB in PSNR and 45.7% in Cosine Distance better than existing RGB-based methods on average), but also achieves state-of-the-art inverse rendering and relighting capability. Our code will be released soon.

---

## 论文详细总结（自动生成）

以下是对论文《PhyGaP: Physically-Grounded Gaussians with Polarization Cues》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的 3D 高斯泼溅（3DGS）方法在处理高反光物体时，虽然能通过延迟渲染（DR）模拟外观，但由于 RGB 图像信息量有限，难以准确分解物体的**固有反照率（Albedo）**和**环境反射光**。这导致重建的模型在改变光照环境（重光照）时，会出现颜色偏移、阴影不自然或表面不连续等问题。
*   **核心问题**：如何在 3DGS 框架中利用偏振（Polarization）线索，实现物理一致的反射分解、精确的几何重建以及高保真的重光照。

### 2. 方法论
*   **核心思想**：将 2D 高斯（2DGS）作为几何基础，引入偏振物理模型（pBRDF）进行约束，并设计一种高效的间接光照处理机制。
*   **关键技术细节**：
    *   **偏振延迟渲染（PolarDR）**：将传统的渲染方程扩展为偏振形式。使用 **Stokes 向量** ($s_0, s_1, s_2, s_3$) 描述光线状态。通过偏振双向反射分布函数（pBRDF）建模镜面反射（产生强线偏振）和漫反射（偏振较弱且相位偏移 90°）。
    *   **物理属性建模**：每个高斯基元学习反照率 ($\lambda$)、折射率 ($\eta$)、表面法线 ($n$) 和粗糙度 ($r$)。
    *   **GridMap（自遮挡感知环境图）**：针对非凸物体的自遮挡和间接光照问题，在物体包围盒上放置多个锚点相机生成局部环境贴图（Local Cubemaps）。通过距离加权融合这些贴图来近似间接光，避免了昂贵的多反弹光线追踪。
    *   **损失函数**：结合了 RGB 损失、偏振损失（监督 Stokes 向量）、掩码损失、深度-法线一致性损失以及边缘感知法线平滑损失。

### 3. 实验设计
*   **数据集与场景**：
    *   **合成数据**：SMVP3D（squirrel, snail, david）、Mitsuba3 渲染场景（matpre, teapot）。
    *   **真实数据**：PANDORA（owl, vase）、RMVP3D（frog, dog）。
    *   **自定义采集**：使用双 RGB 相机加线性偏振片（LP）搭建的系统采集的真实场景（buddha, bull 等）。
*   **对比方法（Benchmark）**：
    *   **RGB 类 3DGS**：GS-IR, 3DGS-DR, R3DG, GIR, Ref-Gaussian。
    *   **偏振类方法**：PANDORA, NeRSP（基于 NeRF）；PolGS（并发的基于 3DGS 的方法）。
*   **评价指标**：PSNR, SSIM, LPIPS（图像质量）；余弦距离, MAE（法线精度）；Chamfer Distance（几何精度）。

### 4. 资源与算力
*   **硬件设备**：主要在 **NVIDIA RTX 4090D** GPU 上进行训练。
*   **训练时长**：PhyGaP 训练 30k 次迭代。作为对比，NeRF 类方法（如 PANDORA）在 V100 上需要 150k 次迭代。
*   **计算开销**：引入 GridMap 机制后，训练时间增加约 24.1%，推理时间增加约 26.8%。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 9 个主要测试场景，涉及合成与真实、全偏振与部分偏振等多种设置。
*   **充分性**：
    *   进行了详尽的**消融实验**，验证了 PolarDR（偏振约束）和 GridMap（间接光处理）对结果的贡献。
    *   对比了多种 SOTA 方法，不仅评估了视觉质量，还深入评估了法线精度和重光照效果。
    *   **客观性**：实验使用了公开数据集和标准指标，并展示了在部分偏振信息（更符合实际应用）下的鲁棒性，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能提升**：PhyGaP 在法线重建精度上比现有 RGB 方法平均提升 45.7%，PSNR 提升约 2 dB。
*   **反射分解**：偏振线索能有效区分漫反射和镜面反射，从而获得更准确的反照率贴图。
*   **重光照能力**：PhyGaP 是首个在偏振 3DGS 领域实现高质量重光照的方法，能够产生符合物理规律的阴影和高光。

### 7. 优点
*   **物理一致性强**：通过 pBRDF 将偏振物理特性引入 3DGS，解决了反光物体重建的固有歧义。
*   **重光照效果卓越**：GridMap 机制在不增加巨大计算负担的前提下，有效处理了非凸物体的复杂光照。
*   **硬件兼容性**：不仅支持昂贵的偏振相机，也证明了使用普通相机加偏振片的低成本方案的可行性。

### 8. 不足与局限
*   **材质覆盖有限**：目前的 pBRDF 模型主要针对电介质（非金属），对金属材质（涉及复数折射率和相位项）的建模尚不完善。
*   **几何依赖性**：GridMap 依赖于包围盒锚点，对于形状极度复杂或具有深孔的物体，间接光近似可能不够准确。
*   **环境假设**：环境贴图假设光照来自无穷远，在处理具有近距离光源的真实室内环境时可能存在偏差。

（完）
