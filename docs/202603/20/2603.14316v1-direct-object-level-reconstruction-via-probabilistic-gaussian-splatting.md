---
title: Direct Object-Level Reconstruction via Probabilistic Gaussian Splatting
title_zh: 基于概率高斯泼溅的直接物体级重建
authors: "Shuai Guo, Ao Guo, Junchao Zhao, Qi Chen, Yuxiang Qi, Zechuan Li, Dong Chen, Tianjia Shao, Mingliang Xu"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14316v1"
tags: ["query:3dgsir"]
score: 8.0
evidence: 使用2D高斯泼溅进行物体级3D重建
tldr: 针对现有Gaussian Splatting方法在单物体重建时存在背景冗余、计算开销大的问题，本文提出一种基于概率高斯喷溅的直接物体级重建方法。通过将前景概率集成到高斯原语中，并利用YOLO和SAM生成的概率掩码进行监督，结合动态剪枝和双阶段过滤策略，实现了仅需标准3DGS约1/10的高斯数量即可达到同等重建质量，显著提升了存储和计算效率。
selection_source: fresh_fetch
motivation: 现有的高斯喷溅方法通常进行全场景重建，导致在处理单物体时引入大量冗余背景信息，增加了计算和存储负担。
method: 将前景-背景概率直接集成到高斯原语中，利用YOLO和SAM生成的连续概率掩码进行监督，并配合动态剪枝和双阶段过滤策略。
result: "在MIP-360、T&T和NVOS数据集上的实验表明，该方法在保持高重建质量的同时，高斯数量仅为标准3DGS的1/10。"
conclusion: 该方法通过概率建模实现了高效且鲁棒的单物体3D重建，在保证高保真度的同时大幅降低了资源消耗。
---

## 摘要
物体级三维重建在文化遗产数字化、工业制造和虚拟现实等领域发挥着重要作用。然而，现有的基于高斯泼溅（Gaussian Splatting）的方法通常依赖于全场景重建，这引入了大量冗余的背景信息，导致计算和存储开销增加。为了解决这一局限性，我们提出了一种基于二维高斯泼溅（2D Gaussian Splatting）的高效单物体三维重建方法。通过将前景-背景概率线索直接集成到高斯基元中，并在训练过程中动态剪枝低概率高斯，该方法从根本上聚焦于感兴趣的物体，并提高了内存和计算效率。我们的流水线利用 YOLO 和 SAM 生成的概率掩码来监督概率高斯属性，用连续概率值取代二值掩码，以缓解边界模糊问题。此外，我们为训练启动阶段提出了一种双阶段过滤策略，以抑制背景高斯。并且，在训练过程中，渲染的概率掩码被反过来用于细化监督并增强跨视图的边界一致性。在 MIP-360、T&T 和 NVOS 数据集上进行的实验表明，我们的方法在存在掩码错误的情况下表现出强大的自纠错能力，并实现了与标准 3DGS 方法相当的重建质量，而所需的高斯数量仅为后者的约 1/10。这些结果验证了我们单物体重建方法的高效性和鲁棒性，并突显了其在需要高保真度和计算效率的应用中的潜力。

## Abstract
Object-level 3D reconstruction play important roles across domains such as cultural heritage digitization, industrial manufacturing, and virtual reality. However, existing Gaussian Splatting-based approaches generally rely on full-scene reconstruction, in which substantial redundant background information is introduced, leading to increased computational and storage overhead. To address this limitation, we propose an efficient single-object 3D reconstruction method based on 2D Gaussian Splatting. By directly integrating foreground-background probability cues into Gaussian primitives and dynamically pruning low-probability Gaussians during training, the proposed method fundamentally focuses on an object of interest and improves the memory and computational efficiency. Our pipeline leverages probability masks generated by YOLO and SAM to supervise probabilistic Gaussian attributes, replacing binary masks with continuous probability values to mitigate boundary ambiguity. Additionally, we propose a dual-stage filtering strategy for training's startup to suppress background Gaussians. And, during training, rendered probability masks are conversely employed to refine supervision and enhance boundary consistency across views. Experiments conducted on the MIP-360, T&T, and NVOS datasets demonstrate that our method exhibits strong self-correction capability in the presence of mask errors and achieves reconstruction quality comparable to standard 3DGS approaches, while requiring only approximately 1/10 of their Gaussian amount. These results validate the efficiency and robustness of our method for single-object reconstruction and highlight its potential for applications requiring both high fidelity and computational efficiency.

---

## 论文详细总结（自动生成）

### 论文总结：基于概率高斯泼溅的直接物体级重建

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 3D 高斯泼溅（3DGS）及其变体主要针对全场景重建。在仅需重建特定物体（如文物、工业零件）的应用中，全场景建模会引入大量冗余的背景信息，导致存储空间浪费和计算开销激增。
*   **研究动机**：传统的“先重建全场景再分割物体”模式效率低下。本文旨在提出一种**直接物体级重建**方法，在训练初期就过滤背景，使计算资源集中在目标物体上，从而实现轻量化、高效率的单物体建模。

#### 2. 核心方法论
该方法基于 **2D 高斯泼溅 (2DGS)** 框架，核心思想是将前景概率集成到高斯原语中，实现动态背景抑制。
*   **概率高斯建模**：为每个高斯原语增加一个可学习的概率属性 $p$，表示其属于目标物体的可能性。渲染时，除了颜色和深度，还会合成一张概率掩码图。
*   **数据准备与细化**：
    *   **概率掩码生成**：利用 YOLO 检测目标并配合 SAM 生成像素级概率掩码。不同于传统的二值掩码（0或1），本文使用 [0, 1] 之间的连续概率值以缓解边界模糊。
    *   **前景点云提取**：通过多视图投影一致性校验，从 SfM 初始点云中剔除背景点，仅保留高置信度的前景点用于初始化。
    *   **低质量样本过滤**：自动识别并剔除 YOLO/SAM 误检的视图（如背景中出现同类物体导致的错误分割）。
*   **损失函数设计**：结合了光度损失（由概率掩码加权）、深度畸变损失、法线一致性损失以及专门的**概率损失 ($L_p$)**。
*   **双阶段分割策略**：
    *   第一阶段（前 7000 次迭代）：使用 SAM 生成的掩码进行初步引导。
    *   第二阶段：使用模型自身渲染的概率掩码替代 SAM 掩码，利用多视图几何一致性修复 SAM 掩码的边界噪声，提升边缘锐度。

#### 3. 实验设计
*   **数据集**：MIP-360、Tanks & Temples (T&T)、NVOS、LLFF 以及一个使用 Blender 渲染的带有精确标注的合成数据集。
*   **Benchmark 与对比方法**：
    *   **重建质量对比**：与标准 3DGS 和 2DGS 对比。
    *   **分割与边界精度对比**：与 NVOS、ISRF、SA3D、SAGA 等“全场景重建后分割”的方法对比。
    *   **效率对比**：对比高斯原语数量（模型大小）和训练/推理耗时。

#### 4. 资源与算力
*   **硬件环境**：实验使用了 **NVIDIA RTX 3090 GPU**。
*   **训练配置**：总训练迭代次数为 30,000 次。在 15,000 次迭代后停止剪枝和致密化。
*   **耗时示例**：在 NVOS 数据集上，该方法总耗时约 1130 秒（约 19 分钟），显著快于需要全场景重建的方法（通常 > 1 小时）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了室内、室外、前向和 360 度等多种场景。
*   **消融实验**：非常充分。分别验证了“连续概率掩码 vs 二值掩码”、“概率掩码替换策略”、“数据细化（点云/视图过滤）”对结果的影响。
*   **客观性**：通过合成数据集提供了 mIoU 和 mAcc 的定量指标，并展示了在存在分割错误时的自纠错能力，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **极高的压缩率**：该方法所需的高斯原语数量仅为标准 3DGS 的 **1/10** 左右，约为 2DGS 的 1/4。
*   **重建质量持平**：在大幅减少参数量的前提下，目标物体的重建质量（PSNR/SSIM）与全场景重建方法相当。
*   **边界更锐利**：通过双阶段策略，渲染出的物体边缘比 SAM 原始分割掩码更清晰，具有更强的跨视图一致性。
*   **效率优势**：由于跳过了背景建模，训练速度和内存占用均得到显著优化。

#### 7. 优点
*   **高效性**：直接针对物体建模，避免了无效计算，非常适合移动端或资源受限的应用。
*   **鲁棒性**：对 2D 分割模型的错误（如 SAM 的模糊边界或 YOLO 的误检）具有很强的容错和自纠正能力。
*   **几何准确性**：基于 2DGS 框架，相比 3DGS 提供了更好的表面对齐和法线估计。

#### 8. 不足与局限
*   **材质限制**：对于具有强反射（镜面）或透明材质的物体，重建效果仍面临挑战（这是高斯泼溅类方法的通病）。
*   **依赖预训练模型**：虽然有纠错机制，但初始阶段仍高度依赖 YOLO 和 SAM 的检测/分割质量，若初始引导完全错误，模型可能难以收敛。
*   **应用范围**：主要针对单物体或特定感兴趣区域，不适用于需要保留环境背景的场景。

（完）
