---
title: "From ex(p) to poly: Gaussian Splatting with Polynomial Kernels"
title_zh: 从 ex(p) 到 poly：基于多项式核的高斯泼溅
authors: "Joerg H. Mueller, Martin Winter, Markus Steinberger"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18707v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 采用多项式核的3D高斯泼溅表示
tldr: "本研究针对3D高斯泼溅（3DGS）中内核改进导致的数据集不兼容问题，提出了一种新型多项式内核。该方法利用多项式近似结合ReLU函数替代传统的指数内核，在保持与现有数据集兼容的同时，实现了更高效的高斯剔除。实验表明，该改进在几乎不损失图像质量的情况下，将渲染性能提升了4%至15%，并为NPU硬件优化提供了新思路。"
selection_source: fresh_fetch
motivation: 旨在解决3DGS内核修改带来的数据集不兼容问题，并进一步提升计算效率。
method: 采用多项式近似结合ReLU函数替换原始的指数内核，从而优化高斯点的剔除过程。
result: "在不同3DGS实现中取得了4%到15%的性能提升，且对图像质量的影响微乎其微。"
conclusion: 该多项式内核在提升渲染效率的同时保证了兼容性，具有显著的硬件适配潜力。
---

## 摘要
高斯泼溅（3DGS）的最新进展对原始核函数引入了各种修改，从而显著提升了性能。然而，许多此类核函数的改动与针对原始高斯核优化的现有数据集不兼容，这为广泛应用带来了挑战。在这项工作中，我们通过提出一种替代核函数来应对这一挑战，该核函数在保持与现有数据集兼容性的同时，提高了计算效率。具体而言，我们将原始的指数核替换为结合了 ReLU 函数的多项式近似。这种修改允许对高斯分布进行更激进的剔除，从而在不同的 3DGS 实现中增强了性能。我们的结果显示，在对图像质量影响微乎其微的情况下，性能显著提升了 4% 到 15%。我们还提供了新核函数的详细数学分析，并讨论了其在 NPU 硬件上实现 3DGS 的潜在优势。

## Abstract
Recent advancements in Gaussian Splatting (3DGS) have introduced various modifications to the original kernel, resulting in significant performance improvements. However, many of these kernel changes are incompatible with existing datasets optimized for the original Gaussian kernel, presenting a challenge for widespread adoption. In this work, we address this challenge by proposing an alternative kernel that maintains compatibility with existing datasets while improving computational efficiency. Specifically, we replace the original exponential kernel with a polynomial approximation combined with a ReLU function. This modification allows for more aggressive culling of Gaussians, leading to enhanced performance across different 3DGS implementations. Our results show a notable performance improvement of 4 to 15% with negligible impact on image quality. We also provide a detailed mathematical analysis of the new kernel and discuss its potential benefits for 3DGS implementations on NPU hardware.

---

## 论文详细总结（自动生成）

这篇论文由华为技术有限公司与格拉茨工业大学的研究人员共同完成，旨在通过数学近似手段提升 3D 高斯泼溅（3DGS）的渲染效率。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：原始 3DGS 使用指数核函数（Exponential Kernel）来计算每个高斯点的贡献，但在渲染过程中，指数函数的计算开销较大（即使使用低精度硬件单元）。此外，现有的许多改进内核的方法往往与已有的 3DGS 数据集不兼容，需要重新训练。
*   **研究动机**：提出一种既能显著提升渲染速度，又能直接应用于现有已训练数据集（无需重训）的替代内核，同时探索其在 NPU 等硬件上的加速潜力。

### 2. 方法论
*   **核心思想**：将原始的指数核 $g(x) = \exp(-1/2 x)$ 替换为带有 ReLU 激活的 $N$ 阶多项式近似 $f_N(x) = \max(\sum_{i=0}^{N} c_i x^i, 0)$。
*   **关键技术细节**：
    *   **多项式拟合**：在 $x \in [0, -2\ln(1/255)]$ 范围内，通过 $L_1$ 损失函数和梯度下降法优化多项式系数 $c_i$。研究发现一阶多项式（线性近似）在性能与质量之间取得了最佳平衡。
    *   **更激进的剔除（Culling）**：由于多项式具有有限支撑集（在根处变为 0），研究者推导出了比原始 3DGS 更紧凑的通用边界半径和不透明度相关边界半径，从而大幅减少需要处理的 Tile 和高斯点数量。
    *   **抗混叠不变性证明**：从数学上证明了抗混叠归一化因子对于任意核函数都是通用的，确保了新内核在不同视角下的稳定性。
    *   **硬件适配**：讨论了该方法如何利用 NPU 的矩阵乘法和 ReLU 硬件单元实现高效并行计算。

### 3. 实验设计
*   **数据集**：使用了 3DGS 领域的标准 Benchmark，包括 **Mip-NeRF 360**、**Tanks and Temples** 以及 **Deep Blending**。
*   **对比基准**：以原始 3DGS（结合了 StopThePop 的剔除优化）作为 Baseline。
*   **对比方法/实现**：在 5 种不同的 3DGS 实现中集成了该内核进行测试：
    1.  **Baseline** (CUDA)
    2.  **gsplat** (高性能库)
    3.  **Faster-GS** (进一步优化的 CUDA 实现)
    4.  **vk_gaussian_splatting** (Vulkan API)
    5.  **MetalSplatter** (Apple Metal API)

### 4. 资源与算力
*   **硬件型号**：实验主要在配备 **NVIDIA RTX 5090** GPU 的工作站上进行；针对 Metal 实现的测试在 **Apple M1 Ultra** 上进行。
*   **训练/优化时长**：遵循标准协议，场景在原始指数核下训练 **30,000 次迭代**，随后直接替换内核进行推理评估。文中也提到了直接使用多项式核进行训练的对比实验。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 13 个主流场景，并在 5 种不同的渲染引擎/API 上进行了交叉验证。
*   **消融实验**：详细对比了一阶、二阶、三阶多项式的质量与性能差异，并分别测试了“仅更换内核”与“更换内核+优化剔除”的效果。
*   **充分性评价**：实验设计非常充分且客观。通过在多个第三方开源库中集成该方法，证明了其通用性和鲁棒性。

### 6. 主要结论与发现
*   **性能提升**：在几乎不损失图像质量的前提下，渲染性能提升了 **4% 至 15%**（在某些实现中甚至达到 29%）。
*   **质量保持**：三阶多项式几乎可以完美复现指数核的质量；一阶多项式虽有轻微指标下降，但视觉差异极小。
*   **剔除贡献**：性能提升的主要驱动力来自于多项式核带来的更紧凑的边界剔除，而非单纯的函数计算简化。

### 7. 优点
*   **即插即用（Drop-in Replacement）**：无需对现有数据集进行重新训练即可直接提速。
*   **数学严谨性**：提供了关于抗混叠和边界半径推导的详尽数学证明。
*   **跨平台潜力**：不仅限于 CUDA，在 Vulkan 和 Metal 上的成功验证展示了其在移动端和嵌入式硬件上的应用前景。

### 8. 不足与局限
*   **高亮度伪影**：在某些极端高亮度场景（如白色天空）中，由于颜色值未归一化且多项式截断较快，可能会出现微小的暗点伪影。
*   **性能边际效应**：在已经高度优化（如使用硬件加速指数指令）的框架中，单纯替换函数的收益会减小，更多依赖于剔除策略。
*   **阶数限制**：高阶多项式虽然质量更高，但计算开销会迅速抵消剔除带来的收益，因此应用范围主要局限于低阶多项式。

（完）
