---
title: "tttLRM: Test-Time Training for Long Context and Autoregressive 3D Reconstruction"
title_zh: tttLRM：用于长上下文和自回归 3D 重建的测试时训练
authors: "Chen Wang, Hao Tan, Wang Yifan, Zhiqin Chen, Yuheng Liu, Kalyan Sunkavalli, Sai Bi, Lingjie Liu, Yiwei Hu"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20160v1"
tags: ["query:3dgsir"]
score: 8.0
evidence: 自回归3D重建并解码为高斯泼溅
tldr: 本研究提出tttLRM，一种利用测试时训练（TTT）层的大型3D重建模型，旨在解决长上下文和自回归3D重建中的计算复杂度问题。该模型通过将多视图图像观测高效压缩进TTT层的快速权重中，形成隐式3D表示，并可解码为高斯泼溅（GS）等显式格式。其支持流式观测下的渐进式重建与细化，在物体和场景的3D重建任务中均表现出优于现有技术的性能和更快的收敛速度。
selection_source: carryover_cache
motivation: 旨在克服传统3D重建模型在处理长上下文序列时计算复杂度高且难以有效融合多视图信息的问题。
method: 引入TTT层将图像观测高效压缩为隐式潜在表示，并结合自回归机制和线性复杂度架构实现显式3D格式解码。
result: 实验证明该方法在物体和场景的3D高斯重建质量及收敛速度上均显著优于当前最先进的算法。
conclusion: tttLRM通过TTT层实现了高效的长上下文3D重建，为大规模、流式3D场景建模提供了有力的新方案。
---

## 摘要
我们提出了 tttLRM，这是一种新型的大型 3D 重建模型，它利用测试时训练（TTT）层实现了具有线性计算复杂度的长上下文、自回归 3D 重建，从而进一步扩展了模型的能力。我们的框架有效地将多个图像观测压缩到 TTT 层的快速权重中，在潜空间中形成隐式 3D 表示，该表示可以解码为各种显式格式，例如用于下游应用的 3D 高斯泼溅（GS）。我们模型的在线学习变体支持从流式观测中进行渐进式 3D 重建和精细化。我们证明了在新视图合成任务上的预训练可以有效地迁移到显式 3D 建模，从而提高重建质量并加快收敛速度。大量的实验表明，与现有最先进的方法相比，我们的方法在物体和场景的前馈 3D 高斯重建中均取得了优越的性能。

## Abstract
We propose tttLRM, a novel large 3D reconstruction model that leverages a Test-Time Training (TTT) layer to enable long-context, autoregressive 3D reconstruction with linear computational complexity, further scaling the model's capability. Our framework efficiently compresses multiple image observations into the fast weights of the TTT layer, forming an implicit 3D representation in the latent space that can be decoded into various explicit formats, such as Gaussian Splats (GS) for downstream applications. The online learning variant of our model supports progressive 3D reconstruction and refinement from streaming observations. We demonstrate that pretraining on novel view synthesis tasks effectively transfers to explicit 3D modeling, resulting in improved reconstruction quality and faster convergence. Extensive experiments show that our method achieves superior performance in feedforward 3D Gaussian reconstruction compared to state-of-the-art approaches on both objects and scenes.

---

## 论文详细总结（自动生成）

### tttLRM: 用于长上下文和自回归 3D 重建的测试时训练 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的 3D 重建模型（如 LRM）在处理多视图输入时，通常受限于 Transformer 注意力机制的**二次方计算复杂度**，导致其难以扩展到长序列（如超过 32 视图）或处理高分辨率图像。此外，现有模型大多是离线处理，缺乏像人类视觉系统那样从连续流式观测中逐步构建和细化 3D 表示的能力。
**tttLRM 的核心目标**是利用**测试时训练（Test-Time Training, TTT）**技术，构建一个具有**线性复杂度**的大型重建模型，支持长上下文（64+ 视图）输入和自回归流式重建，同时保持高质量的显式 3D 输出（如 3D 高斯泼溅）。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：将 TTT 层的“快速权重”（Fast Weights）视为一种隐式的 3D 潜空间存储器。输入图像序列被视为训练数据，用于在线更新这些权重，从而将场景信息压缩进网络参数中。
*   **关键技术细节**：
    *   **LaCT 模块**：采用大块测试时训练（Large Chunk TTT）层，通过 Muon 优化器在推理过程中更新权重。其计算复杂度随序列长度线性增长。
    *   **隐式到显式的解码**：模型首先通过输入视图更新快速权重 $W$，然后使用一组“虚拟视图标记”（Virtual Tokens）作为查询（Query），从 $W$ 中提取特征，最后通过线性解码器生成 3D 高斯（3DGS）参数（位置、颜色、不透明度、旋转、缩放）。
    *   **自回归重建**：通过修改更新机制，使模型能以 RNN 的方式处理流式输入，每输入一组新视图即更新一次内部状态并输出当前的重建结果。
    *   **序列并行化**：为了处理超长序列，引入了分布式训练方案，将标记分片到不同 GPU 上，通过同步梯度确保全局优化。

#### 3. 实验设计
*   **数据集**：
    *   **物体级**：Objaverse（730K 物体用于训练），Google Scanned Objects (GSO) 用于测试。
    *   **场景级**：DL3DV-10K（大规模视频场景），Tanks&Temples（用于验证泛化性）。
*   **Benchmark 与对比方法**：
    *   **前馈模型**：GS-LRM（基于注意力机制）、Long-LRM（当前最强的长序列模型）。
    *   **优化模型**：3DGS、Mip-Splatting、Scaffold-GS。
*   **评价指标**：PSNR、SSIM、LPIPS。

#### 4. 资源与算力
*   **硬件设施**：使用了 **64 台 NVIDIA A100 (80GB) GPU** 进行大规模分布式训练。
*   **训练细节**：采用了课程学习策略，从低分辨率（144x256）逐步训练到高分辨率（540x960）。第一阶段在 0.3T Token 上训练了 80K 步。
*   **推理效率**：在处理 256 视图（约 2M Token）时，24 层 LaCT 模块的速度远快于 3 层传统的注意力层。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了物体级（512px/1024px）和场景级（16/32/64 视图）的广泛实验。
*   **消融实验**：
    1.  **预训练效果**：验证了从新视图合成任务（LVSM）迁移权重能显著加快收敛并提升质量。
    2.  **自回归策略**：对比了“预测并合并”与“全量更新”策略，证明了 TTT 更新在纠正累积误差方面的必要性。
    3.  **优化器与正则化**：测试了 Muon 优化器及深度/不透明度损失对结果的影响。
*   **客观性**：实验对比了不同视图数量下的表现，并跨数据集（Tanks&Temples）验证了泛化能力，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **线性扩展性**：tttLRM 成功打破了注意力机制的瓶颈，在处理长上下文时表现出极高的计算效率。
*   **性能领先**：在 DL3DV 和 GSO 数据集上，tttLRM 的重建质量（PSNR）显著优于 Long-LRM 和 GS-LRM，且在 16/32 视图下甚至优于部分需要长时间优化的传统方法。
*   **自回归优势**：模型能够随着观测视角的增加，动态地完善 3D 场景的覆盖范围和细节。
*   **表示灵活性**：该框架不仅能生成 3DGS，还能通过更换查询标记生成 Triplane（三平面）等其他 3D 格式。

#### 7. 优点（亮点）
*   **首创性**：首次将 TTT 引入大型 3D 重建模型，实现了长上下文和自回归建模的统一。
*   **高分辨率支持**：支持高达 1024px 的单图转 3D 重建，细节表现力强（如毛发、文字）。
*   **知识迁移**：证明了隐式视图合成任务的预训练知识可以无缝迁移到显式 3D 几何重建中。
*   **分布式友好**：设计的序列并行方案解决了超长序列训练的显存溢出问题。

#### 8. 不足与局限
*   **内存容量限制**：快速权重的容量是固定的，在处理极其复杂的超大规模场景时，可能会出现“遗忘”早期观测信息的情况。
*   **质量折损**：相比于纯隐式的神经渲染模型，解码为显式 3DGS 过程中仍存在轻微的质量损失（隐式与显式表示的固有权衡）。
*   **推理延迟**：虽然具有线性复杂度，但在自回归模式下，频繁更新快速权重仍会带来一定的计算开销，尚未完全达到超高帧率的实时重建。

（完）
