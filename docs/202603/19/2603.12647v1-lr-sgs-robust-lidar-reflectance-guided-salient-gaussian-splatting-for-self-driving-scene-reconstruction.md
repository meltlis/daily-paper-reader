---
title: "LR-SGS: Robust LiDAR-Reflectance-Guided Salient Gaussian Splatting for Self-Driving Scene Reconstruction"
title_zh: LR-SGS：用于自动驾驶场景重建的鲁棒激光雷达反射率引导显著高斯泼溅方法
authors: "Ziyu Chen, Fan Zhu, Hui Zhu, Deyi Kong, Xinkai Kuang, Yujia Zhang, Chunmao Jiang"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12647v1"
tags: ["query:3dgsir"]
score: 8.0
evidence: 激光雷达反射率引导的高斯泼溅处理复杂光照
tldr: 针对自动驾驶场景重建中现有3DGS方法对LiDAR信息利用不足、在复杂光照下表现不佳的问题，本文提出LR-SGS。该方法引入结构感知的显著高斯表示，利用LiDAR几何与反射率特征进行初始化，并将校准后的反射率作为光照不变特征与RGB联合对齐。实验证明，该方法在Waymo数据集上实现了更高的重建精度、更少的高斯数量和更短的训练时间，显著提升了复杂场景下的鲁棒性。
selection_source: fresh_fetch
motivation: 现有方法未能充分挖掘LiDAR反射率信息及LiDAR与RGB的互补性，导致在剧烈运动和复杂光照场景下重建质量下降。
method: 提出显著高斯表示，利用LiDAR几何与反射率特征初始化并优化结构，同时引入校准后的反射率作为光照不变的材质通道以增强边界一致性。
result: 在Waymo数据集上，LR-SGS在复杂光照场景下比OmniRe提升了1.18 dB PSNR，且具有更少的高斯参数和更快的训练速度。
conclusion: LR-SGS通过有效整合LiDAR反射率与几何特征，为自动驾驶场景提供了一种高效且鲁棒的3D重建新方案。
---

## 摘要
最近的 3D 高斯泼溅 (3DGS) 方法已经证明了自动驾驶场景重建和新视角合成的可行性。然而，大多数现有方法要么仅依赖摄像头，要么仅将激光雷达 (LiDAR) 用于高斯初始化或深度监督，而点云中包含的丰富场景信息（如反射率）以及 LiDAR 与 RGB 之间的互补性尚未得到充分利用，导致在具有高自运动和复杂光照等挑战性的自动驾驶场景中性能下降。为了解决这些问题，我们提出了一种用于自动驾驶场景的鲁棒且高效的激光雷达反射率引导显著高斯泼溅方法 (LR-SGS)，该方法引入了一种结构感知的显著高斯表示，其由从 LiDAR 提取的几何和反射率特征点初始化，并通过显著变换和改进的密度控制进行细化，以捕捉边缘和平面结构。此外，我们将 LiDAR 强度校准为反射率，并将其作为光照不变的材料通道附加到每个高斯分布上，与 RGB 联合对齐以增强边界一致性。在 Waymo 开放数据集上的大量实验表明，LR-SGS 以更少的高斯数量和更短的训练时间实现了卓越的重建性能。特别是在复杂光照场景下，我们的方法在 PSNR 指标上超过了 OmniRe 1.18 dB。

## Abstract
Recent 3D Gaussian Splatting (3DGS) methods have demonstrated the feasibility of self-driving scene reconstruction and novel view synthesis. However, most existing methods either rely solely on cameras or use LiDAR only for Gaussian initialization or depth supervision, while the rich scene information contained in point clouds, such as reflectance, and the complementarity between LiDAR and RGB have not been fully exploited, leading to degradation in challenging self-driving scenes, such as those with high ego-motion and complex lighting. To address these issues, we propose a robust and efficient LiDAR-reflectance-guided Salient Gaussian Splatting method (LR-SGS) for self-driving scenes, which introduces a structure-aware Salient Gaussian representation, initialized from geometric and reflectance feature points extracted from LiDAR and refined through a salient transform and improved density control to capture edge and planar structures. Furthermore, we calibrate LiDAR intensity into reflectance and attach it to each Gaussian as a lighting-invariant material channel, jointly aligned with RGB to enforce boundary consistency. Extensive experiments on the Waymo Open Dataset demonstrate that LR-SGS achieves superior reconstruction performance with fewer Gaussians and shorter training time. In particular, on Complex Lighting scenes, our method surpasses OmniRe by 1.18 dB PSNR.

---

## 论文详细总结（自动生成）

以下是对论文《LR-SGS: Robust LiDAR-Reflectance-Guided Salient Gaussian Splatting for Self-Driving Scene Reconstruction》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 高斯泼溅（3DGS）方法在自动驾驶场景重建中，往往仅将 LiDAR 用于初始点云提供或简单的深度监督，未能充分挖掘 LiDAR 点云中蕴含的**反射率（Reflectance）信息**以及 LiDAR 与 RGB 图像之间的**多模态互补性**。
*   **研究动机**：在高速运动、复杂光照（如夜间、强光）等挑战性场景下，纯视觉方法容易出现纹理不一致和优化不稳定。LiDAR 提供的反射率具有光照不变性，且其几何特征能提供强有力的结构约束，这对于提升重建的鲁棒性和精度至关重要。

### 2. 方法论
LR-SGS 的核心思想是构建一个由 LiDAR 反射率引导的结构感知型高斯表示。
*   **LiDAR 强度校准**：将原始 LiDAR 强度（Intensity）校准为与距离和入射角无关的反射率（Reflectance），作为高斯球的一个额外属性通道，提供光照不变的材质特征。
*   **显著高斯表示（Salient Gaussians）**：
    *   **结构感知**：针对边缘（Edge）和平面（Planar）结构，将高斯球的三个缩放参数简化为一个主导缩放方向和两个共享的非主导缩放方向，在减少参数量的同时更精准地拟合环境轮廓。
    *   **特征初始化**：利用 LiDAR 的几何平滑度提取边缘点和平面点，并结合反射率梯度提取材质边界点，共同初始化显著高斯。
    *   **显著变换（Salient Transform）**：在训练过程中，根据高斯球的线性和平面度指标，动态地在“显著”与“非显著”状态间切换，确保关键结构得到重点建模。
*   **联合损失函数（Joint Loss）**：
    *   引入**梯度方向一致性**和**梯度幅值一致性**损失，强制要求渲染的反射率图与 RGB 灰度图在材质边界处对齐，从而消除模糊并增强边界清晰度。

### 3. 实验设计
*   **数据集**：使用 **Waymo Open Dataset**。
*   **场景分类**：选取了 24 个序列，涵盖四类典型挑战场景：密集交通（Dense Traffic）、高速行驶（High-Speed）、复杂光照（Complex Lighting）和静态场景（Static）。
*   **对比方法（Baselines）**：包括 3DGS、DeformGS、PVG、StreetGS 和 OmniRe 等 SOTA 方法。
*   **评价指标**：RGB 渲染使用 PSNR、SSIM、LPIPS；反射率渲染使用 RMSE；针对动态物体使用 PSNR*。

### 4. 资源与算力
*   **训练配置**：所有实验均进行 30k 次迭代。
*   **训练时长**：文中提到在特定序列上，LR-SGS 的训练时间约为 **61 分钟**（相比之下，StreetGS 约 64 分钟，OmniRe 约 67 分钟）。
*   **算力设备**：论文**未明确说明**具体的 GPU 型号和数量，但从训练时长推断，应为单张高性能专业级显卡（如 A100 或 RTX 3090/4090 级别）。

### 5. 实验数量与充分性
*   **实验规模**：在 24 个不同类型的真实世界序列上进行了测试，每类场景包含 6 个序列，实验覆盖面较广。
*   **消融实验**：针对显著高斯（SG）、LiDAR 特征初始化（LF-Init）、反射率通道以及联合损失（Joint Loss）分别做了详细的消融研究。
*   **客观性**：通过定量指标（表 I、II、III）和定性视觉对比（图 4、5、6）多维度验证，且每组实验取 5 次运行的平均值，表现出较高的客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：LR-SGS 在所有场景类别中均表现优异，特别是在**复杂光照**下，PSNR 比 OmniRe 提升了 **1.18 dB**。
*   **效率提升**：由于显著高斯减少了冗余参数并优化了初始化，LR-SGS 使用更少的高斯数量（减少约 10%-15%）实现了更高的渲染 FPS（36.95 vs 基准的 30.55）。
*   **结构恢复**：显著高斯表示能有效恢复环境中的细微结构（如车道线、墙壁凸起），即使在 LiDAR 未覆盖的区域，通过显著变换策略也能实现良好的结构外推。

### 7. 优点（亮点）
*   **多模态深度融合**：不只是简单使用 LiDAR 深度，而是深度挖掘了反射率这一物理属性，解决了光照剧烈变化下的重建难题。
*   **参数效率高**：通过结构感知的参数共享机制，在提升质量的同时降低了存储和计算开销。
*   **初始化策略明智**：利用几何和反射率双重特征点进行初始化，显著加快了收敛速度。

### 8. 不足与局限
*   **LiDAR 依赖性**：虽然引入了 SfM 点云补充，但核心优势仍高度依赖高质量的 LiDAR 数据。在 LiDAR 稀疏或缺失的区域，性能提升可能受限。
*   **校准复杂性**：LiDAR 强度到反射率的校准依赖于精确的距离和入射角计算，在极端天气（如大雨、浓雾）导致 LiDAR 散射严重时，反射率的可靠性可能会下降。
*   **场景规模限制**：目前的实验主要集中在约 100 帧的短序列，对于超大规模城市级场景的扩展性仍有待进一步验证。

（完）
