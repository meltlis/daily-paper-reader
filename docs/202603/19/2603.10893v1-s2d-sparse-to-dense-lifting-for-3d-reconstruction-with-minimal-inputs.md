---
title: "S2D: Sparse to Dense Lifting for 3D Reconstruction with Minimal Inputs"
title_zh: S2D：面向极简输入 3D 重建的稀疏到稠密提升
authors: "Yuzhou Ji, Qijian Tian, He Zhu, Xiaoqi Jiang, Guangzhi Cao, Lizhuang Ma, Yuan Xie, Xin Tan"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10893v1"
tags: ["query:3dgsir"]
score: 8.0
evidence: 从稀疏输入中进行高质量3DGS重建
tldr: 针对点云和3D高斯泼溅（3DGS）在稀疏输入下渲染效果差、重建质量下降的问题，本文提出S2D（Sparse to Dense lifting）框架。该框架通过高效的一步扩散模型修复稀疏点云产生的图像伪影，并结合随机样本丢弃和加权梯度的重建策略，实现了从极少数输入视角到稠密新视角的稳健拟合。S2D在保持生成一致性和提升重建质量方面表现优异，显著降低了3DGS应用对输入数据量的需求。
selection_source: fresh_fetch
motivation: 现有的3D表示方法在输入视角极少时，往往面临渲染不真实和重建质量严重退化的问题。
method: 提出一种包含一步扩散模型修复伪影以及采用随机样本丢弃与加权梯度优化策略的S2D提升框架。
result: 实验证明S2D在不同稀疏度下均能生成高一致性的新视角引导，并达到顶尖的重建质量。
conclusion: S2D通过极简的输入实现了高质量的3DGS重建，为低成本、高效率的3D场景建模提供了新方案。
---

## 摘要
显式 3D 表示已成为 3D 模拟与理解的重要媒介。然而，最常用的点云和 3D 高斯泼溅 (3DGS) 分别面临非写实渲染以及在稀疏输入下性能显著下降的问题。在本文中，我们提出了“稀疏到稠密提升”(S2D)，这是一种连接这两种表示的新型流水线，能够以极简输入实现高质量的 3DGS 重建。具体而言，S2D 提升包含两个方面：首先，我们提出了一种高效的一步扩散模型，通过提升稀疏点云来进行高保真图像伪影修复；同时，为了重建 3D 一致性场景，我们还设计了相应的重建策略，利用随机样本丢弃和加权梯度，实现从稀疏输入视角到稠密新视角的鲁棒模型拟合。大量实验表明，在不同的输入稀疏度下，S2D 在生成新视角引导方面达到了最佳的一致性，并具备顶尖的稀疏视角重建质量。通过以现有方法中尽可能少的采集量重建稳定场景，S2D 为 3DGS 应用实现了极简的输入需求。

## Abstract
Explicit 3D representations have already become an essential medium for 3D simulation and understanding. However, the most commonly used point cloud and 3D Gaussian Splatting (3DGS) each suffer from non-photorealistic rendering and significant degradation under sparse inputs. In this paper, we introduce Sparse to Dense lifting (S2D), a novel pipeline that bridges the two representations and achieves high-quality 3DGS reconstruction with minimal inputs. Specifically, the S2D lifting is two-fold. We first present an efficient one-step diffusion model that lifts sparse point cloud for high-fidelity image artifact fixing. Meanwhile, to reconstruct 3D consistent scenes, we also design a corresponding reconstruction strategy with random sample drop and weighted gradient for robust model fitting from sparse input views to dense novel views. Extensive experiments show that S2D achieves the best consistency in generating novel view guidance and first-tier sparse view reconstruction quality under different input sparsity. By reconstructing stable scenes with the least possible captures among existing methods, S2D enables minimal input requirements for 3DGS applications.

---

## 论文详细总结（自动生成）

### S2D: 面向极简输入 3D 重建的稀疏到稠密提升论文总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：3D 高斯泼溅（3DGS）在密集视角输入下表现优异，但在极稀疏输入（如仅 1-6 张图像）时，渲染质量会因视角偏差产生严重的伪影和退化。
*   **核心问题**：如何在极少量的输入下，重建出具备高保真度、3D 一致性且支持大范围视角外推的 3D 场景。
*   **整体含义**：论文提出了 **S2D (Sparse to Dense)** 框架，通过结合视觉大模型（VFM）生成的点云结构信息与扩散模型的生成能力，将稀疏的输入“提升”为稠密且一致的新视角引导，从而实现高质量的 3DGS 重建。

#### 2. 论文提出的方法论
*   **核心思想**：利用点云提供结构约束，利用扩散模型修复图像伪影，并通过改进的优化策略确保 3D 一致性。
*   **关键技术细节**：
    *   **一步扩散修复模型 (One-step Artifact Fixer)**：基于 `pix2pix-turbo` 构建，通过单步推理实现高效修复。
    *   **双重引导 (Dual Guidance)**：修复模型同时接收“点云渲染图”（提供结构骨架）和“邻近参考图”（提供纹理细节）作为引导。
    *   **混合模块 (Mixing Module)**：利用 DINO 特征和图像特征进行交叉注意力融合，决定如何结合点云和参考图的信息。
    *   **随机样本丢弃 (Random Sample Drop)**：在优化 3DGS 时，通过概率采样平衡原始输入视角与生成的新视角引导，防止模型过拟合于生成图中的不一致细节。
    *   **加权梯度 (Weighted Gradient)**：根据点云投影的置信度掩码（Confidence Mask）对像素级梯度进行加权，降低伪影区域对模型训练的负面影响。

#### 3. 实验设计
*   **数据集/场景**：
    *   **室内外场景**：3DOVS（单图输入）、RE10K（2 图）、MIP360（6 图）、DL3DV（6 图）。
    *   **自动驾驶场景**：Waymo Open Dataset、NuScenes。
*   **Benchmark 与对比方法**：
    *   **传统/优化类**：3DGS, Mip-Splatting, ScaffoldGS。
    *   **前馈预测类**：NoPoSplat, AnySplat, DepthSplat。
    *   **生成/修复类**：ViewCrafter, SEVA, DIFIX (SOTA 场景增强器)。
*   **评价指标**：PSNR, SSIM, LPIPS, FID。

#### 4. 资源与算力
*   **数据准备**：处理 DL3DV 数据集生成配对训练数据耗费了 **850 H200 GPU 小时**。
*   **模型训练**：修复模型在 **8 台 H200 GPU** 上训练了 **60 小时**（Batch Size 为 4）。
*   **推理与评估**：在单张 **RTX 4090** 上进行，修复速度约为 1 FPS，显存占用约 11.1 GB。
*   **对比说明**：其修复速度远快于基于视频扩散的方法（如 SEVA 的 0.08 FPS）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从单图到多图、从室内到室外及驾驶场景的多种配置。
*   **消融实验**：针对双重引导机制、混合模块位置、损失函数项（DINO, SSIM 等）、重建策略参数（$\alpha, \beta$）均做了详细对比。
*   **客观性**：实验采用了标准的 3D 重建评估流程（测试未见过的真实帧），而非仅评估生成的图像质量，对比基准涵盖了最新的前馈模型和生成模型，具有较高的说服力。

#### 6. 论文的主要结论与发现
*   **性能领先**：S2D 在极稀疏输入下的重建质量显著优于现有前馈模型和基于扩散的修复模型（如 DIFIX）。
*   **结构一致性**：引入点云作为结构引导是解决大视角偏差下伪影问题的关键，比单纯依赖邻近视角参考更稳健。
*   **策略有效性**：随机样本丢弃和加权梯度策略能有效缓解生成引导图中的细微不一致性，确保最终 3DGS 模型的稳定性。

#### 7. 优点
*   **灵活性高**：不限制输入图像的数量，支持从单图到稠密输入的平滑过渡。
*   **效率卓越**：采用一步扩散模型，相比多步迭代的扩散模型，大幅降低了重建过程中的计算开销。
*   **鲁棒性强**：通过点云引导，解决了以往生成式方法在长序列或大视角变换下容易产生“漂浮物”或结构崩溃的问题。

#### 8. 不足与局限
*   **VFM 依赖性**：由于第一步依赖视觉大模型（VFM）生成点云，若 VFM 在极低纹理或极端光照下失效，S2D 的结构引导也会受损。
*   **累积误差**：虽然有修复机制，但在极长距离的外推中，点云的累积误差仍可能导致渲染质量下降。
*   **应用限制**：目前主要针对静态场景，虽然在驾驶场景有测试，但对于复杂动态物体的处理能力尚未在文中深入探讨。

（完）
