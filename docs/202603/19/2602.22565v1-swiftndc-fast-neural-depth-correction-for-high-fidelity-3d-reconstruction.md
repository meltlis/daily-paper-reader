---
title: "SwiftNDC: Fast Neural Depth Correction for High-Fidelity 3D Reconstruction"
title_zh: SwiftNDC：用于高保真三维重建的快速神经深度修正
authors: "Kang Han, Wei Xiang, Lu Yu, Mathew Wyatt, Gaowen Liu, Ramana Rao Kompella"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22565v1"
tags: ["query:3dgsir"]
score: 8.0
evidence: 加速用于高保真网格重建的3DGS
tldr: SwiftNDC是一个快速通用的神经深度校正框架，旨在解决深度引导3D重建中的尺度漂移和多视图不一致问题。它通过神经深度校正场生成一致的深度图，并利用重投影误差过滤构建高质量点云，为3D高斯泼溅（3DGS）提供可靠的几何初始化。该方法显著缩短了网格重建的优化时间，并提升了新视角合成的渲染质量，实现了高效且高保真度的3D重建。
selection_source: carryover_cache
motivation: 现有的深度引导3D重建方法受限于尺度漂移和多视图不一致性，且需要繁重的优化过程才能获得高精度几何。
method: 通过神经深度校正场生成一致性深度图，并结合反投影与重投影误差过滤构建稠密点云，为后续3DGS优化提供几何初始化。
result: 在五个数据集上的实验证明，该方法显著加快了网格重建速度，并提升了新视角合成的渲染保真度。
conclusion: 将神经深度细化与鲁棒的几何初始化相结合，是实现高效、高质量3D重建的有效途径。
---

## 摘要
深度引导的三维重建作为重优化方法的快速替代方案已广受欢迎，但现有方法仍面临尺度漂移、多视图不一致以及需要大量精细化处理才能实现高保真几何的问题。在本文中，我们提出了 SwiftNDC，这是一个围绕神经深度修正场（Neural Depth Correction field）构建的快速通用框架，能够生成跨视图一致的深度图。基于这些修正后的深度，我们通过反投影和鲁棒的重投影误差过滤生成稠密点云，从而为下游重建获得干净且分布均匀的几何初始化。这种可靠的稠密几何显著加速了用于网格重建的 3D 高斯泼溅（3DGS），能够以更少的优化迭代次数实现高质量表面。对于新视角合成，SwiftNDC 还能提高 3DGS 的渲染质量，凸显了强大几何初始化的优势。我们在五个数据集上进行了全面研究，包括两个用于网格重建的数据集和三个用于新视角合成的数据集。SwiftNDC 一致地缩短了精确网格重建的运行时间，并提升了视角合成的渲染保真度，证明了将神经深度精细化与鲁棒几何初始化相结合对于高保真且高效的三维重建的有效性。

## Abstract
Depth-guided 3D reconstruction has gained popularity as a fast alternative to optimization-heavy approaches, yet existing methods still suffer from scale drift, multi-view inconsistencies, and the need for substantial refinement to achieve high-fidelity geometry. Here, we propose SwiftNDC, a fast and general framework built around a Neural Depth Correction field that produces cross-view consistent depth maps. From these refined depths, we generate a dense point cloud through back-projection and robust reprojection-error filtering, obtaining a clean and uniformly distributed geometric initialization for downstream reconstruction. This reliable dense geometry substantially accelerates 3D Gaussian Splatting (3DGS) for mesh reconstruction, enabling high-quality surfaces with significantly fewer optimization iterations. For novel-view synthesis, SwiftNDC can also improve 3DGS rendering quality, highlighting the benefits of strong geometric initialization. We conduct a comprehensive study across five datasets, including two for mesh reconstruction, as well as three for novel-view synthesis. SwiftNDC consistently reduces running time for accurate mesh reconstruction and boosts rendering fidelity for view synthesis, demonstrating the effectiveness of combining neural depth refinement with robust geometric initialization for high-fidelity and efficient 3D reconstruction.

---

## 论文详细总结（自动生成）

以下是对论文《SwiftNDC: Fast Neural Depth Correction for High-Fidelity 3D Reconstruction》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：高质量三维重建（如 NeRF 或 3DGS）通常依赖繁重的逐场景优化，耗时巨大。虽然前馈深度估计网络（如 MVSNet, VGGT）速度快，但普遍存在尺度漂移、局部偏差和多视图不一致性。
*   **核心问题**：如何快速获得跨视图一致且高精度的稠密几何信息，以解决深度引导重建中的表面粗糙、空洞及优化收敛慢的问题。
*   **整体含义**：SwiftNDC 旨在通过一个轻量级的神经修正场，将快速但模糊的深度图转化为高保真的几何初始化，从而桥接“快速深度估计”与“高质量辐射场优化”之间的鸿沟。

### 2. 方法论
*   **核心思想**：利用稀疏的 SfM（Structure from Motion）点云作为锚点，通过一个轻量级的多层感知机（MLP）在像素级修正前馈网络的深度输出。
*   **关键技术流程**：
    1.  **初始深度获取**：同时提取多视图深度（VGGT，保证全局一致性）和单目深度（Video Depth Anything，捕捉精细细节）。
    2.  **神经深度修正场 (NDC)**：构建一个 MLP $f_\theta$，输入像素坐标、视图索引及初始深度特征，预测像素级的仿射变换系数（缩放 $\alpha$ 和偏移 $\beta$）。
    3.  **两阶段训练策略**：
        *   *全局优化*：在整个场景上训练以捕捉系统性偏差。
        *   *局部微调*：对每张视图进行极速微调（<1秒），消除残余的局部误差。
    4.  **重投影误差过滤**：通过跨视图的重投影循环校验，剔除深度图中的离群点，确保生成的稠密点云在几何上是自洽的。
    5.  **下游集成**：将过滤后的稠密点云作为 3DGS 的初始化，用于快速网格提取或高质量视角合成。

### 3. 实验设计
*   **数据集**：
    *   **网格重建**：DTU（物体级）、Tanks and Temples (TnT)（场景级）。
    *   **新视角合成**：MipNeRF 360、Tanks and Temples、Deep Blending。
*   **Benchmark 与对比方法**：
    *   **神经隐式方法**：NeRF, VolSDF, NeuS, Neuralangelo。
    *   **3DGS 变体**：原始 3DGS, SuGaR, 2DGS, GOF, PGSR。
*   **评价指标**：Chamfer Distance (CD)、F1 Score、PSNR、SSIM、LPIPS。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA L40S (48 GB VRAM)**。
*   **训练/运行耗时**：
    *   **深度修正阶段**：对于少于 60 张图像的场景，整个修正过程（含推理与训练）在 **1 分钟内**完成。
    *   **网格重建**：在 DTU 数据集上，仅需 1k 次 3DGS 迭代（约 3 分钟）即可达到 SOTA 精度；在 TnT 数据集上总耗时约 26 分钟。
    *   **对比**：比 Neuralangelo（>128小时）快数百倍，比 PGSR（45分钟）和 GOF（2小时）显著更高效。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流数据集上进行了广泛测试，涵盖了从室内物体到大规模室外场景的多种复杂情况。
*   **消融实验**：非常充分。作者分别验证了单目与多视图深度融合的必要性、两阶段训练的加速效果、像素级修正相对于全局仿射对齐的优越性，以及重投影过滤对几何质量的影响。
*   **客观性**：通过与多种不同类型的基准方法（隐式 vs 显式）在相同硬件和标准指标下对比，证明了其在速度与精度平衡点上的领先地位。

### 6. 主要结论与发现
*   **几何初始化的重要性**：可靠的稠密几何初始化能极大加速 3DGS 的收敛，并显著提升其在弱观测区域的渲染保真度。
*   **精度突破**：SwiftNDC 在不进行 3DGS 优化的前提下（仅靠修正深度融合），其网格精度已优于许多需要长时间优化的方法。
*   **效率提升**：通过摊销优化（Amortized Optimization），成功将高质量深度修正的成本降至秒级。

### 7. 优点
*   **极速性能**：实现了亚分钟级的深度修正和分钟级的网格重建。
*   **即插即用**：生成的稠密点云可直接作为现有 3DGS 框架的增强前端，具有很强的通用性。
*   **鲁棒性**：结合了单目细节和多视图一致性，有效解决了传统 MVS 方法在弱纹理区域失效的问题。

### 8. 不足与局限
*   **位姿依赖**：高度依赖 COLMAP 等工具提供的准确相机位姿和初始稀疏点云。
*   **预处理负担**：虽然优化快，但引入了额外的深度模型推理和重投影过滤步骤，在处理超大规模图像库时，预处理时间占比会增加。
*   **非端到端**：深度修正与后续的辐射场优化是解耦的，未来若能实现端到端联合优化，可能进一步提升细节表现。

（完）
