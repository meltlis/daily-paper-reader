---
title: "GTSR: Subsurface Scattering Awared 3D Gaussians for Translucent Surface Reconstruction"
title_zh: GTSR：面向半透明表面重建的次表面散射感知 3D 高斯
authors: "Youwen Yuan, Xi Zhao"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22036v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 使用3DGS重建半透明物体的表面几何
tldr: 针对半透明物体重建中计算成本高且难以处理光学特性的挑战，本文提出GTSR框架。该方法基于3D高斯泼溅（3DGS），通过结合表面和内部两组高斯球来模拟光线穿过半透明物体时的散射效果。利用菲涅耳项进行混合渲染，并引入Disney BSDF模型增强几何约束，实现了高质量且实时的半透明物体表面重建。
selection_source: fresh_fetch
motivation: 现有的3DGS方法难以处理半透明物体的次表面散射特性，而传统隐式场方法计算开销过大。
method: 提出一种双层高斯结构（表面与内部），结合菲涅耳项混合渲染，并利用Disney BSDF模型进行延迟渲染以优化法向和深度。
result: 在NeuralTO Syn数据集及扩展数据集上，该方法在重建精度和实时渲染性能上均优于现有基准方法。
conclusion: GTSR成功将3DGS扩展到半透明材质领域，为高效、高精度的半透明物体几何重建提供了新方案。
---

## 摘要
从多视图图像中重建半透明物体是一个难题。此前，研究人员使用了可微路径追踪和神经隐式场，这些方法需要较高的计算成本。最近，许多基于 3DGS 流水线的工作在不透明物体的重建上取得了良好的效果，且效率更高。然而，这些方法难以处理半透明物体，因为它们没有考虑半透明物体的光学特性。在本文中，我们提出了一种新型的基于 3DGS 的流水线（GTSR），用于重建半透明物体的表面几何。GTSR 结合了两组高斯函数：表面高斯和内部高斯，分别用于建模光线穿过半透明物体时的表面颜色和散射颜色。为了渲染半透明物体的外观，我们引入了一种利用菲涅耳（Fresnel）项来混合这两组高斯函数的方法。此外，为了改进非轮廓区域的重建细节，我们引入了带有延迟渲染的 Disney BSDF 模型，以增强法线和深度的约束。实验结果表明，我们的方法在 NeuralTO Syn 数据集上优于基准重建方法，同时展现出卓越的实时渲染性能。我们还通过具有不同材料属性的新半透明物体扩展了数据集，并证明了我们的方法能够适应不同的半透明材料。

## Abstract
Reconstructing translucent objects from multi-view images is a difficult problem. Previously, researchers have used differentiable path tracing and the neural implicit field, which require relatively large computational costs. Recently, many works have achieved good reconstruction results for opaque objects based on a 3DGS pipeline with much higher efficiency. However, such methods have difficulty dealing with translucent objects, because they do not consider the optical properties of translucent objects. In this paper, we propose a novel 3DGS-based pipeline (GTSR) to reconstruct the surface geometry of translucent objects. GTSR combines two sets of Gaussians, surface and interior Gaussians, which are used to model the surface and scattering color when lights pass translucent objects. To render the appearance of translucent objects, we introduce a method that uses the Fresnel term to blend two sets of Gaussians. Furthermore, to improve the reconstructed details of non-contour areas, we introduce the Disney BSDF model with deferred rendering to enhance constraints of the normal and depth. Experimental results demonstrate that our method outperforms baseline reconstruction methods on the NeuralTO Syn dataset while showing great real-time rendering performance. We also extend the dataset with new translucent objects of varying material properties and demonstrate our method can adapt to different translucent materials.

---

## 论文详细总结（自动生成）

以下是对论文《GTSR: Subsurface Scattering Awared 3D Gaussians for Translucent Surface Reconstruction》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何利用 3D 高斯泼溅（3DGS）技术，高效且精确地从多视图图像中重建具有复杂光学特性的**半透明物体**表面几何。
*   **研究动机**：
    *   **半透明性挑战**：光线会穿透表面并在内部发生散射和折射，导致传统的“多视图颜色一致性”假设失效。
    *   **现有方法局限**：基于 NeRF 的方法（如 NeuralTO）计算成本极高；现有的 3DGS 表面重建方法（如 2DGS、PGSR）未考虑次表面散射，优化时高斯球会渗入物体内部且透明度降低，导致重建表面出现空洞或精度低下。

### 2. 方法论
GTSR 提出了一套结合物理特性的 3DGS 改进流水线，核心包含以下关键技术：
*   **双层高斯表示（Surface & Interior Gaussians）**：
    *   **表面高斯**：高密度、高不透明度，专门负责建模几何形状和表面颜色。
    *   **内部高斯**：位于物体内部，专门负责建模次表面散射效果。这种分离避免了渲染质量与几何精度之间的优化冲突。
*   **基于菲涅耳项（Fresnel Term）的混合渲染**：
    *   引入视点相关的透明度计算。利用 Schlick 近似公式计算菲涅耳反射率 $F_r$，并将其作为权重调整表面高斯的不透明度。这模拟了半透明物体在垂直观察时更透明、边缘观察时更不透明的物理特性。
*   **基于 Disney BSDF 的延迟渲染 PBR 模块**：
    *   将渲染分解为镜面反射（Specular）、电介质漫反射（Dielectric Diffuse）和散射（Scattering）三部分。
    *   采用**延迟渲染**策略：先生成深度、法线、基础颜色和粗糙度贴图，再在屏幕空间进行复杂的物理光照计算。这增强了法线和深度的几何约束，提升了非轮廓区域的细节。
*   **约束损失函数**：
    *   除了光度损失和几何一致性损失外，引入了**内部高斯约束（$L_{in}$）**，确保内部高斯球被包裹在表面几何之内，防止其漂移到物体外部。

### 3. 实验设计
*   **数据集**：
    *   **NeuralTO Syn**：包含元宝、中国龙、古龙、熊、钉子、果汁 6 个合成场景。
    *   **扩展数据集**：作者自行渲染了具有不同材质属性的新半透明物体以测试适配性。
    *   **真实世界数据**：在黑暗环境下使用闪光灯拍摄的真实半透明物体图像。
*   **基准方法（Benchmarks）**：
    *   **表面重建对比**：NeuS, HF-NeuS, Ref-NeuS, NeuralTO (SOTA), 2DGS, PGSR。
    *   **新视角合成对比**：NeRF, 3DGS, SSS, GaussianShader 等。
*   **评价指标**：Chamfer Distance (CD) 衡量几何精度；PSNR, SSIM, LPIPS 衡量渲染质量。

### 4. 资源与算力
*   **硬件**：使用单张 **Nvidia RTX A6000** GPU。
*   **训练开销**：训练时长约为 **2.5 小时**。
*   **显存占用**：训练过程中显存消耗**低于 8 GB**。
*   **效率对比**：相比于需要数小时甚至数天的神经隐式场方法，GTSR 在保持高精度的同时显著提升了速度。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个标准合成场景、多个扩展材质场景以及 3 个真实世界场景。
*   **消融实验**：针对 PBR 模块、菲涅耳项、内部高斯球设计了多组对比实验，验证了各组件对几何精度和渲染效果的贡献。
*   **评价客观性**：实验对比了从传统的 NeRF 到最新的 3DGS 变体，覆盖面广。通过可视化法线贴图和不透明度分布，直观展示了方法在处理半透明材质时的优越性，实验设计较为充分且公平。

### 6. 主要结论与发现
*   **重建精度领先**：GTSR 在 NeuralTO 数据集上的平均倒角距离（CD）达到最低，显著优于现有的 3DGS 重建方法。
*   **渲染质量优异**：在视角合成任务中，PSNR 等指标均达到或超过了专门针对 NVS 优化的方法。
*   **几何与外观解耦**：通过双层高斯和 PBR 模块，成功解决了半透明物体重建中“为了拟合散射颜色而牺牲表面几何”的问题。

### 7. 优点与亮点
*   **物理启发式设计**：巧妙地将菲涅耳效应引入 3DGS 渲染公式，以极小的计算代价模拟了复杂的半透明视觉特征。
*   **高效性**：利用延迟渲染降低了 PBR 计算的复杂度，使其在保持实时渲染能力的同时支持高质量几何优化。
*   **鲁棒性**：在缺乏表面纹理的半透明物体上，通过 PBR 约束依然能恢复出平滑且准确的表面。

### 8. 不足与局限
*   **光照假设限制**：目前假设为**共轭光源（Co-located Light）**，即光源与相机位置重合，无法处理复杂的环境光照或多光源场景。
*   **物理模型简化**：简化了光线传输过程，忽略了折射（Refraction）和背面照明（Back-side illumination），因此在处理玻璃等全透明物体时效果有限。
*   **极细微细节缺失**：对于极其微小的几何特征（如龙鳞），由于材质解耦不够彻底，重建结果仍可能存在轻微模糊。

（完）
