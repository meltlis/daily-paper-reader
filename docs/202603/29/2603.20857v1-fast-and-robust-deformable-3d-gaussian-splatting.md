---
title: Fast and Robust Deformable 3D Gaussian Splatting
title_zh: 快速且鲁棒的可变形 3D 高斯泼溅
authors: "Han Jiao, Jiakai Sun, Lei Zhao, Zhanjie Zhang, Wei Xing, Huaizhong Lin"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20857v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 用于动态场景的可变形3D高斯泼溅
tldr: 针对动态场景重建中3D高斯泼溅（3DGS）存在的渲染速度慢、依赖初始点云及暗光场景易陷入局部最优等问题，本文提出FRoG框架。通过引入高斯嵌入与粗到细的时间嵌入策略加速渲染，并结合深度与误差引导的采样策略增强对稀疏初始化的鲁棒性，同时利用透明度调节优化暗光表现，实现了兼具高渲染速度与SOTA视觉质量的动态重建。
selection_source: fresh_fetch
motivation: 现有的基于变形场的动态3DGS方法在渲染效率、初始化鲁棒性以及暗光场景处理方面存在局限。
method: 提出FRoG框架，采用时间嵌入早期融合加速渲染，并引入深度与误差引导的采样及透明度调节策略。
result: 实验证明该方法在保持最先进视觉质量的同时，显著提升了渲染速度并增强了对复杂场景的鲁棒性。
conclusion: FRoG为高质量、高效率的动态场景重建提供了一个稳健的解决方案，有效解决了变形场优化中的核心挑战。
---

## 摘要
3D 高斯泼溅（3D Gaussian Splatting）在静态场景的新视角合成中展现了卓越的实时渲染能力和优异的视觉质量。基于这些优势，研究人员已逐步将 3D 高斯扩展到动态场景重建。在各种技术中，基于变形场的方法已成为一种极具前景的方法。这些方法在标准场（canonical field）中维护 3D 高斯属性，并利用变形场在时间序列中对该场进行变换。然而，这些方法经常面临渲染速度不理想、对初始点云高度依赖以及在昏暗场景中易陷入局部最优等挑战。为了克服这些局限性，我们提出了 FRoG，这是一个用于高质量动态场景重建的高效且鲁棒的框架。FRoG 将逐高斯嵌入（per-Gaussian embedding）与由粗到精的时间嵌入策略相结合，通过时间嵌入的早期融合来加速渲染。此外，为了增强对稀疏初始化的鲁棒性，我们引入了一种新型的深度和误差引导采样策略。该策略在低偏差的初始位置为标准场填充新的 3D 高斯，显著减轻了变形场的优化负担，并改善了静态和动态区域的细节重建。此外，通过调节不透明度变化，我们缓解了昏暗场景中的局部最优问题，提高了色彩保真度。全面的实验结果验证了我们的方法在保持最先进视觉质量的同时，实现了更快的渲染速度。

## Abstract
3D Gaussian Splatting has demonstrated remarkable real-time rendering capabilities and superior visual quality in novel view synthesis for static scenes. Building upon these advantages, researchers have progressively extended 3D Gaussians to dynamic scene reconstruction. Deformation field-based methods have emerged as a promising approach among various techniques. These methods maintain 3D Gaussian attributes in a canonical field and employ the deformation field to transform this field across temporal sequences. Nevertheless, these approaches frequently encounter challenges such as suboptimal rendering speeds, significant dependence on initial point clouds, and vulnerability to local optima in dim scenes. To overcome these limitations, we present FRoG, an efficient and robust framework for high-quality dynamic scene reconstruction. FRoG integrates per-Gaussian embedding with a coarse-to-fine temporal embedding strategy, accelerating rendering through the early fusion of temporal embeddings. Moreover, to enhance robustness against sparse initializations, we introduce a novel depth- and error-guided sampling strategy. This strategy populates the canonical field with new 3D Gaussians at low-deviation initial positions, significantly reducing the optimization burden on the deformation field and improving detail reconstruction in both static and dynamic regions. Furthermore, by modulating opacity variations, we mitigate the local optima problem in dim scenes, improving color fidelity. Comprehensive experimental results validate that our method achieves accelerated rendering speeds while maintaining state-of-the-art visual quality.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **FRoG**（Fast and Robust Deformable 3D Gaussian Splatting）的框架，旨在解决动态场景重建中基于变形场的 3D 高斯泼溅（3DGS）方法存在的效率与鲁棒性问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与背景（研究动机）
*   **核心问题**：现有的动态 3DGS 方法（特别是基于变形场的方法）虽然在视觉质量上有所突破，但仍面临三大挑战：
    1.  **渲染速度慢**：如 E-D3DGS 等方法采用双重变形（Late-fusion）机制，每帧需要多次 MLP 前向计算，导致渲染开销大。
    2.  **初始化依赖性强**：高度依赖初始点云质量。在点云稀疏区域，变形场难以通过梯度下降学习长距离变换，导致细节丢失和运动伪影。
    3.  **暗光场景鲁棒性差**：在昏暗或有阴影的场景中，模型容易陷入局部最优，产生“影子浮点数（shadow floaters）”，导致颜色偏移。

### 2. 方法论
FRoG 提出了三个关键技术模块：
*   **时间嵌入早期融合（Early Fusion for Temporal Embedding）**：
    *   **核心思想**：放弃双重变形路径，改用基于 **Hadamard 乘积（元素级乘法）** 的早期融合策略。
    *   **技术细节**：将粗粒度（Coarse）和细粒度（Fine）时间嵌入进行 Hadamard 乘积运算（$t_{fused} = t_c \odot t_f$），再与逐高斯嵌入拼接后输入单次 MLP。这种非线性交互能有效捕捉局部时间信号，同时将渲染效率提升至单次前向传播水平。
*   **标准场采样策略（Canonical Field Sampling Strategy）**：
    *   **核心思想**：在标准场中直接注入“低成本锚点”来填补稀疏区域。
    *   **技术细节**：利用**中值深度（Median Depth）**（比均值深度更鲁棒，能避开半透明浮点数干扰）和 L1 误差图定位渲染质量差的区域。通过反向投影将这些高误差像素转化为 3D 坐标，直接作为新高斯分布插入标准场，从而简化变形场的优化负担。
*   **激进的不透明度削减策略（Aggressive Opacity Reduction Strategy）**：
    *   **核心思想**：通过惩罚项消除暗光场景中的伪影。
    *   **技术细节**：引入一个额外的惩罚因子 $\phi(k \Delta \alpha)$（其中 $\phi$ 为 Sigmoid 函数，$k$ 为超参数）。当变形场预测不透明度大幅下降时，该策略会更果断地削减其渲染权重，从而抑制靠近相机的“影子浮点数”。

### 3. 实验设计
*   **数据集**：
    *   **N3DV**：多视角固定相机场景（如 *flame salmon*, *cook spinach*），包含长序列和复杂运动。
    *   **HyperNeRF**：单目手机拍摄场景（如 *3D Printer*, *Chicken*），侧重拓扑变化。
    *   **Technicolor**：多视角固定相机，侧重复杂细节。
*   **Benchmark 与对比方法**：
    *   对比了 NeRF 类方法（Mix Voxels, K-Planes, TiNeuVox 等）和 3DGS 类方法（D3DGS, 4DGaussians, E-D3DGS, Swift4D 等）。
    *   **评价指标**：PSNR、SSIM、LPIPS（质量）；Storage、Training Time、FPS（效率）。

### 4. 资源与算力
*   **硬件平台**：使用了 **NVIDIA RTX A6000** 和 **RTX 3090** GPU。
*   **训练时长**：
    *   N3DV 数据集：约 1 小时 56 分钟（相比 E-D3DGS 的 2 小时 41 分钟有所缩短）。
    *   HyperNeRF 数据集：约 1 小时 21 分钟。
*   **渲染速度**：在 A6000 上达到 **91-125 FPS**，显著高于 E-D3DGS（约 40-78 FPS）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大主流数据集的十多个场景，实验设置涵盖了单目和多视角两种主流模式。
*   **消融实验**：非常详尽。分别验证了：
    1.  不同融合策略（Add, Concat, Attention, Dual vs. Product）的优劣。
    2.  采样策略的有效性（对比了无采样和基于逆变形采样的方案）。
    3.  不透明度削减策略中超参数 $k$ 的敏感度分析。
*   **客观性**：为了公平对比，作者对所有基于 3DGS 的方法使用了统一的初始点云脚本，并分别在相同硬件环境下测试了 FPS。

### 6. 主要结论与发现
*   **性能领先**：FRoG 在保持或超越 SOTA 视觉质量（PSNR/SSIM）的同时，渲染速度提升了约 **1.5 到 2 倍**。
*   **鲁棒性增强**：通过标准场采样，FRoG 能在初始点云极度稀疏的区域重建出清晰的背景和动态细节。
*   **色彩修复**：激进的不透明度策略有效解决了暗光场景下的颜色偏移问题，显著提升了 PSNR。

### 7. 优点
*   **平衡性极佳**：在渲染速度（FPS）、训练时间、存储空间和视觉质量之间找到了极好的平衡点。
*   **采样逻辑严密**：识别出“中值深度”在处理动态场景浮点数时的优越性，并提出了比“逆变形映射”更稳健的直接注入法。
*   **即插即用**：提出的早期融合和不透明度策略具有较强的通用性，可推广至其他变形场模型。

### 8. 不足与局限
*   **单目遮挡挑战**：在单目视频中，如果某些区域长期被遮挡（从未被观察到），模型仍会产生模糊或伪影（这是单目重建的固有难题）。
*   **依赖外部先验**：作者提到未来可能需要引入 2D 扩散模型等外部先验来填补完全未观察到的区域。
*   **超参数依赖**：不透明度削减策略中的 $k$ 值需要手动调优，虽然论文给出了推荐值，但在极端场景下可能仍需微调。

（完）
