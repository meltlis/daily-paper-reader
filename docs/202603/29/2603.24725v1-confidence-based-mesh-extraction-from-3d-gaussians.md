---
title: Confidence-Based Mesh Extraction from 3D Gaussians
title_zh: 基于置信度的 3D 高斯网格提取
authors: "Lukas Radl, Felix Windisch, Andreas Kurz, Thomas Köhler, Michael Steiner, Markus Steinberger"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24725v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 从3D高斯中提取网格
tldr: 针对3D高斯泼溅（3DGS）在处理强视图依赖效应场景时网格提取困难的问题，本文提出了一种基于置信度的自监督框架。该框架通过可学习的置信度动态平衡光度与几何监督，并引入了针对基元颜色和法向方差的约束损失，结合改进的解耦外观模型，在保持3DGS高效性的同时，显著提升了无界场景下的几何重建精度。
selection_source: fresh_fetch
motivation: 现有的3DGS网格提取方法在处理复杂视图依赖效应时，往往需要依赖耗时的多视图技术或大型预训练模型，牺牲了3DGS原有的效率优势。
method: 引入自监督置信度机制动态平衡监督信号，并结合基元颜色与法向方差损失以及解耦的D-SSIM外观模型来优化表面提取。
result: 该方法在无界场景的网格提取任务中取得了最先进的性能，同时保持了极高的处理效率。
conclusion: 通过简单的置信度框架和改进的损失函数，可以在不牺牲效率的前提下有效解决3DGS在复杂场景中的几何重建难题。
---

## 摘要
最近，3D 高斯泼溅（3DGS）凭借其显式表示和快速软件光栅化，极大地加速了从位姿图像中提取网格的过程。虽然几何损失和其他先验的加入提高了提取表面的准确性，但在具有大量视角依赖效应的场景中，网格提取仍然具有挑战性。为了解决由此产生的歧义，先前的工作依赖于多视图技术、迭代网格提取或大型预训练模型，这牺牲了 3DGS 固有的效率。在这项工作中，我们通过为 3DGS 引入一种自监督置信度框架，提出了一种简单且高效的替代方案：在该框架内，可学习的置信度值动态地平衡光度监督和几何监督。扩展我们的置信度驱动公式，我们引入了惩罚每个基元颜色和法线方差的损失函数，并证明了它们对表面提取的益处。最后，我们通过解耦 D-SSIM 损失的各个项，利用改进的外观模型对上述内容进行了补充。我们的最终方法在保持高效的同时，为无界网格提供了最先进的结果。

## Abstract
Recently, 3D Gaussian Splatting (3DGS) greatly accelerated mesh extraction from posed images due to its explicit representation and fast software rasterization. While the addition of geometric losses and other priors has improved the accuracy of extracted surfaces, mesh extraction remains difficult in scenes with abundant view-dependent effects. To resolve the resulting ambiguities, prior works rely on multi-view techniques, iterative mesh extraction, or large pre-trained models, sacrificing the inherent efficiency of 3DGS. In this work, we present a simple and efficient alternative by introducing a self-supervised confidence framework to 3DGS: within this framework, learnable confidence values dynamically balance photometric and geometric supervision. Extending our confidence-driven formulation, we introduce losses which penalize per-primitive color and normal variance and demonstrate their benefits to surface extraction. Finally, we complement the above with an improved appearance model, by decoupling the individual terms of the D-SSIM loss. Our final approach delivers state-of-the-art results for unbounded meshes while remaining highly efficient.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **CoMe**（Confidence-Based Mesh Extraction）的方法，旨在从 3D 高斯泼溅（3DGS）表示中高效提取高质量的无界（Unbounded）网格。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：3DGS 在处理具有强**视图依赖效应**（如镜面反射、透明物体）的场景时，几何与外观往往会发生耦合。为了最小化光度误差，优化过程常会产生“虚假几何”（如在表面后方放置高不透明度高斯），导致提取的网格充满噪声和伪影。
*   **研究动机**：现有的改进方法要么依赖耗时的多视图几何约束，要么依赖大型预训练模型（如单目深度/法线估计），这牺牲了 3DGS 原有的高效性。本文旨在探索：在不依赖外部先验的前提下，仅通过自监督手段能将网格提取质量提升到什么程度。

### 2. 方法论
论文提出了一个自监督的置信度框架，核心技术包括：
*   **自监督置信度框架**：为每个高斯基元引入可学习的置信度值 $\hat{C}$。通过损失函数 $L_{conf} = L_{rgb} \cdot \hat{C} - \beta \cdot \log \hat{C}$ 动态平衡光度损失和几何约束。在难以重建的区域（如反射区），系统会降低置信度，从而减弱光度损失的权重，让几何先验主导。
*   **置信度驱动的加密（Densification）**：修改了 3DGS 的加密策略，通过置信度调整梯度阈值，防止在低置信度区域（通常是伪影高发区）过度生成细小高斯。
*   **混合方差损失（Variance Losses）**：
    *   **颜色方差损失**：约束单个高斯的颜色与最终渲染像素颜色的一致性，防止通过多个半透明高斯堆叠来“欺骗”光度损失。
    *   **法线方差损失**：增强单个高斯法线与混合法线的一致性，使表面更平滑。
*   **解耦外观模型（Decoupled Appearance Model）**：改进了 D-SSIM 损失。分析发现 SSIM 中的亮度（Luminance）项受光照影响最大，而对比度和结构项相对稳定。因此，仅对亮度项应用外观补偿，避免外观模型补偿了错误的几何渲染。

### 3. 实验设计
*   **数据集**：
    *   **Tanks & Temples**：主要用于评估无界场景的几何重建（F1-score）。
    *   **ScanNet++**：评估真实室内环境下的鲁棒性（处理曝光变化、模糊等）。
    *   **Mip-NeRF 360**：评估无界场景的渲染质量和背景重建。
    *   **DTU**：评估有界物体的倒角距离（Chamfer Distance）。
*   **对比方法**：包括 GOF、SOF、MiLo（无界 SOTA）、QGS、PGSR（有界 SOTA）等。
*   **基准测试**：统一了数据预处理流程（如 COLMAP 脚本和分辨率），确保对比的公平性。

### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA RTX 4090** GPU 上进行。
*   **耗时**：
    *   **优化时间**：约 **18-20 分钟**。
    *   **对比**：比 MiLo（约 60 分钟）快 3 倍，与 SOF（约 17 分钟）效率相当。
    *   **网格提取**：使用改进的 Marching Tetrahedra，耗时约 6.6 分钟。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了室内、室外、有界、无界等多种场景，实验设计非常全面。
*   **消融实验**：详细测试了置信度框架、方差损失、解耦外观模型以及超参数 $\beta$ 的影响。
*   **客观性**：作者特别指出了数据预处理对结果的影响，并重新运行了所有基准代码以确保在相同输入下进行对比，这种做法在学术上非常严谨。

### 6. 主要结论与发现
*   **性能领先**：在无界场景（Tanks & Temples）中取得了 0.521 的平均 F1 分数，显著优于之前的无界方法。
*   **效率优势**：在保持最先进精度的同时，运行速度远快于依赖复杂约束的方法。
*   **置信度作用**：学习到的置信度图能有效识别出反射表面、薄叶子或观测不足的区域，证明了其作为自监督信号的有效性。

### 7. 优点
*   **纯自监督**：不依赖任何预训练的深度或法线大模型，具有极强的通用性和轻量化特征。
*   **解耦设计**：对 D-SSIM 损失的深入分析和改进非常具有启发性，有效解决了光照不一致问题。
*   **细节保留**：相比于过度平滑的竞争对手（如 MiLo），CoMe 能保留更丰富的拓扑细节。

### 8. 不足与局限
*   **密度依赖**：该框架仍依赖于中等密度的相机采集，在极稀疏视角下可能仍会出现空洞。
*   **远景挑战**：对于极远处的背景，由于视角密度不足，高斯分布不够密集，提取的几何质量会有所下降。
*   **遮挡区域**：在严重遮挡或完全未观测到的区域，仍可能产生凹陷或不完整的几何。

（完）
