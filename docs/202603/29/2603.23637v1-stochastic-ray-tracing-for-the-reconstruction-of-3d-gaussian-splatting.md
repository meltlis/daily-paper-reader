---
title: Stochastic Ray Tracing for the Reconstruction of 3D Gaussian Splatting
title_zh: 用于 3D 高斯泼溅重建的随机光线追踪
authors: "Peiyu Xu, Xin Sun, Krishna Mullia, Raymond Fei, Iliyan Georgiev, Shuang Zhao"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23637v1"
tags: ["query:3dgsir"]
score: 10.0
evidence: 用于可重打光3DGS场景的随机光线追踪
tldr: 本研究针对基于光线追踪的3D高斯泼溅（3DGS）中排序开销大及阴影处理不准确的问题，提出了一种可微分、无需排序的随机光线追踪框架。该框架核心是一个无偏蒙特卡洛估计器，通过每条光线仅采样少量高斯分布来计算梯度，从而绕过了复杂的排序过程。该方法不仅在标准3DGS重建中达到了与光栅化相当的速度和质量，还在可重光照场景中实现了全光线追踪阴影，显著提升了重建精度。
selection_source: fresh_fetch
motivation: 传统光线追踪3DGS因需对光线路径上的所有高斯球进行排序而导致效率低下，且在处理阴影等复杂光效时仍依赖近似方法。
method: 提出一种基于随机采样的无偏蒙特卡洛梯度估计器，通过仅评估每条光线上的一小部分高斯子集来实现无需排序的微分渲染。
result: 在标准场景下达到了与光栅化方法相当的重建速度和质量，在可重光照场景中通过全光线追踪阴影获得了更高的保真度。
conclusion: 该随机光线追踪框架为3DGS提供了一个高效且通用的重建与渲染方案，克服了排序瓶颈并增强了复杂光影效果的表现力。
---

## 摘要
基于光线追踪的 3D 高斯泼溅（3DGS）方法克服了光栅化的局限性——如僵化的针孔相机假设、不准确的阴影以及缺乏原生的反射或折射——但由于需要对每条光线上所有相交的高斯体进行排序，其速度仍然较慢。此外，现有的光线追踪方法在处理可重光照场景时仍依赖于光栅化风格的近似（如阴影映射），这削弱了光线追踪所承诺的通用性。我们提出了一种用于光线追踪 3DGS 的可微、无需排序的随机公式——这是首个利用随机光线追踪来重建和渲染标准及可重光照 3DGS 场景的框架。其核心是一个用于像素颜色梯度的无偏蒙特卡罗估计器，该估计器每条光线仅评估一小部分采样的高斯子集，从而避开了排序的需求。对于标准 3DGS，我们的方法在重建质量和速度上与基于光栅化的 3DGS 相当，同时显著优于基于排序的光线追踪。对于可重光照 3DGS，同样的随机估计器通过全光线追踪的阴影射线驱动每个高斯体的着色，提供了比以往工作显著更高的重建保真度。

## Abstract
Ray-tracing-based 3D Gaussian splatting (3DGS) methods overcome the limitations of rasterization -- rigid pinhole camera assumptions, inaccurate shadows, and lack of native reflection or refraction -- but remain slower due to the cost of sorting all intersecting Gaussians along every ray. Moreover, existing ray-tracing methods still rely on rasterization-style approximations such as shadow mapping for relightable scenes, undermining the generality that ray tracing promises.   We present a differentiable, sorting-free stochastic formulation for ray-traced 3DGS -- the first framework that uses stochastic ray tracing to both reconstruct and render standard and relightable 3DGS scenes. At its core is an unbiased Monte Carlo estimator for pixel-color gradients that evaluates only a small sampled subset of Gaussians per ray, bypassing the need for sorting. For standard 3DGS, our method matches the reconstruction quality and speed of rasterization-based 3DGS while substantially outperforming sorting-based ray tracing. For relightable 3DGS, the same stochastic estimator drives per-Gaussian shading with fully ray-traced shadow rays, delivering notably higher reconstruction fidelity than prior work.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stochastic Ray Tracing for the Reconstruction of 3D Gaussian Splatting》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 3D 高斯泼溅（3DGS）主要依赖光栅化渲染，虽然速度快，但在处理非针孔相机模型、精确阴影、反射和折射时存在局限。现有的基于光线追踪（Ray Tracing）的 3DGS 方法虽然解决了上述问题，但面临两个痛点：
    1.  **排序开销大**：每条光线需要对所有相交的高斯球按深度排序，计算成本高。
    2.  **重打光近似化**：在处理可重光照（Relightable）场景时，仍依赖阴影映射（Shadow Mapping）等光栅化时代的近似技术，未能发挥光线追踪的全部潜力。
*   **研究背景**：本文旨在提出一种**可微分、无需排序**的随机光线追踪框架，既能实现高效的场景重建，又能通过物理精确的光线追踪处理复杂的重光照效果。

### 2. 方法论
*   **核心思想**：利用**无偏蒙特卡洛（Monte Carlo）估计器**来近似像素颜色及其梯度。通过每条光线仅随机采样极少数高斯球（而非处理全部并排序），极大地降低了计算复杂度。
*   **关键技术细节**：
    *   **随机混合（Stochastic Blending）**：在前向传播中，不进行确定性的 Alpha 混合，而是根据高斯球的贡献概率随机选择一个高斯球来代表该像素颜色。
    *   **无偏梯度估计器**：这是本文的核心贡献。作者推导出了一个针对颜色（$c_i$）和不透明度（$\alpha_i$）梯度的随机估计公式。通过在光线上采样两个高斯球（一个当前球 $g_I$ 和一个在其后的球 $g_K$），利用两者的颜色差异来估计梯度，从而避开了对所有高斯球的排序。
    *   **可重光照扩展**：将该估计器应用于阴影射线（Shadow Rays）。每个高斯球的着色通过一个轻量级神经解码器完成，输入包括入射光方向、可见度（通过随机追踪阴影射线获得）和材质特征。
*   **算法流程**：
    1.  **前向阶段**：使用随机采样或基于排序的算法（可选）计算像素颜色。
    2.  **反向阶段**：运行随机采样算法（Algorithm 2），抽取索引对 $(I, K)$，计算梯度并回传至高斯参数（均值、协方差、不透明度等）。

### 3. 实验设计
*   **数据集与场景**：
    *   **标准新视角合成（NVS）**：MipNeRF360、Tanks & Temples、Deep Blending、NeRF Synthetic。
    *   **可重光照重建**：NRHints 数据集（包含 Lego, Basket, Cat 等复杂几何和材质场景）。
*   **对比方法**：
    *   **标准 3DGS**：Vanilla 3DGS（基于光栅化）、3DGRT（基于排序的光线追踪）。
    *   **可重光照方法**：RNG、GS³。
    *   **随机化变体**：StochasticSplats（另一种随机化方案的对比）。
*   **评价指标**：PSNR、SSIM、LPIPS 以及训练/推理耗时。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **Nvidia RTX 5880 Ada Generation GPU** 上运行。
*   **训练时长**：
    *   标准场景（如 MipNeRF360）：约 **20-33 分钟**，速度与光栅化 3DGS 相当，比排序光线追踪（3DGRT）快 2 倍以上。
    *   可重光照场景：包含两个阶段，第一阶段 1.5 万次迭代初始化几何，第二阶段 8.5 万次迭代优化材质与光影。
*   **采样设置**：反向传播通常使用 $M_b = 8$ 个样本。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流 NVS 数据集和 1 个重光照专用数据集，总计测试了数十个不同类型的场景（室内、室外、合成、真实）。
*   **消融与对比**：
    *   进行了**等时间对比（Equal-time comparison）**，证明在相同时间预算下，该方法质量优于 3DGRT。
    *   对比了 **StochasticSplats**，通过可视化梯度图证明了本文方法在处理高不透明度区域时方差更小、重建更稳定。
*   **充分性评价**：实验设计较为全面，不仅验证了精度，还重点验证了光线追踪在处理复杂相机模型（如鱼眼镜头）和环境光照明下的通用性，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能突破**：该方法是首个能同时用于重建和渲染的随机光线追踪 3DGS 框架，成功消除了排序瓶颈。
*   **质量与速度平衡**：在标准 NVS 任务中，其速度和质量可媲美光栅化 3DGS，同时保留了光线追踪的灵活性。
*   **重光照优势**：通过全光线追踪阴影，在重光照基准测试中显著超过了 RNG 和 GS³ 等依赖近似阴影的方法，尤其在阴影区域的几何恢复上表现更优。

### 7. 优点
*   **数学严谨性**：提出了无偏的梯度估计器，解决了随机采样在可微分渲染中的梯度回传难题。
*   **高效性**：无需排序和构建复杂的阴影图，降低了显存占用和计算开销。
*   **通用性**：原生支持非线性相机、环境光照明和精确阴影，无需针对不同效果开发专门的光栅化 Pass。

### 8. 不足与局限
*   **随机方差**：随机采样不可避免地引入了方差，虽然在颜色上表现良好，但作者提到这会影响 3DGS 的**致密化（Densification）和剪枝（Pruning）策略**，导致生成的高斯球数量可能多于传统方法。
*   **依赖硬件**：虽然算法本身通用，但高性能实现高度依赖于 OptiX 等硬件加速光线追踪架构。
*   **超参数敏感**：采样数量（$M_b, M_f$）需要在速度和质量之间进行权衡，对于极度复杂的场景可能需要更多样本。

（完）
