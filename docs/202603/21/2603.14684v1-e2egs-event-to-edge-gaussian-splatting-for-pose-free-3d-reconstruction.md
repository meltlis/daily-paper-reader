---
title: "E2EGS: Event-to-Edge Gaussian Splatting for Pose-Free 3D Reconstruction"
title_zh: E2EGS：面向无位姿三维重建的事件到边缘高斯泼溅
authors: "Yunsoo Kim, Changki Sung, Dasol Hong, Hyun Myung"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14684v1"
tags: ["query:3dgsir"]
score: 9.0
evidence: 基于事件到边缘的高斯喷溅3D重建
tldr: 针对传统3D重建方法在快速运动或恶劣光照下依赖高质量RGB和精确位姿的问题，本文提出E2EGS框架。该框架仅利用事件相机数据，通过时空相干性分析从噪声事件流中提取关键边缘信息，并以此引导高斯初始化、位姿跟踪和捆集调整，实现了无需预知位姿的高质量3D重建和轨迹估计，显著提升了复杂环境下的鲁棒性。
selection_source: fresh_fetch
motivation: 现有基于事件的重建方法通常依赖已知位姿或泛化能力有限的深度模型，难以在未知区域实现稳健的位姿估计和视角合成。
method: 通过基于补丁的时空相干性分析提取边缘，并利用边缘信息指导结构感知的高斯初始化及边缘加权损失函数进行联合优化。
result: 在合成和真实数据集上的实验证明，E2EGS在重建质量和相机轨迹精度方面均达到了领先水平。
conclusion: 该研究成功建立了一个完全无需位姿的事件驱动3D重建新范式，为极端条件下的视觉感知提供了有效方案。
---

## 摘要
神经辐射场 (NeRF) 和 3D 高斯泼溅 (3DGS) 的出现推动了新视角合成 (NVS) 的发展。然而，这些方法需要高质量的 RGB 输入和精确的对应位姿，限制了其在快速相机运动或恶劣光照等现实世界条件下的鲁棒性。事件相机以高时间分辨率和宽动态范围捕捉每个像素的亮度变化，能够实现对动态场景的精确感知，并提供了一个极具前景的解决方案。然而，现有的基于事件的 NVS 方法要么假设位姿已知，要么依赖于受限于初始观测的深度估计模型，在相机穿过先前未见区域时无法泛化。我们提出了 E2EGS，这是一个仅基于事件流运行的无位姿框架。我们的核心见解是，边缘信息提供了对于准确轨迹估计和高质量 NVS 至关重要的丰富结构线索。为了从噪声事件流中提取边缘，我们利用了边缘和非边缘区域截然不同的时空特征。事件相机的运动在边缘处诱发一致的事件，而非边缘区域则产生稀疏噪声。我们通过基于补丁的时间相干性分析来利用这一点，该分析通过测量局部方差来提取边缘，同时鲁棒地抑制噪声。提取的边缘引导了结构感知的高斯初始化，并在初始化、跟踪和光束法平差过程中实现了边缘加权损失。在合成和真实数据集上的广泛实验表明，E2EGS 实现了卓越的重建质量和轨迹精度，为基于事件的三维重建建立了一个完全无位姿的范式。

## Abstract
The emergence of neural radiance fields (NeRF) and 3D Gaussian splatting (3DGS) has advanced novel view synthesis (NVS). These methods, however, require high-quality RGB inputs and accurate corresponding poses, limiting robustness under real-world conditions such as fast camera motion or adverse lighting. Event cameras, which capture brightness changes at each pixel with high temporal resolution and wide dynamic range, enable precise sensing of dynamic scenes and offer a promising solution. However, existing event-based NVS methods either assume known poses or rely on depth estimation models that are bounded by their initial observations, failing to generalize as the camera traverses previously unseen regions. We present E2EGS, a pose-free framework operating solely on event streams. Our key insight is that edge information provides rich structural cues essential for accurate trajectory estimation and high-quality NVS. To extract edges from noisy event streams, we exploit the distinct spatio-temporal characteristics of edges and non-edge regions. The event camera's movement induces consistent events along edges, while non-edge regions produce sparse noise. We leverage this through a patch-based temporal coherence analysis that measures local variance to extract edges while robustly suppressing noise. The extracted edges guide structure-aware Gaussian initialization and enable edge-weighted losses throughout initialization, tracking, and bundle adjustment. Extensive experiments on both synthetic and real datasets demonstrate that E2EGS achieves superior reconstruction quality and trajectory accuracy, establishing a fully pose-free paradigm for event-based 3D reconstruction.

---

## 论文详细总结（自动生成）

以下是对论文《E2EGS: Event-to-Edge Gaussian Splatting for Pose-Free 3D Reconstruction》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 3D 重建方法（如 NeRF 和 3DGS）高度依赖高质量的 RGB 图像和精确的相机位姿。然而，在快速运动或极端光照条件下，RGB 图像易产生伪影，且位姿估计往往失效。
*   **研究背景**：事件相机具有高动态范围和高时间分辨率，能捕捉亮度变化（边缘信息），是解决上述问题的理想选择。但现有基于事件的重建方法要么需要预知位姿，要么依赖于泛化能力有限的预训练深度模型，导致在相机探索新区域时误差累积严重。
*   **研究动机**：开发一个完全不依赖预知位姿、不依赖预训练深度模型、仅利用事件流实现高质量 3D 重建和轨迹估计的框架。

### 2. 方法论
E2EGS 的核心思想是利用事件流中蕴含的**边缘结构信息**来引导位姿估计和高斯初始化。
*   **基于补丁的时空相干性分析**：
    *   利用边缘在时间轴上产生一致响应的特性，将事件图划分为重叠补丁。
    *   计算连续补丁间的时空差异方差，通过自适应阈值区分结构化边缘与随机噪声，生成鲁棒的边缘图。
*   **边缘引导的高斯初始化**：
    *   在检测到的边缘位置，利用主成分分析（PCA）确定边缘方向。
    *   采用**逆深度采样（Inverse Depth Sampling）**沿射线放置 3D 高斯点。这种方式在远距离处分配更多样本，能更好地捕捉旋转运动信息，从而辅助位姿优化。
*   **边缘加权损失函数**：
    *   在联合优化位姿和高斯参数时，引入空间权重。对边缘区域赋予更高的损失权重，使优化过程优先对齐几何显著的边界，提高收敛速度和轨迹精度。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：Replica（包含 room0, office0 等 5 个场景）。
    *   **真实数据集**：TUM-VIE（包含 1d, 3d, 6dof, desk 等 5 个序列）。
*   **对比方法（Baselines）**：
    *   **3D 重建**：IncEventGS（带深度监督和随机初始化版本）、Event-3DGS（需位姿）、EvGGS。
    *   **轨迹估计**：DEVO、ESVO2、IncEventGS。
*   **评估指标**：
    *   **NVS 质量**：PSNR、SSIM、LPIPS。
    *   **位姿精度**：绝对轨迹误差（ATE RMSE）。

### 4. 资源与算力
*   **硬件环境**：AMD Ryzen Threadripper PRO 3955WX 处理器，单块 **NVIDIA RTX A5000 GPU**。
*   **软件实现**：基于 3DGS 官方实现及 IncEventGS 的跟踪-映射流水线。
*   **训练时长**：文中未明确给出具体的总训练时长，但提到边缘引导损失能实现更快的收敛。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个合成场景和 5 个真实世界复杂场景，对比了当前最先进的（SOTA）事件驱动重建和里程计方法。
*   **消融实验**：
    *   验证了边缘损失、边缘初始化对位姿精度的贡献。
    *   分析了边缘高斯比例（$r_{edge}$）对重建效果的影响。
*   **充分性评价**：实验设计较为全面，通过长序列测试证明了该方法在处理“相机探索未知区域”时的优越性，对比实验客观且具有说服力。

### 6. 主要结论与发现
*   **摆脱依赖**：E2EGS 证明了仅靠事件流的边缘信息即可实现高精度的无位姿 3D 重建，无需预训练深度模型。
*   **轨迹鲁棒性**：在长序列中，基于边缘的约束比基于深度模型的约束更稳健。深度模型在超出初始观测范围后往往失效，而 E2EGS 能持续提供几何引导。
*   **重建质量**：通过边缘加权优化，重建出的场景边界更清晰，减少了纹理缺失区域的波浪状伪影。

### 7. 优点
*   **完全自主**：不依赖 RGB、位姿先验或外部深度模型，提升了系统在极端环境下的独立运行能力。
*   **几何感知**：创新的时空相干性分析有效滤除了事件相机的噪声，提取出高质量的结构特征。
*   **初始化策略**：逆深度采样策略巧妙地增强了系统对旋转运动的感知，显著降低了轨迹漂移。

### 8. 不足与局限
*   **弱纹理区域挑战**：事件相机在平滑、无纹理区域响应稀疏，导致 E2EGS 在这些区域的深度估计和高斯建模质量仍有提升空间。
*   **参数敏感性**：边缘提取的可靠性受局部事件统计特性的影响，当前策略在极弱几何结构区域可能表现不佳。
*   **算力需求**：虽然使用了 3DGS 框架，但联合优化位姿和大量高斯点在实时性方面可能仍面临挑战（文中未详细讨论实时帧率）。

（完）
